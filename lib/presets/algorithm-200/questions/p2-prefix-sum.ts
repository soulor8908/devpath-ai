// lib/presets/algorithm-200/questions/p2-prefix-sum.ts
// 「算法 200 题」Phase 2 · 前缀和专题（7 题深度题解）
//
// 设计说明：
//   - 每题答案为八段式深度结构：题意约束分析 → 思路推导（从暴力到最优）→
//     正确性要点 → 复杂度对比 → Go 实现 → 边界易错 → 举一反三 → 实际工程应用
//   - 本专题主线：前缀和三重身份——区间查询工具（303/724）、计数与存在性工具
//     （560/523/525/437 配哈希表）、以及它的逆运算差分（1109 区间批量更新）
//   - 旧版答案只贴代码被用户投诉无深度，本版按面试口述标准重写

import type { Question } from "../../../types";

export const P2_PREFIX_SUM_QUESTIONS: Question[] = [
  {
    id: "algo-303",
    nodeId: "p2-prefix-sum",
    question: `303. 区域和检索 - 数组不可变（LeetCode 303）
题意：给定整数数组 nums，实现 NumArray 类支持多次查询 sumRange(left, right)，返回下标区间 [left, right] 内元素之和（含两端）。数组在构造后不再修改。
示例：输入 nums = [-2,0,3,-5,2,-1]，sumRange(0,2) → 输出 1（-2+0+3=1）。
约束：1 ≤ nums.length ≤ 10^4，最多调用 10^4 次 sumRange，-10^5 ≤ nums[i] ≤ 10^5。`,
    answer: `## 题意与约束分析
「数组不可变 + 高频区间求和」是前缀和的标准画像：n 与查询次数都到 10^4，若每次查询 O(n) 暴力累加，总代价 10^8 起步；预处理 O(n)、查询 O(1) 后总代价 O(n+q)，轻松达标。

## 思路推导：从暴力到最优
暴力：每次 sumRange 都遍历 [left,right] 累加，单次 O(n)，q 次 O(nq)。卡点在「同一批元素被反复加」——前缀 [0,r] 的和被算了无数遍。
结构性质：区间和可拆成两个前缀和相减，sum(l,r) = pre[r+1] - pre[l]。前缀和数组只需在构造时算一次，之后每次查询就是一次减法。这是「空间换时间 + 聚合运算可逆（和有减法）」的最简形态。

## 正确性要点
- 定义 pre[i] = nums[0..i-1] 的和（长度 n+1，pre[0]=0），则 pre[r+1]-pre[l] = nums[0..r] 之和 - nums[0..l-1] 之和 = nums[l..r] 之和，与目标区间一一对应，无重无漏。
- pre[0]=0 这个哨兵让 l=0 的查询也走同一公式，无需特判——这是前缀和下标设计的核心约定。

## 复杂度分析
- 时间：构造 O(n)，查询 O(1)，对比暴力查询 O(n)
- 空间：O(n)，前缀和数组

## 代码实现
\`\`\`go
type NumArray struct {
    pre []int // pre[i] = nums[0..i-1] 的和，长度 n+1
}

func Constructor(nums []int) NumArray {
    pre := make([]int, len(nums)+1)
    for i, x := range nums {
        pre[i+1] = pre[i] + x // 递推：多一个元素就加一次
    }
    return NumArray{pre: pre}
}

func (a *NumArray) SumRange(left int, right int) int {
    return a.pre[right+1] - a.pre[left] // 区间和 = 右端前缀 - 左端前缀
}
\`\`\`

## 边界与易错点
- 最常见的挂法是下标 off-by-one：用「pre[i] = 前 i 个元素和」这套约定就要写 pre[r+1]-pre[l]；若用「pre[i] = nums[0..i]（含 nums[i]）」则要写 pre[r]-pre[l-1] 并特判 l=0。两套混用必错。
- 查询区间含 l==r，公式天然成立（pre[r+1]-pre[r] = nums[r]），不用特判。
- 值域 10^5 × 10^4 = 10^9，int32 够用；约束再放大要留意溢出（Go 的 int 为 64 位无此虑）。

## 举一反三
- LC 304 二维区域和检索：二维前缀和，查询用容斥，识别信号「矩阵不可变 + 高频子矩阵求和」。
- LC 560 和为 K 的子数组：前缀和从「查询工具」升级为「计数工具」，配哈希表。
- LC 1480 一维数组的动态和：裸前缀和，练手题。
- 识别信号：「静态数据 + 反复区间聚合」→ 预处理前缀结构；聚合运算需可逆（和有减法、异或自逆）。

## 实际工程应用
前缀和是 OLAP 报表系统的基本功：日活累计表一次预计算，任意日期区间活跃量 O(1) 出数；时序数据库用差分/前缀和加速 range query；音视频里积分图（Summed Area Table）是二维前缀和，支撑图像局部均值滤波 O(1)。`,
    keyPoints: [
      "区间和 = 两个前缀和相减：预处理 O(n)，查询 O(1)",
      "pre[0]=0 哨兵统一 l=0 边界，免特判",
      "前缀和成立的前提是聚合运算可逆（和有减法、异或自逆）",
      "两套下标约定（含/不含当前元素）混用是 off-by-one 的重灾区",
    ],
    followUps: [
      "如果数组会被修改（更新与查询交替），前缀和还够用吗？（不够，升级树状数组/线段树）",
      "推广到二维矩阵怎么做？查询的容斥公式是什么？",
      "前缀和能求区间积吗？含 0 时怎么办？",
    ],
    favorited: false,
  },
  {
    id: "algo-724",
    nodeId: "p2-prefix-sum",
    question: `724. 寻找数组的中心下标（LeetCode 724）
题意：给定整数数组 nums，返回「中心下标」：该下标左侧所有元素之和等于右侧所有元素之和（左右两侧均不含中心下标本身）。若存在多个返回最左边的一个，不存在返回 -1。
示例：输入 nums = [1,7,3,6,5,6] → 输出 3（左和 1+7+3=11，右和 5+6=11）。
约束：1 ≤ nums.length ≤ 10^4，-1000 ≤ nums[i] ≤ 1000；下标 0 的左和、下标 n-1 的右和视为 0。`,
    answer: `## 题意与约束分析
求「左和 == 右和」的下标，本质是找前缀和与后缀和的交点。n ≤ 10^4，O(n) 一次遍历可解，没有优化压力，但它是理解「在线维护前缀和」的最佳入门。

## 思路推导：从暴力到最优
暴力：对每个下标 i，分别遍历左右两侧求和再比较，O(n^2)。卡点明显：左和随 i 右移只是「加上 nums[i-1]」，右和只是「减去 nums[i]」，完全没必要重算。
结构性质：设 total 为总和、left 为 i 左侧和，则右和 = total - left - nums[i]，三者知二推三。于是先 O(n) 求 total，再一次遍历维护 left 并实时判定 left == total - left - nums[i]，在线完成，连前缀和数组都不用存。

## 正确性要点
- 不变式：循环进入第 i 轮时，left 恰好等于 nums[0..i-1] 之和（i=0 时 left=0 成立；每轮结尾 left += nums[i] 维持不变式）。
- 判定式 left == total-left-nums[i] 等价于 left == rightSum(i)，由 total = left + nums[i] + rightSum 移项即得，双向推导严密。
- 从左到右扫描、命中即返回 ⇒ 返回的一定是最左中心下标，满足题意。

## 复杂度分析
- 时间：O(n)，两次线性遍历（求和 + 扫描），对比暴力 O(n^2)
- 空间：O(1)，只用两个标量

## 代码实现
\`\`\`go
func pivotIndex(nums []int) int {
    total := 0
    for _, x := range nums {
        total += x
    }
    left := 0
    for i, x := range nums {
        if left == total-left-x { // 左和 == 右和（右和 = 总和 - 左和 - 自身）
            return i
        }
        left += x // 不变式：left 始终等于 i 左侧元素和
    }
    return -1
}
\`\`\`

## 边界与易错点
- 中心下标「不含自身」，判定式里必须减 x；写成 left == total-left 就错了。
- 左边界 0 的左和为 0、右边界 n-1 的右和为 0，哨兵语义已含在公式里，无需特判；但有人误从 i=1 开始扫，漏掉下标 0。
- 全零数组返回 0（任何下标都是中心，取最左）；单元素数组返回 0。
- 判定必须放在 left += x 之前，顺序写反整体错位一位。

## 举一反三
- LC 1991 找到数组的中间位置：与本题完全同题。
- LC 2270 分割数组的方案数：同样「左和 vs 右和」比较，统计满足 left ≥ right 的分割点数。
- LC 238 除自身以外数组的乘积：把「和」换成「积」，前后缀积 O(1) 额外空间，识别信号「每个位置需要左右两侧的聚合信息」。
- 识别信号：「找平衡点/分割点，左右聚合值满足某关系」→ 总和 + 在线前缀。

## 实际工程应用
负载均衡器选「流量切分点」、数据分片找大小均衡的分区边界，都是中心下标思想：先算总量再扫描累计量找均衡点；金融对账中找「左账 = 右账」的轧差时点也是同一模型。`,
    keyPoints: [
      "右和 = 总和 - 左和 - 自身：知二推三，免去后缀和数组",
      "循环不变式：left 恒等于 i 左侧元素和，判定在累加之前",
      "从左往右命中即返，天然保证最左解",
      "O(1) 空间在线维护前缀和，比存前缀和数组更省",
    ],
    followUps: [
      "改成求乘积相等（左右积）怎么做？含 0 怎么处理？",
      "如果要统计所有满足「左和 ≥ 右和」的分割点个数呢？（LC 2270）",
      "为什么这题不需要真的建前缀和数组？什么场景下必须建？",
    ],
    favorited: false,
  },
  {
    id: "algo-560",
    nodeId: "p2-prefix-sum",
    question: `560. 和为 K 的子数组（LeetCode 560）
题意：给定整数数组 nums（含负数）和整数 k，统计和为 k 的连续子数组个数。子数组必须连续且非空。
示例：输入 nums = [1,1,1], k = 2 → 输出 2（下标 0-1 的 [1,1] 与下标 1-2 的 [1,1]）。
约束：1 ≤ nums.length ≤ 2×10^4，-1000 ≤ nums[i] ≤ 1000，-10^7 ≤ k ≤ 10^7。`,
    answer: `## 题意与约束分析
统计「和为 k 的连续子数组」个数。数组含负数 ⇒ 滑动窗口的单调性前提被破坏（右扩后和不单调增），滑窗出局。n ≤ 2×10^4，O(n^2) 枚举约 4×10^8 次悬，需要 O(n) 级别的计数法。

## 思路推导：从暴力到最优
暴力：枚举所有 (i,j) 区间用前缀和 O(1) 求和，共 O(n^2) 个区间。卡点：对每个 j，都在重复问「有多少个 i ≤ j 使 pre[j]-pre[i]=k」。
结构性质：和为 k ⟺ pre[j] - pre[i] = k ⟺ pre[i] = pre[j] - k。于是从左到右扫描，用哈希表记录「历史上每个前缀和值出现的次数」，每到一个 j，查 cnt[pre[j]-k] 累加进答案——一次遍历完成所有区间的计数。

## 正确性要点
- 双射：每个合法子数组 (i,j] 唯一对应一对前缀和 pre[i]、pre[j] 满足差为 k；扫描到 j 时 cnt 恰好收录所有 i < j 的前缀和，故 cnt[pre[j]-k] 精确等于「以 j 结尾的合法子数组数」，求和无重无漏。
- 初始 cnt[0]=1 对应「空前缀」，让从 nums[0] 开始的子数组（pre[i]=0）也被计入，这是不漏解的关键哨兵。
- 先查后加：统计以 j 结尾的区间时，j 自身的前缀和尚未入表，避免把长度 0 的空区间算进去（k=0 时尤其重要）。

## 复杂度分析
- 时间：O(n)，一次遍历 + 哈希表均摊 O(1) 查询，对比暴力 O(n^2)
- 空间：O(n)，最坏情况下前缀和全不同

## 代码实现
\`\`\`go
func subarraySum(nums []int, k int) int {
    cnt := map[int]int{0: 1} // 哨兵：空前缀和 0 出现 1 次
    pre, ans := 0, 0
    for _, x := range nums {
        pre += x
        ans += cnt[pre-k] // 历史上有几个前缀和 = pre-k，就有几个以当前结尾的合法子数组
        cnt[pre]++        // 先查后加，避免计入空区间
    }
    return ans
}
\`\`\`

## 边界与易错点
- 漏写 cnt[0]=1：导致所有从数组头开始的合法子数组丢失，k 恰等于某前缀和时答案偏小。
- 「先查后加」顺序写反：k=0 时会把「自身减自身」的空区间算进来，答案虚高。
- 试图用滑窗：含负数时右扩和可增可减，滑窗漏解——这是本题最大的思维陷阱，也是面试官必考点。
- pre 不必存数组，滚动变量即可；哈希表 key 是前缀和的值，不是下标。

## 举一反三
- LC 974 和可被 K 整除的子数组：前缀和 mod k 同余计数，识别信号「和整除 k」。
- LC 523 连续的子数组和：同余 + 长度限制，存在性判定。
- LC 930 和相同的二元子数组：数组为 0/1 非负，滑窗可行，是「非负」换来的替代解。
- 识别信号：「含负数 + 连续子数组 + 和等于/整除定值」→ 前缀和 + 哈希计数。

## 实际工程应用
「区间聚合值等于目标」的计数在业务里高频出现：交易系统找「累计流水恰好抹平」的时段、风控里找「净资金流为阈值」的连续窗口、日志分析里找「错误数 - 成功数 = k」的时段——前缀和 + 哈希是在线流式处理这类问题的标准件，实时累计窗口背后就是这套思想。`,
    keyPoints: [
      "pre[j]-pre[i]=k ⟺ pre[i]=pre[j]-k：把区间计数转成前缀和值计数",
      "cnt[0]=1 哨兵覆盖从头开始的子数组，漏写必丢解",
      "先查后加防止把空区间算进去（k=0 时致命）",
      "含负数是滑窗的禁区：和不单调，只能前缀和 + 哈希",
    ],
    followUps: [
      "为什么这题不能用滑动窗口？把「等于 k」改成「小于等于 k」呢？",
      "改成「和可被 k 整除」怎么做？（LC 974 同余计数）",
      "如果要求最长的和为 k 的子数组，哈希表该存什么？",
    ],
    favorited: false,
  },
  {
    id: "algo-523",
    nodeId: "p2-prefix-sum",
    question: `523. 连续的子数组和（LeetCode 523）
题意：给定整数数组 nums 和整数 k，判断是否存在长度至少为 2 的连续子数组，其元素和为 k 的整数倍（0 也算 k 的倍数）。
示例：输入 nums = [23,2,4,6,7], k = 6 → 输出 true（[2,4] 和为 6，或全体和为 42，均为 6 的倍数）。
约束：1 ≤ nums.length ≤ 10^5，0 ≤ nums[i] ≤ 10^9，1 ≤ k ≤ 2^31-1。`,
    answer: `## 题意与约束分析
判定「长度 ≥ 2、和为 k 倍数」的连续子数组是否存在。n ≤ 10^5 ⇒ O(n^2) 出局，需要近 O(n)。「k 的倍数」提示模运算；「长度 ≥ 2」提示要记录下标而不只是计数。

## 思路推导：从暴力到最优
暴力：枚举所有长度 ≥ 2 的区间，O(n^2)。卡点同 LC 560。
结构性质是同余定理：区间和 (i,j] 能被 k 整除 ⟺ (pre[j]-pre[i]) % k == 0 ⟺ pre[j] % k == pre[i] % k——「差是 k 的倍数」与「余数相同」互为充要条件。于是扫描时只需记录每个余数首次出现的下标；遇到相同余数且下标差 ≥ 2 即命中。余数至多 k 种，哈希表规模可控。

## 正确性要点
- 同余双射：(a-b) % k == 0 ⟺ a % k == b % k（模 k 意义下两前缀同余）。命中即存在合法区间，不命中即不存在，充要无遗漏。
- 只记首现下标：同一余数若再次出现，与最早下标跨度最大；首现都不满足长度 ≥ 2，更晚的出现更不可能——保留首现既不漏解又让判定最宽松。
- 哨兵 {0: -1}：空前缀余数 0 记在下标 -1，使「从 nums[0] 开始、长度 ≥ 2 的前缀」也能被正确判定（如全体数组和恰为 k 倍数的情形）。

## 复杂度分析
- 时间：O(n)，一次遍历，对比暴力 O(n^2)
- 空间：O(min(n, k))，余数至多 k 种

## 代码实现
\`\`\`go
func checkSubarraySum(nums []int, k int) bool {
    first := map[int]int{0: -1} // 哨兵：空前缀余数 0 在下标 -1
    pre := 0
    for i, x := range nums {
        pre = (pre + x) % k // 滚动维护前缀和模 k
        if j, ok := first[pre]; ok {
            if i-j >= 2 { // 同余且长度 ≥ 2，命中
                return true
            }
        } else {
            first[pre] = i // 只记首现下标，跨度最大
        }
    }
    return false
}
\`\`\`

## 边界与易错点
- 和为 0 也算 k 的倍数：如 nums=[0,0], k=1 → true；不要额外特判 0，同余框架天然覆盖。
- 「长度 ≥ 2」必须靠下标差判定，只记余数不记下标会误报（单元素余数重复不算）。
- 更新策略写反：每次覆盖下标会让跨度变小，可能把本可达标的区间判丢——只能记首现。
- k 接近 2^31 时 pre+x 可能溢出 int32，Go 的 int 为 64 位安全；用定长整型的语言要取宽型。
- Go 中负数取余结果符号跟随被除数；本题 nums[i] ≥ 0 无需处理，若负数入题需 (pre%k+k)%k 归一化。

## 举一反三
- LC 974 和可被 K 整除的子数组：去掉长度限制、改判定为计数，哈希表存余数出现次数。
- LC 560 和为 K 的子数组：同模板的「差值」版（pre-k）而非「同余」版。
- LC 525 连续数组：同为「前缀和首现下标」求最长区间。
- 识别信号：「连续子数组 + 和整除/模 k」→ 前缀和取模 + 同余表；带长度限制 → 存下标，纯计数 → 存次数。

## 实际工程应用
同余哈希是「周期性对账」的核心技巧：流水系统检查某连续时段交易额是否为手续费档位的整数倍、库存系统校验周期盘点差值能否整除包装规格——把「整除判定」转成「余数匹配」后，一次扫描即可完成，这正是流式校验思想在业务规则引擎里的落地形态。`,
    keyPoints: [
      "同余定理：(a-b)%k==0 ⟺ a%k==b%k，整除判定转余数匹配",
      "长度 ≥ 2 的限制 ⇒ 哈希表存首现下标而非次数",
      "哨兵余数 0 配下标 -1，让从头开始的前缀也能命中",
      "只记首现：同余数最早下标跨度最大，覆盖会丢解",
    ],
    followUps: [
      "为什么余数相同时只保留最早下标？保留最新会怎样？",
      "改成「统计个数」怎么做？（LC 974，存出现次数）",
      "如果数组含负数，取模要怎么处理才能保证同余判定正确？",
    ],
    favorited: false,
  },
  {
    id: "algo-525",
    nodeId: "p2-prefix-sum",
    question: `525. 连续数组（LeetCode 525）
题意：给定只含 0 和 1 的二进制数组，返回含有相同数量 0 和 1 的最长连续子数组的长度。
示例：输入 nums = [0,1,0] → 输出 2（[0,1] 或 [1,0] 均满足）。
约束：1 ≤ nums.length ≤ 10^5，nums[i] ∈ {0,1}。`,
    answer: `## 题意与约束分析
找「0 与 1 个数相等」的最长连续子数组。n ≤ 10^5 ⇒ O(n^2) 出局。「数量相等」是平衡条件，需要把它翻译成前缀和语言才能用哈希表加速。

## 思路推导：从暴力到最优
暴力：枚举区间并数 0/1 个数，O(n^2)。卡点：每次都在重复计数。
结构性质（转化的艺术）：把 0 视为 -1，则「0 和 1 数量相等」⟺ 区间元素和为 0 ⟺ 区间两端前缀和相等（pre[j]==pre[i]）。于是问题归约为「找相同前缀和值的最远一对下标」。扫描时用哈希表记录每个前缀和首次出现的下标，再遇同值即更新最大跨度——一次遍历 O(n) 收工。这一步「01 → ±1」的换元是本题灵魂。

## 正确性要点
- 换元双射：区间内 0 数 = 1 数 ⟺ (1 的个数) - (0 的个数) = 0 ⟺ 换元后区间和为 0 ⟺ pre[j] == pre[i]，两方向均成立。
- 最长性：对同一前缀和值，首现下标 j0 最早 ⇒ 任何后续下标 i 与之配对的跨度 i-j0 是该值下的最大跨度；跨不同值取全局 max 即答案，无遗漏。
- 哨兵 {0: -1}：空前缀和 0 在下标 -1，保证「从 nums[0] 起就平衡」的前缀（如 [0,1]）能算出长度 i-(-1) = i+1。

## 复杂度分析
- 时间：O(n)，一次遍历，对比暴力 O(n^2)
- 空间：O(n)，前缀和值域 [-n, n] 至多 2n+1 种

## 代码实现
\`\`\`go
func findMaxLength(nums []int) int {
    first := map[int]int{0: -1} // 哨兵：前缀和 0 首现于下标 -1
    pre, ans := 0, 0
    for i, x := range nums {
        if x == 0 {
            pre-- // 0 换元为 -1
        } else {
            pre++
        }
        if j, ok := first[pre]; ok {
            if i-j > ans { // 同值前缀再相遇，跨度即平衡区间长度
                ans = i - j
            }
        } else {
            first[pre] = i // 只记首现，保证跨度最大
        }
    }
    return ans
}
\`\`\`

## 边界与易错点
- 忘换元直接数 0/1：失去前缀和的可用结构，只能退回 O(n^2)。换元是解题入口。
- 首现下标被覆盖：遇到同值前缀若更新下标，跨度会变小，最长解丢失——只写一次。
- 漏哨兵 {0: -1}：从头平衡的数组（如 [0,1,1,0]）算不出全长。
- 「子数组」是连续的，别和「子序列」混淆；若题改子序列则答案恒为 2×min(0 的个数, 1 的个数)。

## 举一反三
- LC 560 和为 K 的子数组：同为前缀和 + 哈希，求「计数」而非「最长」，存次数而非首现下标。
- LC 523 连续的子数组和：同余 + 首现下标，判定存在性。
- LC 1124 表现良好的最长时间段：「>8 小时记 +1、否则 -1」，求和为正的最长区间——同款换元，但条件从「相等」变「大于」，需单调栈增强。
- 识别信号：「两类元素数量平衡/差值固定的最长连续区间」→ 换元转前缀和 + 首现下标表。

## 实际工程应用
换元 + 前缀和是「平衡度分析」的通用框架：监控系统找「成功与失败调用数持平的最长时间窗」评估稳定性边界、量化交易里找「买卖量均衡的最长时段」、网络 QoS 里找上下行流量平衡窗口——核心都是把两类计数差映射成 ±1 游走，相同高度重逢即平衡区间，与随机游走重返原点的分析同源。`,
    keyPoints: [
      "换元是灵魂：0 变 -1，「数量相等」转「区间和为 0」",
      "和为 0 ⟺ 两端前缀和相等：找同值前缀的最远下标对",
      "求最长存首现下标，求个数存出现次数，别记混",
      "哨兵前缀和 0 在下标 -1，覆盖从头平衡的解",
    ],
    followUps: [
      "把条件改成「1 比 0 多 k 个的最长子数组」怎么做？",
      "为什么求最长只能存首现下标，而求计数要存出现次数？",
      "LC 1124（和为正的最长区间）为什么不能直接套同值前缀模板？",
    ],
    favorited: false,
  },
  {
    id: "algo-437",
    nodeId: "p2-prefix-sum",
    question: `437. 路径总和 III（LeetCode 437）
题意：给定二叉树根节点 root 和整数 targetSum，统计路径和等于 targetSum 的路径条数。路径必须沿父子边向下（不必从根开始、不必到叶结束），节点值可为负。
示例：输入 root = [10,5,-3,3,2,null,11,3,-2,null,1], targetSum = 8 → 输出 3（5→3、5→2→1、-3→11 三条）。
约束：节点数 ≤ 1000，-10^9 ≤ Node.val ≤ 10^9，-1000 ≤ targetSum ≤ 1000。`,
    answer: `## 题意与约束分析
数「向下连续路径、和为目标值」的条数。路径起点终点都不固定 ⇒ 每对祖先-后代都是候选，朴素 O(n^2)。节点值含负 ⇒ 路径和不单调，无法提前剪枝。需要把「祖先链上的区间和」翻译成前缀和。

## 思路推导：从暴力到最优
暴力：以每个节点为起点向下 DFS 累加判定，O(n^2)。卡点：任意两节点间路径和被反复计算。
结构性质：设 pre(x) 为根到 x 的路径和，则「祖先 a（不含）到后代 d 的路径和」= pre(d) - pre(a)，与数组版 LC 560 完全同构，只是「前缀序列」从数组变成了每条根到叶的路径。于是 DFS 下行时滚动维护 pre，用哈希表记录当前祖先链上各前缀和出现次数，每到一节点查 cnt[pre-target]；离开子树时把本节点前缀和从表中撤销（回溯），保证表里只有当前路径的祖先，兄弟子树互不污染。

## 正确性要点
- 与 LC 560 的双射同构：合法路径 ⟺ 同一条根-叶链上两个前缀和差为 target；哈希表在任意时刻恰好装当前节点全部祖先（处理自身前不含自身）的前缀和，查询结果精确无重无漏。
- 回溯撤销是正确性关键：递归返回前 cnt[pre]--，保证访问兄弟子树时表里没有本分支的前缀和——路径不能跨分支，撤销维护了「祖先链」这一不变式。
- 哨兵 {0:1} 让「从根出发的路径」被计入；先查后加避免空路径（target=0 时自身减自身）。

## 复杂度分析
- 时间：O(n)，每节点入出栈各一次、哈希操作均摊 O(1)，对比暴力 O(n^2)
- 空间：O(h)，h 为树高（哈希表 + 递归栈），最坏链树 O(n)

## 代码实现
\`\`\`go
func pathSum(root *TreeNode, targetSum int) int {
    cnt := map[int64]int{0: 1} // 哨兵：空前缀（根的上方）
    var ans int
    var dfs func(*TreeNode, int64)
    dfs = func(node *TreeNode, pre int64) {
        if node == nil {
            return
        }
        pre += int64(node.Val)         // 根到当前节点的路径和
        ans += cnt[pre-int64(targetSum)] // 祖先链上有几个合法起点
        cnt[pre]++
        dfs(node.Left, pre)
        dfs(node.Right, pre)
        cnt[pre]-- // 回溯：离开前撤销，兄弟子树互不影响
    }
    dfs(root, 0)
    return ans
}
\`\`\`

## 边界与易错点
- 忘回溯 cnt[pre]--：跨分支的「伪路径」被计入，答案虚高——这是本题第一易错点。
- 先加后查：target=0 时把空路径（自身减自身）算进去。
- 溢出：节点值 ±10^9 × 1000 节点，路径和超 int32，用 int64 承接；哈希表 key 类型要匹配。
- 路径方向只能父到子，不可回溯向上——前缀和模型天然保证（只查祖先）。
- 空树返回 0，哨兵不影响（无节点触发查询）。

## 举一反三
- LC 560 和为 K 的子数组：数组版同款「pre-k 计数」，树版多了回溯撤销。
- LC 112 / LC 113 路径总和 I/II：根到叶固定端点版，DFS 累减目标值即可，无需哈希。
- LC 124 二叉树中的最大路径和：路径可转弯（经 LCA），模型不同，用后序递归返回单边最大贡献。
- 识别信号：「树上向下路径 + 和条件 + 端点自由」→ 根路径前缀和 + 哈希 + 回溯。

## 实际工程应用
「层级结构上的累计值匹配」广泛存在：文件系统里找「目录累计大小恰好为配额」的链路段、组织架构里统计「任意上级到下级的累计成本等于预算」的链路、调用链追踪（Trace）里找「累计耗时达到告警阈值」的 span 区间——树上前缀和 + 回溯是这类「沿单一路径聚合」问题的统一解法，也是 DOM 树、AST 分析中的常用技巧。`,
    keyPoints: [
      "根到节点路径和即「树上前缀和」，与数组版 LC 560 完全同构",
      "哈希表任意时刻只装当前祖先链：靠回溯撤销维护这个不变式",
      "先查后加 + cnt[0]=1 哨兵，覆盖从根出发的路径且防空路径",
      "节点值含负 ⇒ 路径和不单调 ⇒ 不能剪枝，必须全量计数",
    ],
    followUps: [
      "为什么离开子树必须 cnt[pre]--？不撤销会错在哪？",
      "如果路径必须从根开始，算法怎么简化？",
      "路径允许转弯（经过 LCA，如 LC 124）时为什么这套模型失效？",
    ],
    favorited: false,
  },
  {
    id: "algo-1109",
    nodeId: "p2-prefix-sum",
    question: `1109. 航班预订统计（LeetCode 1109）
题意：n 个航班编号 1..n，bookings[i] = [first, last, seats] 表示给 [first, last] 区间内每个航班预订 seats 个座位。返回每个航班的最终预订总数数组。
示例：输入 bookings = [[1,2,10],[2,3,20],[2,5,25]], n = 5 → 输出 [10,55,45,25,25]（航班 2 被三条记录覆盖：10+20+25=55）。
约束：1 ≤ n ≤ 2×10^4，1 ≤ bookings.length ≤ 2×10^4，1 ≤ first ≤ last ≤ n。`,
    answer: `## 题意与约束分析
m 次「区间加」操作后输出全量数组。m、n 都到 2×10^4，若每次操作 O(len) 逐个加，最坏 O(mn) = 4×10^8 悬。区间更新远多于读取 ⇒ 差分数组把每次区间加压到 O(1)。

## 思路推导：从暴力到最优
暴力：每条 booking 遍历 [first,last] 逐个加 seats，O(mn)。卡点：同一航班被多条记录重复更新，大量加法浪费。
结构性质：差分是前缀和的逆运算——若 diff 是 ans 的差分（diff[i]=ans[i]-ans[i-1]），则「给 ans[l..r] 同加 v」在差分上只改两点：diff[l]+=v、diff[r+1]-=v。因为差分只对「边界的增量变化」敏感，区间内部相邻差不变。于是所有操作先在差分数组上 O(1) 打标记，最后一次前缀和还原出真实数组，总代价 O(m+n)。

## 正确性要点
- 逆运算双射：ans[i] = diff[0..i] 的前缀和，diff[i] = ans[i]-ans[i-1]，二者互逆、信息无损。
- 区间加的正确性：diff[l]+=v 使 l 及之后所有前缀和都 +v；diff[r+1]-=v 使 r+1 及之后都 -v；两相抵消后恰有 ans[l..r] 净 +v，区间外不变——逐点验证成立。
- 操作可叠加：多次区间加在差分上是独立打点，加法交换律保证顺序无关，最终一次前缀和统一结算。

## 复杂度分析
- 时间：O(m+n)，m 次 O(1) 打点 + 一次 O(n) 还原，对比暴力 O(mn)
- 空间：O(n)，差分数组

## 代码实现
\`\`\`go
func corpFlightBookings(bookings [][]int, n int) []int {
    diff := make([]int, n+1) // 多留一位，r+1 永不越界
    for _, b := range bookings {
        l, r, seats := b[0]-1, b[1]-1, b[2] // 航班号 1-based 转 0-based
        diff[l] += seats
        diff[r+1] -= seats // 区间右端后一位打负标记
    }
    ans := make([]int, n)
    cur := 0
    for i := 0; i < n; i++ {
        cur += diff[i] // 前缀和还原真实值
        ans[i] = cur
    }
    return ans
}
\`\`\`

## 边界与易错点
- 1-based 转 0-based：航班号从 1 开始，下标换算错一位全盘皆错。
- diff 数组长度开 n 而非 n+1：当 r=n-1 时 r+1=n 越界；或者对 r+1 做边界判断，反而啰嗦。
- 试图边打标记边还原：必须所有操作打完再统一前缀和，否则后续 diff[l] 的增量影响不到已结算的位置。
- 差分的适用边界：只在「批量区间加、最后整体读」场景用；若更新与点查交替出现，差分每次查询要 O(n) 还原，应升级树状数组/线段树。

## 举一反三
- LC 370 区间加法：裸差分，练手题。
- LC 1094 拼车：上下车即「区间载客数 ±」，按站点展开后差分求峰值，识别信号「区间占用 + 问最大值」。
- LC 253 会议室 II：同一模型的「时间点差分」（进场 +1、出场 -1），求同时在线峰值。
- 识别信号：「批量区间同加同减 + 最后一次性查询」→ 差分；「更新查询交替」→ 树状数组/线段树。

## 实际工程应用
差分是「批量区间配置下发」的利器：限流系统给「10:00-12:00 时段限速上调」这类区间规则打点，最后一次前缀和出全时段配置表；游戏里「某时间段全服 Buff 叠加 N 层」、CDN「某区域带宽配额区间调整」都是先差分记账、定期统一结算；日历系统的忙闲统计（会议区间 +1）求最大并发会议室数也是同款。`,
    keyPoints: [
      "差分是前缀和的逆运算：区间加 O(1) 打点，一次前缀和 O(n) 还原",
      "区间 [l,r] 加 v ⟺ diff[l]+=v 且 diff[r+1]-=v，内部相邻差不变",
      "diff 多开一位，r+1 永不越界，免边界特判",
      "适用边界：批量更新 + 末尾整体读；交替读写要升级树状数组",
    ],
    followUps: [
      "如果操作改成「区间加 + 随时单点查询」，差分还够用吗？该换什么？",
      "二维差分怎么写？「子矩阵同加 v」要打几个点？",
      "拼车题（LC 1094）怎么建模成差分？",
    ],
    favorited: false,
  },
];
