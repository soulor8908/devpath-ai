// lib/presets/algorithm-200/questions/p1-tree.ts
// 「算法 200 题」内置知识库 · Phase1 二叉树与 BFS/DFS 深度题解（13 题）
// 每题答案固定八段：题意与约束分析 / 思路推导：从暴力到最优 / 正确性要点 /
// 复杂度分析 / 代码实现 / 边界与易错点 / 举一反三 / 实际工程应用
// 代码统一 Go 实现；TreeNode 定义（Val int; Left, Right *TreeNode）各题省略并以注释说明

import type { Question } from "../../../types";

export const P1_TREE_QUESTIONS: Question[] = [
  {
    id: "algo-94",
    nodeId: "p1-tree",
    question: `94. 二叉树中序遍历（LeetCode 94）\n题意：给定二叉树的根节点，按左-根-右的顺序返回中序遍历的值序列。\n示例：输入 [1,null,2,3] → 输出 [1,3,2]（根 1 无左子树先输出，右子树中 2 的左孩子 3 先于 2 输出）\n约束：节点数 0~100，节点值为任意整数`,
    answer: `## 题意与约束分析
返回二叉树中序遍历（左-根-右）的值序列。n ∈ [0, 100]，O(n) 绰绰有余；考点是递归之外的迭代写法与 O(1) 空间的 Morris 遍历。

## 思路推导：从暴力到最优
中序的定义即算法：先左、再根、再右，递归最直白。追问「不用递归」用显式栈模拟调用栈：一路向左压栈到底，弹栈访问，转右子树重复。追问「空间 O(1)」引出 Morris：借叶子空右指针做线索指回中序后继，用完还原。三法层层递进。

## 正确性要点
- 递归语义：inorder(node) 输出该子树中序序列，空节点直接返回，归纳成立。
- 迭代不变式：弹栈时左子树已全部输出，此刻输出根恰好满足「左-根-右」。
- cur = cur.Right 后重复左压栈，右子树同样按中序处理；循环条件 cur != nil 或栈非空，不丢分支。

## 复杂度分析
- 时间 O(n)：每节点入栈出栈各一次；输出本身 n 个元素，无更优渐近界。
- 空间 O(h)：递归与显式栈均为树高；Morris O(1) 但暂改树结构。

## 代码实现
\`\`\`go
// TreeNode 定义省略：type TreeNode struct { Val int; Left, Right *TreeNode }

// 递归：dfs(node.Left); 取值; dfs(node.Right)，略。
// 迭代 + 显式栈（面试主写）
func inorderTraversal(root *TreeNode) []int {
    res := []int{}
    var stack []*TreeNode
    cur := root
    for cur != nil || len(stack) > 0 {
        for cur != nil { // 一路向左压栈到底
            stack = append(stack, cur)
            cur = cur.Left
        }
        cur = stack[len(stack)-1] // 弹出：左子树已全部输出
        stack = stack[:len(stack)-1]
        res = append(res, cur.Val)
        cur = cur.Right // 转右子树重复
    }
    return res
}
\`\`\`

## 边界与易错点
- 空树：递归天然处理；迭代外层条件必须含「栈非空」。
- 最常见错误：弹栈后忘记 cur = cur.Right，死循环反复访问同一节点。
- Morris 必须还原线索指针，否则树结构被破坏。
- 退化链 n 大时递归栈溢出，工程上选迭代。

## 举一反三
- 144 前序：取值提到压栈时刻，信号「根最先输出」。
- 145 后序：按「根-右-左」走再整体逆序。
- 230 BST 第 K 小：中序第 k 个即答案，BST 中序 = 升序是核心。
- 99 恢复 BST：中序序列定位两个逆序节点交换回来。

## 实际工程应用
BST 中序是数据库 B+ 树有序全量扫描的简化模型：按主键顺序导出、供归并排序输入。迭代版栈模拟是「递归改循环防栈溢出」重构的原型；表达式树输出中缀表达式也套此模板。`,
    keyPoints: ["中序 = 左-根-右，递归定义即算法", "迭代栈不变式：弹栈时左子树已全部输出", "Morris 借空指针做线索换 O(1) 空间，用完必须还原", "BST 中序 = 升序序列，是 230/99 等题的钥匙"],
    followUps: ["前序、后序的迭代写法分别怎么改？", "Morris 遍历的具体步骤是什么，为什么要恢复现场？", "多线程环境下 Morris 暂改树结构会有什么问题？"],
    favorited: false,
  },
  {
    id: "algo-104",
    nodeId: "p1-tree",
    question: `104. 二叉树的最大深度（LeetCode 104）\n题意：给定二叉树根节点，返回从根到最远叶子所经过的节点总数，即最大深度。\n示例：输入 [3,9,20,null,null,15,7] → 输出 3（最深路径 3→20→15 共 3 个节点）\n约束：节点数 0~10^4`,
    answer: `## 题意与约束分析
求最大深度：根到最深叶子经过的节点数。n ∈ [0, 10^4]，O(n) 一次遍历即可；考点在递归语义设计、BFS 替代写法，及与 111 最小深度的区分。

## 思路推导：从暴力到最优
暴力枚举每条根到叶路径取最长，子树高度被重复计算。观察最优子结构：深度 = 1 + max(左深度, 右深度)，后序递归一次遍历完成。BFS 视角：层序遍历每弹空一层计数加一，层数即深度，同样 O(n)。两种范式都该脱口而出。

## 正确性要点
- 递归语义：maxDepth(node) 返回该子树最大深度；空树返回 0。
- 归纳步骤：假设左右子树返回值正确，当前深度 = 1 + 较大者；叶子返回 1，与定义一致。
- BFS：进入第 k 轮循环时队列恰含第 k 层全部节点，轮数 = 深度。

## 复杂度分析
- 时间 O(n)：每节点恰访问一次；枚举路径暴力最坏 O(n²)。
- 空间 O(h)：递归栈；BFS 队列最坏 O(n)（满树底层约 n/2）。

## 代码实现
\`\`\`go
// 写法一：DFS 递归（后序聚合）
func maxDepth(root *TreeNode) int {
    if root == nil { return 0 } // 空树深度为 0
    l, r := maxDepth(root.Left), maxDepth(root.Right)
    if l > r { return l + 1 }
    return r + 1
}

// 写法二：BFS 层序计数
func maxDepthBFS(root *TreeNode) int {
    if root == nil { return 0 }
    queue := []*TreeNode{root}
    depth := 0
    for len(queue) > 0 {
        size := len(queue) // 固定本层节点数
        for i := 0; i < size; i++ {
            node := queue[0]; queue = queue[1:]
            if node.Left != nil { queue = append(queue, node.Left) }
            if node.Right != nil { queue = append(queue, node.Right) }
        }
        depth++ // 弹空一层，深度加一
    }
    return depth
}
\`\`\`

## 边界与易错点
- 空树必须返回 0 而非 1：base case 写错，全部答案加一。
- 与 111 最小深度混淆：单侧子树为空不能取 min，必须走另一侧。
- Go 内置 max 在 1.21 后可用，老版本手写比较，面试提一句体现细节。
- 退化链 n 大时递归栈溢出，工程选 BFS 或显式栈。

## 举一反三
- 111 最小深度：叶子判定不同，信号「最近叶子」。
- 559 N 叉树最大深度：children 循环取 max，模板不变。
- 543 直径：同在后序聚合左右信息，公式从 max 变 sum。

## 实际工程应用
目录深度限制、JSON 嵌套深度校验（防深度炸弹 DoS）、组织架构层级统计；BFS 层计数还用于网络路由最大跳数与任务编排最长依赖链估算。`,
    keyPoints: ["深度 = 1 + max(左, 右)，最优子结构", "空树返回 0 是 base case 关键", "BFS 按层扩展，层数即深度", "与 111 最小深度的叶子判定差异是高频坑"],
    followUps: ["最小深度怎么写？单侧空子树为什么不能用 min？", "不用递归怎么实现？BFS 和迭代 DFS 空间各多少？", "如果要求返回最深叶子节点的值呢？"],
    favorited: false,
  },
  {
    id: "algo-226",
    nodeId: "p1-tree",
    question: `226. 翻转二叉树（LeetCode 226）\n题意：给定二叉树根节点，原地交换每个节点的左右子树，返回翻转后的根。\n示例：输入 [4,2,7,1,3,6,9] → 输出 [4,7,2,9,6,3,1]（每层左右整体镜像）\n约束：节点数 0~100`,
    answer: `## 题意与约束分析
翻转二叉树：每个节点的左右孩子互换，返回根。节点数 [0, 100]。这道递归入门题因 Homebrew 作者 Max Howell 被 Google 拒于白板而闻名——写不出不代表工程能力差，但写出来只需真正理解递归语义。

## 思路推导：从暴力到最优
「翻转」要对每个节点做同一件事：交换左右孩子。遍历每个节点一次即可，问题只剩遍历顺序。前序（先交换当前再递归子树）与后序（先递归子树再交换）都正确——因为交换指针后原左子树已挂到右边，前序版递归 root.Left 实际处理的是原右子树，两个递归恰好覆盖全部节点；后序版先递归再交换，指针名义未变，同样覆盖。用 BFS 队列逐层交换也完全等价，遍历范式不影响结果。

## 正确性要点
- 递归语义：invertTree(node) 将以 node 为根的子树翻转并返回翻转后的根。
- 交换 root.Left 与 root.Right 后，对两个新孩子递归，由归纳假设两棵子树各自完成镜像，整体即镜像。
- 前序后序均可的本质：交换只动当前节点的两个指针，与子树内部状态无耦合，操作与遍历顺序可交换。

## 复杂度分析
- 时间 O(n)：每个节点常数次指针操作，不存在更优渐近界（必须触碰每个节点）。
- 空间 O(h)：递归栈深度，最坏 O(n)；BFS 迭代版队列 O(n) 但无递归栈。

## 代码实现
\`\`\`go
func invertTree(root *TreeNode) *TreeNode {
    if root == nil { return nil }
    root.Left, root.Right = root.Right, root.Left // 交换当前节点左右孩子
    invertTree(root.Left)  // 翻转原子右子树（已挂到左侧）
    invertTree(root.Right) // 翻转原子左子树
    return root
}
\`\`\`

## 边界与易错点
- 空树直接返回 nil，否则访问指针字段时 panic。
- 交换后递归的参数名义已变（root.Left 是原右子树），若先存旧 Left 再按旧变量递归，顺序用错会翻转不完全。
- 误以为要交换节点的值：交换的是子树指针，O(1)；逐点改值既慢又破坏结构语义。
- 原地修改有副作用：若调用方还持有旧树引用，需明确告知。

## 举一反三
- 101 对称二叉树：翻转的「逆问题」，判断一棵树是否等于自身镜像；识别信号是左右镜像结构。
- 100 相同的树：同为双树递归比较模板，把镜像比较改成同位置比较。
- 951 翻转等价二叉树：允许任意节点选择性翻转的等价判定，递归枚举四种匹配组合。

## 实际工程应用
UI 框架做 RTL（阿拉伯语）布局镜像时，对视图树做同样的逐节点左右交换；图像处理的水平镜像、博弈程序中对换黑白双方视角的棋盘翻转，都是「遍历 + 就地变换」范式的实例。`,
    keyPoints: ["交换指针而非值，单点操作 O(1)", "前序后序皆可：交换与子树无耦合", "递归语义设计比代码本身更重要", "Homebrew 作者白板被拒的梗：题简单但考递归直觉"],
    followUps: ["迭代（栈或队列）怎么写？", "前序和后序在这题里为什么都对？", "N 叉树的翻转怎么定义和实现？"],
    favorited: false,
  },
  {
    id: "algo-101",
    nodeId: "p1-tree",
    question: `101. 对称二叉树（LeetCode 101）\n题意：判断一棵二叉树是否关于根节点左右镜像对称，即结构镜像且对应位置的值相等。\n示例：输入 [1,2,2,3,4,4,3] → 输出 true（外侧 3 配 3、内侧 4 配 4 均镜像相等）\n约束：节点数 1~1000`,
    answer: `## 题意与约束分析
判断二叉树是否关于根左右镜像对称：结构镜像且对应值相等。n ∈ [1, 1000]。注意对称 ≠ 左右子树相同——比较方向相反，这是与 100 题的核心区别，面试官最爱追问。

## 思路推导：从暴力到最优
展开定义：a、b 互为镜像，当且仅当 a.Val == b.Val，且 a.Left 与 b.Right 镜像、a.Right 与 b.Left 镜像——递归定义即实现。迭代版用队列：每次取一对比较，再交叉入队（a 的左右配 b 的右左）。层序存值再判回文是更慢替代（要存 nil 占位），不如双指针直接比。

## 正确性要点
- 递归语义：isMirror(a, b) 判断两子树是否互为镜像。
- 三种终止：双 nil 为真；单 nil 为假（结构不同）；值不等为假。
- 交叉递归 a.Left↔b.Right、a.Right↔b.Left 即镜像形式化：外配外、内配内，覆盖所有对应位置。
- 迭代不变式：队列中相邻两节点恒构成一对待比镜像位置。

## 复杂度分析
- 时间 O(n)：每节点最多参与一次比较。
- 空间 O(h)：递归栈；迭代队列最坏 O(n)。

## 代码实现
\`\`\`go
func isSymmetric(root *TreeNode) bool {
    if root == nil { return true }
    return isMirror(root.Left, root.Right)
}

func isMirror(a, b *TreeNode) bool {
    if a == nil && b == nil { return true }  // 双空：镜像成立
    if a == nil || b == nil { return false } // 单空：结构不对称
    return a.Val == b.Val &&
        isMirror(a.Left, b.Right) && // 外侧配外侧
        isMirror(a.Right, b.Left)    // 内侧配内侧
}

// 迭代版：队列每次出队一对 (a,b) 比较，
// 双 nil continue；单 nil 或值不等 false；
// 交叉入队 a.Left,b.Right,a.Right,b.Left，略。
\`\`\`

## 边界与易错点
- 与 100 题混淆：比 a.Left 与 b.Left 是「相同树」写法，对称必须交叉比较，写反全错。
- 根节点自身恒对称，直接从 root.Left、root.Right 比起。
- 迭代版双 nil 要 continue 而非返回 true（队列里还有待比节点）。
- 值同但结构不对称（[1,2,2,null,3,null,3]）必须 false，只校验层序序列会漏。

## 举一反三
- 100 相同的树：同位置比较，模板双胞胎；信号「完全相等」而非「镜像」。
- 226 翻转二叉树：翻转后与自身相同即对称，可作另类解法。
- 572 另一棵树的子树：双树递归家族，匹配条件换成「完全相同」。

## 实际工程应用
配置树镜像一致性校验、编译器 AST 模式匹配、图形学对称性检测，都用「双指针同步递归」范式；协议解析校验双向报文是否镜像兼容，思路同构。`,
    keyPoints: ["对称 = 交叉比较：左左配右右、左右配右左", "与 100 相同树只差比较方向", "队列迭代：两两出队、交叉入队，双 nil 用 continue", "值相等 + 结构镜像，缺一不可"],
    followUps: ["迭代怎么写？队列里的 nil 怎么处理？", "和判断两棵树相同的代码差在哪一行？", "树很大且流式读入时，怎么边读边判对称？"],
    favorited: false,
  },
  {
    id: "algo-543",
    nodeId: "p1-tree",
    question: `543. 二叉树的直径（LeetCode 543）\n题意：返回二叉树中任意两节点之间最长路径的边数，路径可以不经过根节点。\n示例：输入 [1,2,3,4,5] → 输出 3（最长路径 4→2→1→3 或 5→2→1→3，共 3 条边）\n约束：节点数 1~10^4`,
    answer: `## 题意与约束分析
二叉树直径 = 任意两节点间最长路径的边数，路径不一定经过根。节点数 [1, 10^4]。关键陷阱：最长路径可以完全位于某棵子树内部，「过根」只是所有候选之一，只算根的左右深度和会漏解。

## 思路推导：从暴力到最优
暴力：枚举所有节点对、用 LCA 求路径长度取最大，O(n²·log n) 起步，不可接受。结构观察：任意一条最长路径必存在唯一「最高点」（两端节点的 LCA），其长度 = 左臂深度 + 右臂深度。于是对每个节点计算「以它为最高点的路径长」取全局最大即可。求深度本身就是后序递归，可以在返回深度的同一函数里顺手用 左深 + 右深 更新全局最优——一次遍历搞定。这就是「返回值与全局状态分工」的经典树形 DP 模式。

## 正确性要点
- 递归语义：depth(node) 返回以 node 为根向下延伸的最长链边数（单臂深度），空节点返回 0。
- 后序不变式：进入节点处理时其左右子树深度已算出，以该节点为最高点的路径长 = l + r。
- 全局正确性：任意路径都有唯一最高点，遍历所有节点取 max(l+r) 不会漏掉最优解。
- 返回值给父节点用 1 + max(l, r)（单臂），更新答案用 l + r（双臂）——两个量绝不能混。

## 复杂度分析
- 时间 O(n)：每节点一次后序访问；暴力 O(n²·log n) 被降为线性。
- 空间 O(h)：递归栈深度。

## 代码实现
\`\`\`go
func diameterOfBinaryTree(root *TreeNode) int {
    maxD := 0
    var depth func(*TreeNode) int
    depth = func(node *TreeNode) int {
        if node == nil { return 0 }
        l, r := depth(node.Left), depth(node.Right)
        if l+r > maxD { maxD = l + r } // 以本节点为最高点的路径长
        if l > r { return l + 1 }      // 返回单臂深度供父节点使用
        return r + 1
    }
    depth(root)
    return maxD
}
\`\`\`

## 边界与易错点
- 边数与节点数口径：题目要边数，空节点返回 0、叶子向上返回 1 恰好对应「一条边」；若改成节点数口径，答案要加一。
- 把 max(l, r) 当 l + r 更新答案：只统计了单臂，漏掉经过最高点的完整路径。
- 误以为直径必过根：反例是最长链完全在左子树内部；全局变量在每个节点更新才是正解。
- 用包级全局变量时多测例要重置（LeetCode 单测例无碍）；工程代码应像示例一样用闭包封装。

## 举一反三
- 124 二叉树最大路径和：同框架，但路径和可为负，子树贡献为负时取 0（丢弃该臂）；识别信号是「路径和」而非「路径长」。
- 687 最长同值路径：聚合条件加「值相等才延续」。
- 1372 最长交错路径：状态要区分左右方向，DFS 返回二元组。

## 实际工程应用
网络拓扑中估算任意两节点的最大传输延迟（树的直径即最坏延迟上界）、组织架构里找最远汇报链路、社交图谱的极值距离探测，都是直径模型。「返回值聚合 + 全局最优」套路也是所有树形 DP 的入门范式。`,
    keyPoints: ["直径 = 某节点的左深 + 右深，最高点唯一", "返回值是单臂深度，更新答案是双臂和，两者分工", "不一定过根：必须每个节点都尝试", "树形 DP 范式：后序聚合 + 全局最优"],
    followUps: ["求带权最大路径和（124 题）怎么改？", "为什么不需要显式求 LCA？", "如何把全局变量改成纯返回值传递（多返回值）？"],
    favorited: false,
  },
  {
    id: "algo-102",
    nodeId: "p1-tree",
    question: `102. 二叉树的层序遍历（LeetCode 102）\n题意：从上到下、每层从左到右遍历二叉树，返回按层分组的二维数组。\n示例：输入 [3,9,20,null,null,15,7] → 输出 [[3],[9,20],[15,7]]（每层一个子数组）\n约束：节点数 0~2000`,
    answer: `## 题意与约束分析
层序遍历：从上到下、每层从左到右，输出按层分组的二维数组。节点数 [0, 2000]。考点不在 BFS 本身，而在「分层」——如何把队列里混在一起的相邻两层干净切开。

## 思路推导：从暴力到最优
BFS 用队列是常识，难点是分层。方案一：节点附带层号入队，按层号归组——可行但每个元素多存元数据。方案二（最优）：外层循环开头固定当前队列长度 size，它恰等于本层节点数；内层只弹 size 个，期间入队的全是下一层——层序模板的标准写法。DFS 也能做：递归携带 depth 往 res[depth] 追加，前序天然保证层内从左到右。两种范式都该会。

## 正确性要点
- FIFO 保证：第 k 层全部节点先于第 k+1 层入队，弹出顺序即层序。
- 循环不变式：进入外层循环时，队列恰好只含当前层的全部节点。
- size 快照：内层次数锁定为 size，新入队节点留给下一轮，分层边界正确。
- DFS 版：depth 首达新值时新建子数组，前序保证层内顺序。

## 复杂度分析
- 时间 O(n)：每节点入队出队各一次。
- 空间 O(n)：队列最坏装下最宽一层（满二叉树底层约 n/2）。

## 代码实现
\`\`\`go
func levelOrder(root *TreeNode) [][]int {
    res := [][]int{}
    if root == nil { return res }
    queue := []*TreeNode{root}
    for len(queue) > 0 {
        size := len(queue) // 本层节点数快照
        level := make([]int, 0, size)
        for i := 0; i < size; i++ {
            node := queue[0]; queue = queue[1:]
            level = append(level, node.Val)
            if node.Left != nil { queue = append(queue, node.Left) }
            if node.Right != nil { queue = append(queue, node.Right) }
        }
        res = append(res, level) // 一层完结
    }
    return res
}
\`\`\`

## 边界与易错点
- 空树返回空二维数组而不是 [[]]，先判 nil。
- 最常见错误：内层直接 for len(queue) > 0 弹到空，所有层合并成一层——必须先快照 size。
- 层内顺序：先入左再入右，写反变成镜像层序。

## 举一反三
- 107 自底向上层序：结果 reverse 即可。
- 103 锯齿形层序：按层号奇偶决定是否反转本层。
- 199 右侧视图：每层最后一个元素。
- 429 N 叉树层序、637 每层平均值：模板套用；识别信号「按层处理」。

## 实际工程应用
消息队列按组织层级逐级扩散通知、爬虫按深度限宽抓取、游戏 AOI 按距离环广播，本质都是 BFS 分层；分布式任务调度按依赖层级批量调度，也是层序思想。`,
    keyPoints: ["分层核心：外层循环开头快照 size", "队列不变式：每层开始时队列恰含本层全部节点", "DFS 带 depth 参数也能分层", "最宽一层决定队列空间 O(n)"],
    followUps: ["DFS 怎么实现层序？层内顺序如何保证？", "锯齿形层序（103）怎么改？", "流式处理无限深的树，队列要怎么设计？"],
    favorited: false,
  },
  {
    id: "algo-108",
    nodeId: "p1-tree",
    question: `108. 将有序数组转换为二叉搜索树（LeetCode 108）\n题意：给定升序数组，构造一棵高度平衡（任意节点左右子树高度差不超过 1）的 BST。\n示例：输入 [-10,-3,0,5,9] → 输出 [0,-3,9,-10,null,5]（以 0 为根的一种合法平衡树，答案不唯一）\n约束：长度 1~10^4，元素严格递增`,
    answer: `## 题意与约束分析
把升序数组转成高度平衡 BST（左右子树高度差 ≤ 1）。数组长度 [1, 10^4]。题眼：升序数组就是 BST 的中序遍历——本题是「已知中序序列，重建一棵平衡树」。

## 思路推导：从暴力到最优
暴力：枚举每个元素当根、再枚举左右划分并验证平衡，指数级，不取。利用中序性质：BST 中序 = 升序，而平衡要求左右子树节点数尽量均分——直接取数组中点作根，左半递归建左子树、右半递归建右子树。每次均分使左右子树规模差 ≤ 1，递归下去高度差自然 ≤ 1，得到理论最优的 log 级高度。中点定位 O(1)，每个元素恰好被取作一次根，总 O(n)。

## 正确性要点
- 递归语义：build(lo, hi) 用 nums[lo..hi] 建平衡 BST 并返回根。
- BST 性质：中点左侧元素全小于根、右侧全大于根（数组升序），左右递归区间不相交，中序不变式保持。
- 平衡性：每次划分左右长度差 ≤ 1；由归纳，每一层的左右子树高度差 ≤ 1 成立。
- 中点下取整或上取整均可，得到不同形态但同样平衡合法的树（答案不唯一）。

## 复杂度分析
- 时间 O(n)：每个元素恰好作为一个区间的根被处理一次；暴力枚举指数级。
- 空间 O(log n)：递归栈深度等于树高，平衡树保证对数级；输出树本身 O(n) 不计。

## 代码实现
\`\`\`go
func sortedArrayToBST(nums []int) *TreeNode {
    var build func(lo, hi int) *TreeNode
    build = func(lo, hi int) *TreeNode {
        if lo > hi { return nil } // 空区间
        mid := lo + (hi-lo)/2     // 中点作根，左右规模差 ≤1
        return &TreeNode{
            Val:   nums[mid],
            Left:  build(lo, mid-1),
            Right: build(mid+1, hi),
        }
    }
    return build(0, len(nums)-1)
}
\`\`\`

## 边界与易错点
- 区间定义要一致：闭区间 [lo, hi] 配 mid±1；写成半开区间边界全乱。
- mid 用 lo + (hi-lo)/2 的写法在大数语言里防溢出（Go int 为 64 位无碍），面试提一句加分。
- base case 是 lo > hi 返回 nil，不是 lo == hi 直接返回叶子——空区间也要能给父指针交出 nil。
- 误以为要逐个插入建树：插入法结果依赖插入顺序与实现，不能保证平衡；中点递归才是正解。

## 举一反三
- 109 有序链表转 BST：不能 O(1) 取中点，用快慢指针找中点，或中序模拟先建框架再填值做到 O(n)。
- 99 恢复 BST：反向题，已知被交换的中序序列恢复 BST；本题是「已知中序造平衡树」。
- 面试变体「有序数组转 AVL」：就是本题，中点递归结果天然满足 AVL 平衡条件。

## 实际工程应用
静态只读数据集建索引：把排序好的 key 列表一次性物化成平衡 BST 或完美二分查找布局，用于内存数据库只读副本、编译器符号表；缓存无关的 vEB 布局构造也从「中点递归」思想出发。`,
    keyPoints: ["升序数组 = BST 中序序列，题眼", "中点作根 → 左右规模差 ≤1 → 天然平衡", "区间边界一致性是代码正确性关键", "答案不唯一：中点上/下取整都合法"],
    followUps: ["为什么中点作根能保证平衡？形式化说明一下。", "换成有序链表（109）怎么做？怎么优化到 O(n)？", "数组有重复元素时怎么处理？"],
    favorited: false,
  },
  {
    id: "algo-98",
    nodeId: "p1-tree",
    question: `98. 验证二叉搜索树（LeetCode 98）\n题意：判断给定二叉树是否为合法 BST：左子树所有值小于根、右子树所有值大于根，且左右子树递归成立。\n示例：输入 [5,1,4,null,null,3,6] → 输出 false（3 小于祖先 5 却在右子树，只做父子检查会漏判）\n约束：节点数 1~10^4，值域覆盖 ±2^31`,
    answer: `## 题意与约束分析
验证一棵树是否合法 BST：左子树所有值 < 根 < 右子树所有值，且递归成立。n ∈ [1, 10^4]。经典陷阱：只比较节点与其直接孩子不够。

## 思路推导：从暴力到最优
错误直觉：逐点检查 node.Left.Val < node.Val < node.Right.Val。反例 [5,1,4,null,null,3,6]——3 < 5 却在 5 的右子树，逐点全过但整体非法。思路一：BST 中序必严格递增，遍历中比较前后值。思路二：递归携带值域 (lo, hi)，每层收紧一端。两者均 O(n)。

## 正确性要点
- 中序法：合法 BST 当且仅当中序严格递增；维护 prev，当前值 > prev 恒成立则无逆序。
- 区间法语义：check(node, lo, hi) 判断子树所有值是否落在开区间 (lo, hi)。
- 约束传播：进入左子树后祖先下界仍有效，node.Val 成为最紧上界——取交集即收紧，不丢约束。
- 严格不等：BST 不允许等值，判负必须 <= 而非 <。

## 复杂度分析
- 时间 O(n)：每节点访问一次。
- 空间 O(h)：递归栈。

## 代码实现
\`\`\`go
// 写法一：中序遍历，序列须严格递增
func isValidBST(root *TreeNode) bool {
    var prev *int
    var inorder func(*TreeNode) bool
    inorder = func(node *TreeNode) bool {
        if node == nil { return true }
        if !inorder(node.Left) { return false } // 左子树判负，短路
        if prev != nil && node.Val <= *prev { return false } // 逆序即非法
        prev = &node.Val
        return inorder(node.Right)
    }
    return inorder(root)
}

// 写法二：上下界递归
// check(node, lo, hi)：node.Val 须在 (lo, hi) 开区间内，
// 左子树递归 check(node.Left, lo, &node.Val)，
// 右子树递归 check(node.Right, &node.Val, hi)，略。
\`\`\`

## 边界与易错点
- 只比父子的「局部检查」是标准错误答案，必须能讲出反例 [5,1,4,null,null,3,6]。
- 值域边界：用最小整数当初界会漏判恰好等界的节点；*int + nil 表无界最稳。
- 重复值 [2,2,2] 必须 false，判负写成 < 就错了。
- 中序法 prev 在访问当前节点后立即更新，再递归右子树。

## 举一反三
- 99 恢复 BST：中序找两个逆序对交换，本题修复版。
- 230 第 K 小、173 BST 迭代器：建立在「BST 中序 = 有序」之上。
- 333/1373 最大 BST 子树：后序聚合「是否 BST + 最值 + 大小」，区间法升级版。

## 实际工程应用
数据库索引页一致性校验（B+ 树页内键有序、页间区间不相交，正是区间递归思想）、有序缓存结构的不变式断言、编译器符号表作用域检查，都是「有序性约束传播」实例。`,
    keyPoints: ["BST 是全局约束：右子树所有值要大于整条祖先链的下界", "中序严格递增 当且仅当 合法 BST", "区间递归：每下一层收紧一端", "严格不等号：等值即非法"],
    followUps: ["为什么只比父子节点是错的？给个反例。", "中序法能边遍历边提前退出吗？", "如果允许重复值（等值挂右子树）怎么改？"],
    favorited: false,
  },
  {
    id: "algo-230",
    nodeId: "p1-tree",
    question: `230. 二叉搜索树中第 K 小的元素（LeetCode 230）\n题意：给定 BST 与整数 k，返回树中第 k 小（从 1 计）的元素值。\n示例：输入 root=[3,1,4,null,2], k=1 → 输出 1（中序序列 [1,2,3,4] 的第 1 个）\n约束：节点数 1~10^4，k 保证合法`,
    answer: `## 题意与约束分析
求 BST 中第 k 小的值（1-indexed），k 保证合法。核心性质：BST 中序遍历 = 升序序列，第 k 个被访问的节点即答案。考点在提前终止，以及「查询频繁」场景下的扩展设计。

## 思路推导：从暴力到最优
暴力：完整中序存入数组取下标 k-1，O(n) 时间空间。优化一：迭代中序遍历，弹栈计数到 k 立即返回，O(h+k)，最好情况不遍历整棵树。优化二（面试官最爱）：查询频繁且树动态增删时，给每个节点增广 leftSize（左子树节点数）——比较 k 与 leftSize+1 决定走向，单次查询 O(h)，维护成本摊到插入删除。这就是 order-statistics tree，「中序 = 顺序统计」思想的延伸。

## 正确性要点
- BST 中序严格升序，第 k 个被访问节点的值即第 k 小。
- 提前终止安全：计数到 k 后不再递归，答案沿调用链直传，不受影响。
- 迭代不变式同 94 题：弹栈时左子树已全部访问，计数器即当前节点的中序名次。
- 增广版：leftSize 在增删路径上 O(h) 更新，rank 比较逻辑与二分查找同构。

## 复杂度分析
- 时间 O(h+k)：迭代提前终止；暴力 O(n)；增广后查询 O(h)，平衡树 O(log n)。
- 空间 O(h)：栈；暴力数组 O(n)。

## 代码实现
\`\`\`go
func kthSmallest(root *TreeNode, k int) int {
    var stack []*TreeNode
    cur := root
    for cur != nil || len(stack) > 0 {
        for cur != nil { // 一路向左，按中序推进
            stack = append(stack, cur)
            cur = cur.Left
        }
        cur = stack[len(stack)-1]
        stack = stack[:len(stack)-1]
        k--
        if k == 0 { return cur.Val } // 第 k 个被访问者即答案
        cur = cur.Right
    }
    return -1 // 题目保证 k 合法，不可达
}
\`\`\`

## 边界与易错点
- k 是 1-indexed：先 k-- 再判 0，或计数到 k，别混用导致差一错误。
- 递归版提前终止：左子树找到就直接返回、不再进右子树；常见错误是找到后仍继续遍历覆盖答案。
- 增广字段忘在旋转/删除时更新，查询静默错误——工程上必须封装进增删流程。
- 误用堆：堆求第 k 小是无序数组场景的思路，BST 自带顺序，杀鸡用牛刀。

## 举一反三
- 173 二叉搜索树迭代器：把本题栈封装成迭代器，next/hasNext 均摊 O(1)。
- 653 两数之和 IV：中序 + 双指针夹逼，升序性质的又一应用。
- 第 k 大：反中序（右-根-左）遍历即可。

## 实际工程应用
有序存储引擎的分位数查询（P99 延迟、中位数工资）本质是 order statistics；时序库按时间取第 k 条、排行榜「超越 87% 的人」的名次换算，都是 BST 中序名次思想的工程放大版。`,
    keyPoints: ["BST 中序 = 升序，第 k 个访问者即答案", "迭代 + 计数提前终止 O(h+k)", "增广 leftSize → O(h) 查询，面试加分项", "1-indexed 差一错误是高频挂点"],
    followUps: ["频繁查询且树动态变化怎么优化？增广字段怎么维护？", "递归版怎么写提前终止？", "求第 k 大呢？（反中序：右-根-左）"],
    favorited: false,
  },
  {
    id: "algo-199",
    nodeId: "p1-tree",
    question: `199. 二叉树的右视图（LeetCode 199）\n题意：想象站在二叉树右侧，返回从上到下能看到的节点值，即每层最右节点。\n示例：输入 [1,2,3,null,5,null,4] → 输出 [1,3,4]（2 被 3 挡住、5 被 4 挡住）\n约束：节点数 0~100`,
    answer: `## 题意与约束分析
返回从右侧看二叉树能看到的节点值（每层最右一个），从上到下。n ∈ [0, 100]。「右侧视图」= 每层右边界，BFS 与 DFS 都能解，考点在「每层最右」的两种刻画。

## 思路推导：从暴力到最优
视角一（BFS）：层序遍历每层最后出队者即右视图成员——102 模板里 i == size-1 时记录。视角二（DFS）：按 根-右-左 做前序变体，每个深度首次被访问的节点必是该层最右者（右分支先探到）；用 len(res) == depth 判首访。两法均 O(n)，面试建议都写并讲清等价性。

## 正确性要点
- BFS：分层不变式保证逐层处理，每层最后出队者即最右节点。
- DFS 首访性质：先右后左时，深度 d 的首访必沿当时最右路径发生。
- 判首访：len(res) == depth 时 append，数组下标即层号。
- 两法同解：「每层最后」与「每层最右首访」指向同一节点。

## 复杂度分析
- 时间 O(n)：每节点访问一次。
- 空间：BFS 队列 O(n)（最宽层）；DFS 递归栈 O(h)。

## 代码实现
\`\`\`go
// 写法一：BFS 取每层最后一个
func rightSideView(root *TreeNode) []int {
    res := []int{}
    if root == nil { return res }
    queue := []*TreeNode{root}
    for len(queue) > 0 {
        size := len(queue)
        for i := 0; i < size; i++ {
            node := queue[0]; queue = queue[1:]
            if i == size-1 { res = append(res, node.Val) } // 层尾即右视
            if node.Left != nil { queue = append(queue, node.Left) }
            if node.Right != nil { queue = append(queue, node.Right) }
        }
    }
    return res
}

// 写法二：DFS 根-右-左，每层首访者入选
// dfs(node, depth)：len(res) == depth 时 append node.Val，
// 再 dfs(node.Right, depth+1)、dfs(node.Left, depth+1)，略。
\`\`\`

## 边界与易错点
- DFS 先右后左写反就变成「左视图」——追问左视图先左后右即可。
- len(res) == depth 判首访，写成 <= 会重复 append 同层节点。
- BFS 层尾用 i == size-1，别用「下一个是 nil」这类脆弱判断。
- 空树返回空数组，先判 nil。

## 举一反三
- 左侧视图：DFS 先左后右 / BFS 每层第一个，镜像题。
- 515 每层最大值、637 每层平均值：换聚合函数。
- 116 填充右侧指针：层序 + 层内串联，右视图的连线版。

## 实际工程应用
层级摘要视图：BI 报表每层只展示代表行、看板每列只露最高优先级卡片；图形渲染从某视角只画轮廓边的可见面裁剪，也是「侧视图」思想。`,
    keyPoints: ["右视图 = 每层最右节点，BFS/DFS 双解", "DFS 根-右-左：每层首访者即答案", "len(res) == depth 判首访，下标即层号", "换遍历方向得左视图，一题两吃"],
    followUps: ["左视图怎么改？", "DFS 为什么先访问右子树就能保证每层首访是最右？", "自底向上的视图怎么做？"],
    favorited: false,
  },
  {
    id: "algo-114",
    nodeId: "p1-tree",
    question: `114. 二叉树展开为链表（LeetCode 114）\n题意：原地将二叉树展开为单链表：所有左指针置空，右指针按前序遍历顺序串联。\n示例：输入 [1,2,5,3,4,null,6] → 输出 [1,null,2,null,3,null,4,null,5,null,6]（前序 1,2,3,4,5,6 的右链）\n约束：节点数 0~2000，进阶要求 O(1) 额外空间`,
    answer: `## 题意与约束分析
把二叉树原地展开成单链表：每个节点 Left 置空、Right 指向下一个，顺序 = 前序遍历。节点数 [0, 2000]，进阶要求 O(1) 空间。「原地」是题眼——新建链表就丢了这题的灵魂。

## 思路推导：从暴力到最优
暴力：先前序遍历存数组，再遍历数组重连指针，O(n) 空间，违规则。观察前序序的结构：根之后紧跟左子树展开链，再接右子树展开链。于是对每个节点：把左子树整体插到根与原右子树之间——暂存右子树，右指针改接左子树，左指针置空，沿新右链走到链尾，把暂存的右子树接上去。从根开始迭代推进 cur，O(1) 空间完成。等价递归版：后序地先展开左右子树再拼接，也很好写。

## 正确性要点
- 不变式：处理 cur 时，cur 之前的链已符合前序序，cur 的左右子树尚未展开。
- 拼接正确性：左子树前序链的最后一个节点（左子树的最右节点）在前序序中恰位于右子树之前，把右子树挂到它后面，顺序与前序遍历完全一致。
- cur = cur.Right 推进：原左子树（现挂在右边）会被逐节点展开处理，不丢节点。
- 无左子树时直接右移，天然处理单子树情形。

## 复杂度分析
- 时间 O(n)：找最右节点的总步数被均摊——每条边最多被「找尾」遍历一次，整体线性。
- 空间 O(1)：迭代版只用几个指针；递归版 O(h)。

## 代码实现
\`\`\`go
func flatten(root *TreeNode) {
    cur := root
    for cur != nil {
        if cur.Left != nil {
            right := cur.Right   // 暂存原右子树
            cur.Right = cur.Left // 左子树整体抬到右边
            cur.Left = nil
            tail := cur.Right
            for tail.Right != nil { tail = tail.Right } // 找左链最右节点
            tail.Right = right // 原右子树挂到链尾
        }
        cur = cur.Right // 推进到下一个待展开节点
    }
}
\`\`\`

## 边界与易错点
- 拼接点是左子树的最右节点，不是左子树根的 Right——后者会跳过左链中间节点。
- 必须先暂存 cur.Right 再覆盖，否则右子树丢失。
- Left 置空别漏：题目要求所有 Left 为 nil。
- 前序数组重连虽简单但违背「原地」要求；面试主动说明空间差异，再写 O(1) 版。

## 举一反三
- 116 填充右侧指针：树的就地改指针家族，层序 + 已建好的 next 链走捷径。
- 430 扁平化多级双向链表：child 链插入逻辑与「左链插到根右之间」完全同构。
- 897 递增顺序搜索树：按中序重排为右链，遍历序换成中序。

## 实际工程应用
树形配置扁平化成线性执行流水线（插件链、中间件链的物化）、AST 转线性 IR 供后端顺序处理、DOM 按文档序展开，都是「按指定遍历序原地重排指针」的应用。`,
    keyPoints: ["前序序：左链整体插到根与原右子树之间", "拼接点是左子树的最右节点", "O(1) 空间迭代：cur 一路向右推进", "原地改指针题家族：116/430/897 同思想"],
    followUps: ["递归（后序）版怎么写？返回值怎么设计？", "为什么找最右节点不会让总复杂度退化成 O(n²)？", "按中序展开（897 题）怎么改？"],
    favorited: false,
  },
  {
    id: "algo-105",
    nodeId: "p1-tree",
    question: `105. 从前序与中序遍历序列构造二叉树（LeetCode 105）\n题意：给定一棵树的前序与中序遍历数组（无重复值），重建并返回该二叉树。\n示例：输入 preorder=[3,9,20,15,7], inorder=[9,3,15,20,7] → 输出 [3,9,20,null,null,15,7]（前序首元素 3 为根，中序中 9 在左、15/20/7 在右）\n约束：长度 1~3000，值互不相同`,
    answer: `## 题意与约束分析
给定前序与中序遍历数组（无重复值）重建二叉树。长度 [1, 3000]。考点：两种遍历信息互补——前序给根，中序给划分。

## 思路推导：从暴力到最优
前序首元素必是根；在中序里找到它，左侧是左子树中序、右侧是右子树中序，按长度同步切分前序，递归重建。暴力版线性扫中序找根 O(n²)；预处理 值→中序下标 哈希表后查根 O(1)，总 O(n)。关键：两数组靠「子树大小」对齐——左子树中序有 L 个节点，前序根后紧跟的 L 个就是左子树前序。下标区间递归，勿切片。

## 正确性要点
- 前序首元素 = 根：由前序定义直接保证。
- 中序定位：根在下标 i，[inLo, i-1] 左子树、[i+1, inHi] 右子树。
- 区间对齐：L = i - inLo，前序左区间 [preLo+1, preLo+L]、右区间 [preLo+L+1, preHi]。
- 无重复值是前提：否则定位不唯一，不可唯一重建。

## 复杂度分析
- 时间 O(n)：哈希 O(1) 定位，每节点一次；暴力线性查找 O(n²)。
- 空间 O(n)：哈希表 + O(h) 递归栈。

## 代码实现
\`\`\`go
func buildTree(preorder []int, inorder []int) *TreeNode {
    idx := make(map[int]int, len(inorder))
    for i, v := range inorder { idx[v] = i } // 值 → 中序下标
    var build func(preLo, inLo, inHi int) *TreeNode
    build = func(preLo, inLo, inHi int) *TreeNode {
        if inLo > inHi { return nil } // 空区间
        rootVal := preorder[preLo]    // 前序首元素即根
        i := idx[rootVal]
        leftSize := i - inLo
        return &TreeNode{
            Val:   rootVal,
            Left:  build(preLo+1, inLo, i-1),          // 左子树
            Right: build(preLo+1+leftSize, i+1, inHi), // 右子树
        }
    }
    return build(0, 0, len(inorder)-1)
}
\`\`\`

## 边界与易错点
- 区间口径统一：中序闭区间 + 前序只传起始下标，混用闭/半开区间是错位源。
- leftSize 用中序区间算（i - inLo），不是前序下标。
- base case：中序区间空即返回 nil；切片版防前序越界。
- 重复值时哈希被覆盖，重建出错——题目保证无重复，面试点出。

## 举一反三
- 106 中序 + 后序：后序末元素是根，其余同构。
- 889 前序 + 后序：缺中序不能唯一确定，需额外假设。
- 1008 前序构造 BST：BST 自带中序 = 排序，值域法 O(n)。

## 实际工程应用
树的序列化/反序列化：分布式系统间传输树结构（查询计划、AST），发两种遍历序即可无损重建；编译器从 IR 恢复语法树同思想。`,
    keyPoints: ["前序定根，中序定左右边界，信息互补", "哈希表把查根从 O(n) 降到 O(1)", "靠子树大小对齐两个区间，勿切片复制", "无重复值是唯一重建的前提"],
    followUps: ["前序 + 后序为什么不能唯一确定一棵树？", "如何改成迭代（栈）实现？", "有重复值时要补充什么信息才能唯一重建？"],
    favorited: false,
  },
  {
    id: "algo-112",
    nodeId: "p1-tree",
    question: `112. 路径总和（LeetCode 112）\n题意：判断是否存在一条从根到叶子的路径，使路径上所有节点值之和等于 targetSum。\n示例：输入 root=[5,4,8,11,null,13,4,7,2,null,null,null,1], targetSum=22 → 输出 true（5→4→11→2 恰好 22）\n约束：节点数 0~5000，值与 target 均可为负`,
    answer: `## 题意与约束分析
判断是否存在根到叶子的路径，使路径和等于 targetSum。节点数 [0, 5000]，值与 target 可负。两个关键词缺一不可：「根到叶子」——到内部节点不算；叶子 = 左右皆空。

## 思路推导：从暴力到最优
暴力：枚举所有根到叶路径求和比较，路径数与叶子数成正比，且每条路径重复累加上层节点，O(叶子数 × 深度)。更优：DFS 自上而下携带「剩余值」——进入节点时 target 减去当前值，到叶子判断剩余是否为 0，每个节点只访问一次，O(n)。注意：因为值可负，不能用「剩余值已变负」提前剪枝（后面的负值可能把它拉回），这是与全正数版本的关键区别。BFS 携带路径和同样可行。

## 正确性要点
- 递归语义：hasPathSum(node, remain) = 以 node 为根的子树中，是否存在到叶子的路径和为 remain。
- 叶子判定：node.Left == nil 且 node.Right == nil 时检查 node.Val == remain（先减后判 0 是等价口径，二者选一并贯彻）。
- 析取传播：非叶子时左子树或右子树任一成立即真，逻辑或覆盖所有路径，不重不漏。
- 空树返回 false：不存在任何路径。

## 复杂度分析
- 时间 O(n)：每个节点一次访问；暴力多算每层重复累加。
- 空间 O(h)：递归栈。

## 代码实现
\`\`\`go
func hasPathSum(root *TreeNode, targetSum int) bool {
    if root == nil { return false } // 空树无路径
    // 叶子：判定剩余值是否恰好等于当前值
    if root.Left == nil && root.Right == nil {
        return root.Val == targetSum
    }
    remain := targetSum - root.Val // 扣除当前节点，向下传剩余值
    return hasPathSum(root.Left, remain) || hasPathSum(root.Right, remain)
}
\`\`\`

## 边界与易错点
- 叶子判定漏掉：把「根到任意节点」当路径，[1,2] target=1 会错判 true（1 不是叶子）。
- 空树且 target=0：必须 false；常见错误是把 base case 写成 remain == 0 返回 true，导致空树判真。
- 值可负：不能用 remain < 0 剪枝；若题目保证全正数该剪枝才成立（可作为追问的回答）。
- 递归口径混用：「先减后判」与「到叶子再减」两套逻辑混写，结果差一个节点值。

## 举一反三
- 113 路径总和 II：收集所有合法路径，回溯模板（append → 递归 → 撤销）；识别信号是「返回所有路径」。
- 437 路径总和 III：路径不要求根起叶止，任意向下段——前缀和 + 哈希表 O(n)。
- 129 求根到叶数字之和：路径构成数字累加，DFS 传中间值同模板。
- 666/面试变体：带权路径族问题都从「自上而下传状态」出发。

## 实际工程应用
决策树命中判定：风控规则树中「是否存在一条规则路径累计分值过阈值」；游戏技能树检查前置点数的可达路径；编译器数据流分析中检查某条执行路径的资源配额是否达标。`,
    keyPoints: ["根到叶子：叶子判定（左右皆空）是题眼", "DFS 传剩余值，叶子处判零", "值可负 → 禁止提前剪枝", "空树 false：base case 别写成 remain == 0 判真"],
    followUps: ["收集所有路径（113）怎么改？回溯撤销在哪？", "路径不要求到叶子（437）怎么做？前缀和为什么对？", "全正数时能加什么剪枝？"],
    favorited: false,
  },
];
