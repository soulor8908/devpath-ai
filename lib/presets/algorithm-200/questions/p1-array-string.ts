// lib/presets/algorithm-200/questions/p1-array-string.ts
// 「算法 200 题」Phase 1 · 数组与字符串专题（16 题深度题解）
//
// 设计说明：
//   - 每题答案为八段式深度结构：题意约束分析 → 思路推导（从暴力到最优）→
//     正确性要点 → 复杂度对比 → Go 实现 → 边界易错 → 举一反三 → 实际工程应用
//   - 本专题主线：三大范式的识别信号与论证——
//     双指针（对撞求和 / 快慢写压缩）、滑动窗口（右扩左缩 + 窗口状态）、
//     原地哈希（下标即槽位）；反复训练「为什么移动这个指针」的排除法口述
//   - 旧版答案只贴代码被用户投诉无深度，本版按面试口述标准重写，
//     补齐推导链、正确性论证与真实工程映射

import type { Question } from "../../../types";

export const P1_ARRAY_STRING_QUESTIONS: Question[] = [
  {
    id: "algo-1",
    nodeId: "p1-array-string",
    question: `1. 两数之和（LeetCode 1）
题意：给定整数数组 nums 和目标值 target，找出和为 target 的两个整数并返回它们的下标（顺序不限）。每种输入恰好一个解，同一元素不能使用两次。
示例：输入 nums = [2,7,11,15], target = 9 → 输出 [0,1]（nums[0]+nums[1]=2+7=9）。
约束：2 ≤ nums.length ≤ 10^4，-10^9 ≤ nums[i], target ≤ 10^9，恰好存在一个有效答案。`,
    answer: `## 题意与约束分析
求的是下标而非值。n ≤ 10^4 时 O(n²) 也能过，但面试必须给出 O(n)；要返回原下标 ⇒ 排序双指针会打乱顺序需额外记录，哈希表更直接。

## 思路推导：从暴力到最优
暴力：双重循环枚举所有 (i,j) 对，O(n²) 时间 O(1) 空间。瓶颈在内层——对每个 x 都要线性查找 target-x 是否存在。
观察：「target-x 是否出现过」是存在性查询，哈希表 O(1) 可查 ⇒ 内层从 O(n) 降到 O(1)。
最优：一次遍历边查边存。对每个 x 先查补数是否已在表中，命中即返回；否则把 (x, 下标) 存入。先查后存保证元素不会和自己配对。

## 正确性要点
- 循环不变式：处理到第 i 个元素时，表中恰好存放 nums[0..i-1] 的值与下标。
- 解的唯一性保证第一次命中即答案；先查后存 ⇒ 配对双方必为不同下标。
- 先查后存还天然处理 x+x=target 的情形：只有第二个 x 到来时才会命中。

## 复杂度分析
- 时间：O(n)，每元素一次查一次存，均摊 O(1)，对比暴力 O(n²)
- 空间：O(n)，最坏存下 n-1 个元素

## 代码实现
\`\`\`go
func twoSum(nums []int, target int) []int {
    idx := make(map[int]int, len(nums)) // 值 -> 下标
    for i, x := range nums {
        if j, ok := idx[target-x]; ok { // 补数之前出现过
            return []int{j, i}
        }
        idx[x] = i // 先查后存，避免同一元素用两次
    }
    return nil
}
\`\`\`

## 边界与易错点
- 必须先查后存：写成先存后查，[3,2,4], target=6 会错配 3+3。
- 返回的是下标不是值，map 的 value 存下标。
- 负数不影响；Go int 为 64 位，题给 ±10^9 不溢出。
- 题设保证有解，防御性返回 nil 即可。

## 举一反三
- LC 167 两数之和 II（有序数组）：对撞双指针 O(1) 空间。
- LC 15 三数之和：要返回所有解且按值去重，哈希去重困难 ⇒ 排序+双指针。
- LC 653 两数之和 IV（BST）：同思路，遍历 + 哈希集合。
- 识别信号：「无序数组 + 找配对 + 只要一组解」→ 哈希表一次遍历；「有序 / 要求 O(1) 空间」→ 双指针。

## 实际工程应用
「查补数」模式是交易撮合引擎的核心：挂买单时查对手盘是否存在价格匹配的卖单（order book 的哈希+跳表索引）；风控系统「同一设备多账号」检测也是边流式处理边查已见集合，与本题同构。`,
    keyPoints: [
      "先查后存：map 存「值→下标」，命中即返回，保证同一元素不被用两次",
      "存在性查询 O(1) 是哈希表对双重循环的降维点",
      "返回下标 ⇒ 排序双指针需额外记录原下标，哈希更直接",
      "恰好一解的前提让首次命中即答案，无需继续扫描",
    ],
    followUps: [
      "数组有序时如何用 O(1) 空间求解？（LC 167 对撞双指针）",
      "要求返回所有不重复配对时，哈希方案要怎么改？",
      "升级为三数之和时为什么排序+双指针优于哈希？",
    ],
    favorited: false,
  },

  {
    id: "algo-49",
    nodeId: "p1-array-string",
    question: `49. 字母异位词分组（LeetCode 49）
题意：给定字符串数组 strs，把字母异位词（字符种类与计数完全相同、仅顺序不同）分到同一组返回，组间顺序不限。
示例：输入 ["eat","tea","tan","ate","nat","bat"] → 输出 [["bat"],["nat","tan"],["ate","eat","tea"]]（异位词同组）。
约束：1 ≤ strs.length ≤ 10^4，0 ≤ strs[i].length ≤ 100，仅含小写字母。`,
    answer: `## 题意与约束分析
本质是「按等价类分组」：异位词是等价关系，需要为每个等价类设计唯一 key。n ≤ 10^4，两两比较 O(n²·k) 约 10^10 次操作不可行，必须每个串只处理一次。

## 思路推导：从暴力到最优
暴力：两两判定异位词再并查集合并，O(n²·k)，超时。
观察：异位词 ⇔ 排序后字符串相同 ⇔ 26 字母计数向量相同。任一者都可当等价类的规范形（canonical form）。
最优：遍历一次，算每个串的规范形作 map key，同 key 追加进同组。排序 key 是 O(n·k log k)；计数向量 key 是 O(n·k)，k 为串长，更优。

## 正确性要点
- 规范形唯一性：s、t 异位 ⇔ 排序串相等（多重集相等判定），计数向量同理，互为充要。
- map 分组是划分：每个串恰好进入一个组，无遗漏无重复。
- Go 中 [26]byte 转 string 作 key：字节值即计数，不同计数向量产生不同字节序列，一一对应。

## 复杂度分析
- 时间：O(n·k)（计数法），对比排序法 O(n·k log k)，k=100 时约快 7 倍
- 空间：O(n·k)，所有串都要进入结果

## 代码实现
\`\`\`go
func groupAnagrams(strs []string) [][]string {
    groups := make(map[string][]string)
    for _, s := range strs {
        var cnt [26]byte // 计数数组当 key：异位词必相同
        for i := 0; i < len(s); i++ {
            cnt[s[i]-'a']++
        }
        key := string(cnt[:]) // 字节值即计数，可作 map key
        groups[key] = append(groups[key], s)
    }
    res := make([][]string, 0, len(groups))
    for _, g := range groups {
        res = append(res, g)
    }
    return res
}
\`\`\`

## 边界与易错点
- 空串 ""：计数全零的 key，所有空串归一组，无需特判。
- Go 的 map key 不能是切片，但可以是数组或 string；[26]byte 转 string 是惯用技巧，注意别用 [26]int（string([]int) 不合法）。
- 字符集若换成 Unicode，计数数组要改成 map[rune]int 再序列化。
- 结果组间顺序题目不限，map 遍历乱序可接受。

## 举一反三
- LC 242 有效的字母异位词：只判两串，单个计数数组即可。
- LC 438 找到字符串中所有字母异位词：定长滑窗+计数比较。
- LC 249 移位字符串分组：规范形换成「相邻字符差值序列」。
- 识别信号：「把 XXX 相同的归一组」→ 设计规范形 key + 哈希分组。

## 实际工程应用
规范形+分组是搜索引擎归并的基本功：query 改写归一（大小写、繁简、全半角归一后聚类）、风控特征分桶（行为序列归一成指纹再聚合），思想都是先算 canonical form 再哈希分组，与本题完全同构。`,
    keyPoints: [
      "异位词 ⇔ 排序串相等 ⇔ 计数向量相等：规范形 key 是分组核心",
      "计数数组 O(n·k) 优于排序 key O(n·k log k)",
      "Go 中 [26]byte 转 string 可作 map key（数组可比较，切片不行）",
      "两两比较 O(n²k) 不可行，必须每个串只处理一次",
    ],
    followUps: [
      "字符集换成 Unicode 时 key 怎么设计？",
      "要求组内按字典序输出时要加什么处理？",
      "移位字符串（abc→bcd）分组的规范形是什么？（LC 249）",
    ],
    favorited: false,
  },

  {
    id: "algo-128",
    nodeId: "p1-array-string",
    question: `128. 最长连续序列（LeetCode 128）
题意：给定未排序的整数数组 nums，找出数字连续的最长序列（如 1,2,3,4）的长度，要求算法时间复杂度为 O(n)。
示例：输入 [100,4,200,1,3,2] → 输出 4（最长连续序列是 [1,2,3,4]，长度为 4）。
约束：0 ≤ nums.length ≤ 10^5，-10^9 ≤ nums[i] ≤ 10^9，数组含重复元素。`,
    answer: `## 题意与约束分析
「连续」指值连续（x, x+1, ...）而非下标连续，且数组未排序。题目硬性要求 O(n) ⇒ 排序法 O(n log n) 直接出局，必须用哈希集合以空间换时间。

## 思路推导：从暴力到最优
暴力一：排序后扫一遍找最长连续段，O(n log n)，不满足要求。
暴力二：对每个 x 向上查 x+1, x+2, ... 是否在集合中，O(n²)。
关键观察：若 x-1 也在集合中，则 x 不是任何序列的起点，从 x 扩展纯属浪费——任何连续序列只需从它的最小元素扩展一次。
最优：哈希集合去重后，仅当 x-1 不在集合时（x 是起点）才向上扩展。每个元素最多被「枚举起点」和「扩展」各访问一次，均摊 O(n)。

## 正确性要点
- 不漏：任意连续序列的最小元素必满足 x-1 ∉ set，必被当作起点枚举到。
- 不重：非起点元素直接跳过，同一序列只被自己的起点统计一次。
- 均摊 O(n)：所有扩展操作覆盖的元素互不重叠，内层循环总步数 = 各序列长度之和 ≤ n。
- 去重无副作用：重复值不影响连续段长度，集合天然去重。

## 复杂度分析
- 时间：O(n)，看似双重循环，实际内层总执行次数 ≤ n，对比暴力二 O(n²)
- 空间：O(n)，哈希集合

## 代码实现
\`\`\`go
func longestConsecutive(nums []int) int {
    set := make(map[int]struct{}, len(nums))
    for _, x := range nums {
        set[x] = struct{}{}
    }
    best := 0
    for x := range set {
        if _, ok := set[x-1]; ok {
            continue // x-1 存在说明 x 不是起点，跳过
        }
        length := 1
        for { // 从起点向后扩展整段序列
            if _, ok := set[x+length]; !ok {
                break
            }
            length++
        }
        if length > best {
            best = length
        }
    }
    return best
}
\`\`\`

## 边界与易错点
- 空数组返回 0，循环不执行自然正确。
- 有重复元素：[1,2,0,1] 答案是 3，集合去重后不受影响。
- 最常见错误：不加「起点判断」对每个元素都扩展 ⇒ 退化成 O(n²)，大数据超时。
- 题给 ±10^9，x+1 不会溢出（Go int 为 64 位）。

## 举一反三
- LC 298 二叉树最长连续序列：父到子值连续，DFS 传递当前长度。
- LC 674 最长连续递增子序列（下标连续）：一次遍历即可，判别「下标连续 vs 值连续」。
- LC 300 最长递增子序列（不连续）：DP/贪心二分，「连续」与否是算法分水岭。
- 识别信号：「未排序 + 值连续 + 要求 O(n)」→ 哈希集合 + 只从起点扩展。

## 实际工程应用
「只从起点扩展」的剪枝思想与区间归并同源：监控系统把离散告警点聚成连续故障时间窗、CDN 日志把离散请求聚合成会话（sessionization），都是先建索引再只从窗口起点扫描，避免 O(n²) 的两两归并。`,
    keyPoints: [
      "只从序列起点（x-1 不在集合）扩展，每个元素均摊访问 O(1)",
      "看似双重循环实则 O(n)：内层总步数 = 各序列长度之和 ≤ n",
      "哈希集合同时解决去重与 O(1) 存在性查询",
      "排序法 O(n log n) 被题目 O(n) 要求直接排除",
    ],
    followUps: [
      "若允许 O(n log n)，排序法怎么写？重复值处理有什么坑？",
      "要求输出最长序列本身而不只是长度，怎么改？",
      "改成二叉树中父子值连续的最长路径怎么做？（LC 298）",
    ],
    favorited: false,
  },

  {
    id: "algo-283",
    nodeId: "p1-array-string",
    question: `283. 移动零（LeetCode 283）
题意：给定数组 nums，把所有 0 移动到数组末尾，同时保持非零元素的相对顺序，必须在原数组上操作，不能拷贝额外数组。
示例：输入 [0,1,0,3,12] → 输出 [1,3,12,0,0]（非零顺序不变，零全部沉底）。
约束：1 ≤ nums.length ≤ 10^4，-2^31 ≤ nums[i] ≤ 2^31-1；进阶：最小化操作（写入）次数。`,
    answer: `## 题意与约束分析
两个硬要求：原地（O(1) 空间）+ 非零保序。「保序」排除了排序和头尾对撞交换，指向快慢指针的「写指针压缩」范式。n ≤ 10^4，单趟 O(n) 足够。

## 思路推导：从暴力到最优
暴力：开新数组依次放非零再补零，O(n) 空间，违反原地要求。
观察：只关心非零的相对顺序 ⇒ 问题等价于「把所有非零按原顺序压到前面，剩余位置置零」。
最优：快慢指针。fast 扫描全数组，slow 指向下一个非零该写的位置；fast 遇非零就与 slow 交换。fast 扫完，[0, slow) 是保序非零，[slow, n) 自然全零。

## 正确性要点
- 循环不变式：任意时刻 nums[0..slow-1] 是已见非零按原序排列，nums[slow..fast-1] 全为 0。
- 交换安全性：fast 遇非零时，nums[slow] 要么是 0（slow<fast）要么是自己（slow=fast），交换不破坏任何已整理元素。
- 保序性：非零元素仅在 fast 递增时被依次处理，相对顺序不变。
- 变体（写入+补零）：先覆写非零再从 slow 起统一填 0，写次数更少，同样成立。

## 复杂度分析
- 时间：O(n)，单趟遍历
- 空间：O(1)，仅两个指针

## 代码实现
\`\`\`go
func moveZeroes(nums []int) {
    slow := 0 // [0, slow) 是已整理好的非零区间
    for fast := 0; fast < len(nums); fast++ {
        if nums[fast] != 0 {
            nums[slow], nums[fast] = nums[fast], nums[slow] // 非零换到前面
            slow++
        }
    }
}
\`\`\`

## 边界与易错点
- 全零数组：slow 不动，交换全是零与零自换，结果不变，正确。
- 无零数组：每次 fast==slow 自交换，无副作用；追求少写可加 fast!=slow 判断。
- 若用「写入+补零」变体，最后的补零循环漏写会残留脏数据。
- 别把题意理解成「排序」——非零相对顺序必须保持。

## 举一反三
- LC 26 / 27 / 80 删除系列：同一「写指针压缩」范式，保留条件各异。
- LC 75 颜色分类：三类元素划分，快慢指针升级为三指针。
- LC 905 按奇偶排序数组：不要求保序，可用对撞指针。
- 识别信号：「原地 + 保序 + 按条件把元素分区」→ 快慢指针写压缩。

## 实际工程应用
写指针压缩是内存整理与日志处理的通用操作：JVM 标记-压缩 GC 把存活对象前移腾挪出连续空间、流式 ETL 中原地过滤无效记录（如 Kafka 消费者就地剔除 tombstone 消息），核心都是本题的「读指针扫描 + 写指针落盘」模型。`,
    keyPoints: [
      "slow 语义：下一个非零写入位；不变式 [0,slow) 保序非零、[slow,fast) 全零",
      "交换法一趟完成；「写入+补零」变体写次数更少",
      "保序是题眼：排序或头尾对撞交换都不满足",
      "全零 / 无零数组是验证不变式的最小用例",
    ],
    followUps: [
      "要求写入次数最少时用交换版还是写入补零版？为什么？",
      "元素分三类（0/1/2 排序）怎么扩展？（LC 75 三指针）",
      "不要求保序时能否减少交换次数？（对撞交换）",
    ],
    favorited: false,
  },

  {
    id: "algo-26",
    nodeId: "p1-array-string",
    question: `26. 删除有序数组中的重复项（LeetCode 26）
题意：给定非递减顺序排列的数组 nums，原地删除重复元素，使每个元素只出现一次，返回新长度 k；前 k 个元素必须为去重后的结果。
示例：输入 nums = [0,0,1,1,1,2,2,3,3,4] → 输出 5，nums 前 5 项为 [0,1,2,3,4]。
约束：1 ≤ nums.length ≤ 3×10^4，数组已排序（双指针成立的前提）。`,
    answer: `## 题意与约束分析
数组已排序 → 相同元素必然相邻，这是全题的题眼。要求原地 O(1) 空间，排除开新数组的哈希去重。n ≤ 3×10^4，一次遍历 O(n) 足够。

## 思路推导：从暴力到最优
暴力：Set 去重再写回，空间 O(n)，违反原地要求。
观察：已排序 ⇒ 重复元素连续出现 ⇒ 只需维护一个「已确认无重复」的区间。
最优：快慢双指针。fast 扫描全数组，slow 指向已去重区间最后一个位置；nums[fast] != nums[slow] 即发现新值，slow++ 写入。

## 正确性要点
- 循环不变式：任意时刻 nums[0..slow] 是 nums[0..fast] 去重后的结果，相对顺序不变。
- 初始 slow=0：首元素必然保留，不变式成立。
- 仅新值推进 slow，重复值跳过，无遗漏无混入。
- 有序性是新值判定只需与前一个保留值比较的前提，无序数组此算法不成立。

## 复杂度分析
- 时间：O(n)，fast 每元素访问一次
- 空间：O(1)，仅两个指针

## 代码实现
\`\`\`go
func removeDuplicates(nums []int) int {
    if len(nums) == 0 {
        return 0
    }
    slow := 0 // 已去重区间 [0, slow] 的右端点
    for fast := 1; fast < len(nums); fast++ {
        if nums[fast] != nums[slow] { // 发现新值
            slow++
            nums[slow] = nums[fast]
        }
    }
    return slow + 1 // 长度 = 下标 + 1
}
\`\`\`

## 边界与易错点
- 空数组直接返回 0，否则 slow=0 越界。
- 返回长度 slow+1，不是下标 slow，off-by-one 高发。
- slow 之后的脏数据无需清理，题目只看前 k 个。

## 举一反三
- LC 27 移除元素：同模板，保留条件换成 != val。
- LC 80 删除重复项 II：允许留 2 个，比较 nums[slow-1]。
- LC 283 移动零：快慢指针+交换。
- 识别信号：「有序数组+原地+O(1) 空间+保序删除」→ 快慢双指针。

## 实际工程应用
原地保序去重是流式日志处理基本功：访问日志按 IP 聚合前的预去重、列式存储（Parquet）的 RLE 压缩编码，核心都是「保序+写指针压缩」，与本题同构。`,
    keyPoints: [
      "慢指针 slow 语义：已去重区间右端点，不变式 nums[0..slow] 无重复",
      "有序 ⇒ 重复相邻 ⇒ 只需与前一个保留值比较，无需 Set",
      "返回 slow+1（长度）不是 slow（下标）",
      "无序数组去重只能哈希 O(n) 空间或排序 O(n log n)",
    ],
    followUps: [
      "允许每个元素最多保留 2 次怎么改？（LC 80）",
      "数组无序、O(1) 空间、不关心顺序时怎么去重？",
      "要求返回新数组不准改原数组时复杂度怎么变？",
    ],
    favorited: false,
  },

  {
    id: "algo-80",
    nodeId: "p1-array-string",
    question: `80. 删除有序数组中的重复项 II（LeetCode 80）
题意：给定非递减数组 nums，原地删除重复项使每个元素最多出现两次，返回新长度 k，前 k 项为处理后的结果，相对顺序保持不变。
示例：输入 [1,1,1,2,2,3] → 输出 5，nums 前 5 项为 [1,1,2,2,3]。
约束：1 ≤ nums.length ≤ 3×10^4，-10^4 ≤ nums[i] ≤ 10^4，数组已排序。`,
    answer: `## 题意与约束分析
LC 26 的放宽版：每个值最多保留 2 个而非 1 个。有序 ⇒ 同值必然相邻；原地 O(1) ⇒ 快慢指针。n ≤ 3×10^4，单趟 O(n) 即可。

## 思路推导：从暴力到最优
暴力：计数器记当前值出现次数，超 2 就标记删除再整体搬移，最坏 O(n²)。
观察：前两个元素必然保留；从第三个起，候选值能否保留只取决于「已保留序列的倒数第二个」——若候选值与它不同，说明该值目前最多保留了 1 个，可以再留。
最优：slow 指向已保留区间最后一个位置（初始 1，前两元素白送）；fast 从 2 开始扫，nums[fast] != nums[slow-1] 时保留（slow++ 写入）。

## 正确性要点
- 循环不变式：nums[0..slow] 是 nums[0..fast] 中「每值至多两次」的保序子序列。
- 若 nums[fast] == nums[slow] == nums[slow-1]（有序 ⇒ 三连相同必相邻），跳过 ⇒ 同值不超 2 个。
- 若 nums[fast] != nums[slow-1]，则 nums[fast] 在已保留序列中出现次数 ≤ 1，保留安全。
- 一般化：允许保留 k 个 ⇒ 比较 nums[slow-k+1]，slow 从 k-1 起，同理成立。

## 复杂度分析
- 时间：O(n) 单趟，对比暴力搬移 O(n²)
- 空间：O(1)，仅两个指针

## 代码实现
\`\`\`go
func removeDuplicates(nums []int) int {
    n := len(nums)
    if n <= 2 {
        return n
    }
    slow := 1 // 已保留区间 [0, slow]，slow 是最后一个保留位置
    for fast := 2; fast < n; fast++ {
        if nums[fast] != nums[slow-1] { // 与倒数第二个保留值不同才可留
            slow++
            nums[slow] = nums[fast]
        }
    }
    return slow + 1
}
\`\`\`

## 边界与易错点
- n ≤ 2 时直接返回 n，否则 slow-1 越界。
- 返回 slow+1（长度）不是 slow（下标），与 LC 26 同样的 off-by-one。
- 必须与倒数第二个保留值 nums[slow-1] 比较；与 nums[slow] 比会退化成只留一个。
- 无需清理 slow 之后的旧值，题目只检查前 k 个。

## 举一反三
- LC 26：k=1 的特例，比较 nums[slow]。
- LC 27 移除元素：保留条件换成 != val。
- 泛化题「每值最多留 k 个」：比较 nums[slow-k+1]，slow 从 k-1 起。
- 识别信号：「有序 + 原地 + 限量保留」→ 快慢指针 + 与第 k 个保留值比较。

## 实际工程应用
限量保留是告警抑制与采样降频的原型：监控系统对同一错误码「每分钟最多透传 2 条」的抑制策略、指标降采样时每个时间窗只保留前 k 个样本，都是「有序流 + 限量透传」模型，工程上用计数器+时间窗替代指针，思想与本题同构。`,
    keyPoints: [
      "slow 语义同 LC 26：已保留区间右端点；前两个元素必然保留",
      "保留判定：nums[fast] != nums[slow-1]（与倒数第二个保留值比较）",
      "泛化 k 次保留：比较 nums[slow-k+1]，slow 从 k-1 起",
      "不变式：nums[0..slow] 是每值至多两次的保序子序列",
    ],
    followUps: [
      "允许每个元素保留 k 次时，判定条件怎么写？",
      "数组无序时「每值最多两次」的原地 O(1) 去重还能做吗？",
      "LC 26（留 1 个）与本题的判定条件差在哪一行？",
    ],
    favorited: false,
  },

  {
    id: "algo-27",
    nodeId: "p1-array-string",
    question: `27. 移除元素（LeetCode 27）
题意：给定数组 nums 和数值 val，原地移除所有等于 val 的元素，返回新长度 k；前 k 个元素为不等于 val 的元素，元素的顺序可以改变。
示例：输入 nums = [3,2,2,3], val = 3 → 输出 2，前 2 项为 [2,2]。
约束：0 ≤ nums.length ≤ 100，0 ≤ nums[i], val ≤ 50；题目明确允许改变元素顺序。`,
    answer: `## 题意与约束分析
原地 O(1) 删除指定值。题目明确「顺序可变」，给出两种策略空间：保序用快慢指针，不保序可用头尾交换进一步减少写次数。n ≤ 100 任何 O(n) 都能过，考点在面试中能否讲清两种策略的取舍。

## 思路推导：从暴力到最优
暴力：每遇 val 就把后面元素整体前移，最坏 O(n²)（如全 val 数组）。
观察一（保序）：与 LC 26 同模板——slow 指向保留区写指针，fast 扫描，非 val 写入 slow 并推进。
观察二（不保序）：val 元素要消失 ⇒ 用尾元素回填：左指针遇 val 就把右端元素换过来、右指针左移；写操作次数 = val 出现次数，适合「写昂贵」场景。

## 正确性要点
- 快慢版不变式：nums[0..slow-1] 恰为已扫描区中的非 val 元素，相对顺序不变。
- 头尾版不变式：[0, l) 内无 val，(r, n) 内全为已判定的 val；换到 l 的元素尚未检查，故 l 不能立刻自增，需重新检查。
- 两版终止时 slow（或 l）都恰好是非 val 元素总数。

## 复杂度分析
- 时间：O(n)，两版相同，对比暴力 O(n²)
- 空间：O(1)；头尾版写入次数 ≤ val 出现次数，快慢版写入次数 = 非 val 个数

## 代码实现
\`\`\`go
func removeElement(nums []int, val int) int {
    slow := 0 // [0, slow) 为保留区
    for fast := 0; fast < len(nums); fast++ {
        if nums[fast] != val {
            nums[slow] = nums[fast] // 非 val 元素前移
            slow++
        }
    }
    return slow
}
\`\`\`

## 边界与易错点
- 空数组返回 0；全 val 数组 slow 不动返回 0，均无需特判。
- 头尾版换入元素后 l 不能立刻 ++，必须重新检查换过来的元素。
- 返回值是长度而不是数组；slow 之后的脏数据无需清理。
- 面试要主动问「是否需要保序」——决定用快慢版还是头尾版，是加分点。

## 举一反三
- LC 26 / 80 / 283：同写指针家族，保留条件各异。
- LC 905 按奇偶排序数组：不要求保序 ⇒ 对撞交换。
- LC 844 比较含退格的字符串：从尾部双指针 + 跳过逻辑。
- 识别信号：「原地删除 + 顺序可变」→ 优先头尾交换省写入。

## 实际工程应用
「顺序可变 ⇒ 尾部回填」是高性能删除的标准优化：C++ vector 的 swap-and-pop 删除、Linux 内核连接跟踪表（conntrack）的就地删除都用此思想——拿末尾元素填空位，把 O(n) 搬移降为 O(1) 交换，与本题头尾版完全一致。`,
    keyPoints: [
      "快慢版保序：slow 为保留区写指针，非 val 前移",
      "头尾交换版不保序，但写入次数 = val 个数，适合写昂贵场景",
      "面试先问「是否需要保序」决定策略，是加分点",
      "返回长度 slow，slow 之后的脏数据无需清理",
    ],
    followUps: [
      "头尾交换版为什么换入元素后左指针不能立刻前进？",
      "若要保序又想少写，怎么改？（fast!=slow 时才写入）",
      "删除链表中所有等于 val 的节点与本题思路差异？（LC 203）",
    ],
    favorited: false,
  },

  {
    id: "algo-88",
    nodeId: "p1-array-string",
    question: `88. 合并两个有序数组（LeetCode 88）
题意：nums1 长度为 m+n，前 m 项与长度为 n 的 nums2 均按非递减排列；把 nums2 合并进 nums1，使 nums1 整体有序，要求原地完成。
示例：输入 nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3 → 输出 [1,2,2,3,5,6]。
约束：0 ≤ m, n ≤ 200，nums1 尾部已预留 n 个空位（这是逆向写入的题眼）。`,
    answer: `## 题意与约束分析
归并排序的 merge 步骤，但要求就地在 nums1 内完成、O(1) 额外空间。nums1 尾部有 n 个空位是全题题眼——暗示从后往前写。数据量小，考点全在原地性论证。

## 思路推导：从暴力到最优
暴力：把 nums2 拷进尾部再整体 sort，O((m+n) log(m+n))，浪费已有序性。
次优：从前往后归并——但 nums1 前半段是有效数据，写入会覆盖未处理元素，需要 O(m) 缓冲数组。
关键观察：尾部是空的 ⇒ 从后往前写。写指针 w 从 m+n-1 起，每次取两数组当前最大值放到 w。w 始终 ≥ 两个读指针，永远不会覆盖还没读的数据。

## 正确性要点
- 循环不变式：nums1[w+1 .. m+n-1] 是已处理元素中最大的若干个，且升序排列。
- 覆盖安全性：任意时刻 w ≥ i 且 w ≥ j，写 nums1[w] 时 nums1[i] 尚未被覆盖，读数安全。
- 循环条件只需 j ≥ 0：nums2 有剩余必须并入；nums1 有剩余时其本就在正确位置，无需处理。
- 每步取大者写入 ⇒ 按降序填入 ⇒ 最终整体升序。

## 复杂度分析
- 时间：O(m+n)，每元素恰好读写一次，对比暴力 O((m+n) log(m+n))
- 空间：O(1)，三个指针

## 代码实现
\`\`\`go
func merge(nums1 []int, m int, nums2 []int, n int) {
    i, j, w := m-1, n-1, m+n-1 // w 为写指针，从尾部开始
    for j >= 0 {               // nums2 必须全部并入
        if i >= 0 && nums1[i] > nums2[j] {
            nums1[w] = nums1[i]
            i--
        } else {
            nums1[w] = nums2[j]
            j--
        }
        w--
    }
}
\`\`\`

## 边界与易错点
- n=0 时循环不进直接结束；m=0 时循环把 nums2 全拷入，天然正确。
- 循环条件若写成 i>=0 && j>=0，会漏掉 nums2 的剩余部分，必须再补循环；写成单条件 j>=0 最简洁。
- 判断里 i>=0 不可省：nums1 读尽后继续读 nums1[i] 会越界。
- 从前往后写是经典错误：会覆盖 nums1 中还没读的有效元素。

## 举一反三
- LC 21 合并两个有序链表：同一归并思想，换数据结构。
- LC 977 有序数组的平方：平方后两端大中间小 ⇒ 对撞指针从大往小填，同「逆向填充」。
- 归并排序本体：merge 子过程。
- 识别信号：「两个有序序列合并 + 目标容器尾部有空位」→ 逆向双指针。

## 实际工程应用
逆向填充与数据库 LSM-Tree 的 compaction 同源：多路归并写回新 SSTable 时为避免读写冲突采用预分配+定位写入；C++ 的 inplace_merge 在缓冲区不足时也利用尾部空间做反向归并，与本题思想一致。`,
    keyPoints: [
      "尾部有空位 ⇒ 从后往前写：w 始终 ≥ 读指针，覆盖安全",
      "循环条件只需 j>=0：nums1 剩余本就在位，nums2 必须并完",
      "每步取两数组当前最大值写入，降序填入得升序",
      "从前往后写会覆盖 nums1 未读元素——经典错误",
    ],
    followUps: [
      "若要求稳定合并（相等时 nums1 元素在前），等号该放哪边？",
      "两个有序链表合并如何复用此思想？（LC 21）",
      "nums1 没有预留空位时有哪些方案，代价各是什么？",
    ],
    favorited: false,
  },

  {
    id: "algo-169",
    nodeId: "p1-array-string",
    question: `169. 多数元素（LeetCode 169）
题意：给定大小为 n 的数组，找出多数元素——出现次数大于 ⌊n/2⌋ 的元素。题目保证多数元素一定存在。
示例：输入 [3,2,3] → 输出 3（3 出现 2 次 > 3/2）。
约束：n == nums.length，1 ≤ n ≤ 5×10^4，-10^9 ≤ nums[i] ≤ 10^9；进阶：O(n) 时间 + O(1) 空间。`,
    answer: `## 题意与约束分析
多数元素出现次数 > n/2 ⇒ 至多只有一个，且「多数 vs 其余所有元素之和」的力量对比严格大于 1:1。进阶要求 O(1) 空间，排除哈希计数 O(n) 与排序取中位 O(n log n)。

## 思路推导：从暴力到最优
暴力：对每个元素数出现次数，O(n²)。哈希：O(n) 时间 O(n) 空间。排序：nums[n/2] 必是多数，O(n log n)。
关键观察（配对抵消）：多数元素票数 > 其余所有之和 ⇒ 把「一个多数 + 一个非多数」成对扔掉，剩余数组中多数仍是多数。两两抵消到最后，站着的必是多数。
最优（Boyer-Moore 投票）：cand 为候选人，count 为净胜票。遇同票 count++，异票 count--；count 归零说明此前区间多数不占优，可整体丢弃，换新候选人。

## 正确性要点
- 核心断言：若某前缀中 cand 出现次数恰好等于其他元素次数（count 归零），删除该前缀后，多数元素在剩余数组中仍是多数——它在前缀占比 ≤ 1/2、在整体 > 1/2，故在剩余部分必 > 1/2。
- 归纳：每次归零丢弃一段，剩余数组的多数不变 ⇒ 最终留下的候选人即多数。
- 题目保证多数存在 ⇒ 无需二次验证；若变体不保证，必须再扫一遍验票。

## 复杂度分析
- 时间：O(n) 单趟
- 空间：O(1)，对比哈希 O(n)、排序 O(n log n)

## 代码实现
\`\`\`go
func majorityElement(nums []int) int {
    cand, count := 0, 0
    for _, x := range nums {
        if count == 0 {
            cand = x // 旧候选人已全军覆没，换新候选
        }
        if x == cand {
            count++
        } else {
            count-- // 一多数一少数配对抵消
        }
    }
    return cand
}
\`\`\`

## 边界与易错点
- 单元素数组：count 归零置候选再 ++，返回自身，正确。
- count==0 时必须先换候选再判断同异，顺序错了会漏一票。
- 变体「多数可能不存在」（如 LC 229 的验证步骤）必须二次遍历计数验证。
- 别和「出现次数最多的元素」混淆：Boyer-Moore 只在「> n/2」前提下成立。

## 举一反三
- LC 229 多数元素 II（> n/3，至多两个）：两个候选人两组计数，最后双重验证。
- LC 1157 子数组多数元素：投票思想 + 线段树/随机化。
- 识别信号：「出现次数超半 + O(1) 空间」→ 摩尔投票；「超 1/k」→ k-1 个候选人。

## 实际工程应用
Boyer-Moore 投票是分布式多数派（quorum）确认的直觉模型：Raft/Paxos 选主要求 > N/2 同意，本质保证「两两对抗后多数派必胜」；流式系统用投票法在 O(1) 内存里估算数据流中的主流值，与本题同源。`,
    keyPoints: [
      "配对抵消：多数票 > 其余总和，一多一少成对丢弃后多数仍是多数",
      "count 归零 ⇒ 该前缀多数不占优可整体丢弃，剩余数组多数不变",
      "count==0 时先换候选再计票，顺序错会漏一票",
      "多数不保证存在时必须二次遍历验票（如 LC 229 的验证步骤）",
    ],
    followUps: [
      "找出所有出现次数 > n/3 的元素怎么做？（LC 229 双候选人）",
      "为什么 Boyer-Moore 不能用于「出现次数最多的元素」？",
      "数据流只能读一遍且内存 O(1)，如何估算多数元素？",
    ],
    favorited: false,
  },

  {
    id: "algo-189",
    nodeId: "p1-array-string",
    question: `189. 轮转数组（LeetCode 189）
题意：给定数组 nums，向右轮转 k 个位置（尾部 k 个元素移到头部，内部顺序不变），要求原地 O(1) 空间。
示例：输入 nums = [1,2,3,4,5,6,7], k = 3 → 输出 [5,6,7,1,2,3,4]。
约束：1 ≤ nums.length ≤ 10^5，0 ≤ k ≤ 10^5（k 可能大于 n，必须先取模）。`,
    answer: `## 题意与约束分析
右轮 k 位 = 后 k 个元素整体搬到前面。n、k 都可达 10^5 ⇒ k 必须先取模（k %= n）。要求 O(1) 空间 ⇒ 排除切片拼接 O(n) 空间，指向「三次翻转」或「环状替换」。

## 思路推导：从暴力到最优
暴力：逐位右移，重复 k 次，每次 O(n)，总 O(n·k)，k=10^5 时 10^10 超时。
观察：右轮 k 位后，原后 k 个元素到了头部、前 n-k 个跟在后面 ⇒ 若先整体翻转，前后两段各自逆序；再把两段分别翻回来即得答案。三次翻转，每元素被访问常数次。
备选：环状替换（从 0 出发沿 +k 跳跃逐位置换，共 gcd(n,k) 个环），同样 O(n)/O(1) 但代码复杂、易写错。

## 正确性要点
- 设 A = 前 n-k 段，B = 后 k 段，目标是把 AB 变成 BA。
- 恒等式：reverse(AB) = B'A'（' 表示段内逆序）；再翻 B' 得 B、翻 A' 得 A ⇒ 三段翻转恰好得到 BA。
- k %= n 保证 k < n，此时 reverse(0,k-1) 与 reverse(k,n-1) 区间合法。
- 每元素至多被交换两次 ⇒ 正确且 O(n)。

## 复杂度分析
- 时间：O(n)，三次翻转共约 2n 次元素访问，对比暴力 O(n·k)
- 空间：O(1)，仅翻转用的临时变量

## 代码实现
\`\`\`go
func rotate(nums []int, k int) {
    n := len(nums)
    if n == 0 {
        return
    }
    k %= n // k 可能大于 n
    reverse := func(l, r int) {
        for l < r {
            nums[l], nums[r] = nums[r], nums[l]
            l++
            r--
        }
    }
    reverse(0, n-1) // 整体翻转：AB -> B'A'
    reverse(0, k-1) // 前 k 个翻回：B' -> B
    reverse(k, n-1) // 后 n-k 个翻回：A' -> A
}
\`\`\`

## 边界与易错点
- 忘记 k %= n：k ≥ n 时 reverse(0, k-1) 越界 panic，最高频挂点。
- n=1 或 k%n==0：翻转空段/全段，天然正确；但要防 n==0 时取模除零。
- 翻转顺序（先整体还是先两段）不影响结果，先整体翻最易记。
- Go 切片共享底层数组，闭包 reverse 直接改原数组即生效。

## 举一反三
- LC 61 旋转链表：右轮 k 位，先取模、再接尾成环后断环。
- 剑指 Offer 58 左旋转字符串：同三次翻转，方向相反。
- LC 541 / 557 反转字符串系列：翻转子过程本身。
- 识别信号：「数组/字符串整体旋转 + 原地 O(1)」→ 三次翻转。

## 实际工程应用
三次翻转是环形缓冲区（ring buffer）块旋转的经典手法：音频处理中旋转采样窗口、日志系统 logrotate 的就地段重排都用「双翻转等价段交换」这一恒等式，零额外内存完成 AB→BA，是嵌入式音频/网络协议栈的日常操作。`,
    keyPoints: [
      "恒等式：reverse(AB)=B'A'，两段各自再翻 ⇒ BA，三次翻转完成轮转",
      "k %= n 是安全前提，漏了会越界 panic",
      "每元素至多访问两次 ⇒ O(n)；环状替换同复杂度但代码复杂",
      "Go 切片共享底层数组，闭包内翻转即原地生效",
    ],
    followUps: [
      "左旋 k 位与右旋是什么关系，代码怎么改？",
      "环状替换法为什么外层循环次数是 gcd(n,k)？",
      "链表右旋 k 位怎么做？（LC 61）",
    ],
    favorited: false,
  },

  {
    id: "algo-41",
    nodeId: "p1-array-string",
    question: `41. 缺失的第一个正数（LeetCode 41）
题意：给定未排序的整数数组 nums，找出其中没有出现的最小正整数，要求 O(n) 时间 + O(1) 空间。
示例：输入 [3,4,-1,1] → 输出 2（1 在数组中、2 不在，2 即缺失的最小正数）。
约束：1 ≤ nums.length ≤ 10^5，-2^31 ≤ nums[i] ≤ 2^31-1，含负数、零与重复值。`,
    answer: `## 题意与约束分析
O(n) 时间 + O(1) 空间的组合排除了排序与哈希集合。关键数学事实：n 个元素的数组，缺失的最小正整数必落在 [1, n+1]——前 n 个正整数若全部出现，答案是 n+1；否则答案 ≤ n。这把值域压缩到下标范围内，使「下标即哈希」成立。

## 思路推导：从暴力到最优
暴力：排序后从 1 开始扫，O(n log n) 不满足要求。哈希集合：O(n) 时间但 O(n) 空间违规。
观察：答案 ∈ [1, n+1] ⇒ 只关心值 1..n 是否出现 ⇒ 用数组自身当下标哈希表：把值 x 放到它该在的位置 x-1（鸽子归巢 / 循环交换）。
最优：第一趟遍历，若 nums[i] ∈ [1,n] 且不在正确位置，就与目标位置交换，直到归位或目标位置已是相同值；第二趟找第一个 nums[i] != i+1 的下标，答案 i+1；全部归位则答案 n+1。

## 正确性要点
- 鸽巢原理：n 个槽对应值 1..n，若 1..n 全出现则每槽归位、答案 n+1；否则必有槽错位，第一个错位槽对应的值即缺失值。
- 交换收敛性：每次交换至少把 nums[i] 或换入值放到最终正确位置，归位元素不再移动 ⇒ 总交换 ≤ n 次，均摊 O(n)。
- nums[nums[i]-1] != nums[i] 的判断防重复值死循环（两位置值相同即停）。
- 范围外的值（≤0 或 >n）原地不管，它们不影响 [1,n] 的归属判定。

## 复杂度分析
- 时间：O(n)，外层 n 次 + 内层总交换 ≤ n 次
- 空间：O(1)，原地修改

## 代码实现
\`\`\`go
func firstMissingPositive(nums []int) int {
    n := len(nums)
    for i := 0; i < n; i++ {
        // 把 nums[i] 换到它该在的位置 nums[x-1]，直到归位或越界
        for nums[i] >= 1 && nums[i] <= n && nums[nums[i]-1] != nums[i] {
            j := nums[i] - 1
            nums[i], nums[j] = nums[j], nums[i]
        }
    }
    for i := 0; i < n; i++ {
        if nums[i] != i+1 { // 第一个错位处即答案
            return i + 1
        }
    }
    return n + 1
}
\`\`\`

## 边界与易错点
- 内层 for 的三个条件（≥1、≤n、目标位不同值）缺一不可，漏第三个在 [1,1] 上死循环。
- 交换后不能立刻 i++：换回来的值可能仍需继续归位。
- 全部归位时返回 n+1（如 [1,2,3] → 4），最易漏的返回值。
- 负数、0、超大值直接跳过，不参与交换。

## 举一反三
- LC 448 找到所有数组中消失的数字：同下标哈希，用取负标记代替交换。
- LC 442 数组中重复的数据：取负标记法，值域 [1,n] 是共同前提。
- LC 268 丢失的数字（[0,n] 缺一个）：异或或求和，更简单。
- 识别信号：「值域 ⊆ [1,n] + O(1) 空间 + 存在性统计」→ 下标原地哈希。

## 实际工程应用
下标即哈希是空间压缩的经典思想：布隆过滤器用位数组+多哈希判存在、数据库位图索引（bitmap index）用比特位表记录取值存在性，与本题「用数组自身槽位承载存在信息」一脉相承——省掉独立哈希表，以 O(1) 附加空间完成 membership 统计。`,
    keyPoints: [
      "答案必在 [1,n+1]：n 个槽最多容纳 1..n，全在则缺 n+1（鸽巢原理）",
      "循环交换归位：把 x 换到 nums[x-1]，每次交换至少归位一个元素",
      "防死循环：目标位已是同值（重复）时停止交换",
      "范围外值（≤0 或 >n）原地忽略，不影响 [1,n] 的归属判定",
    ],
    followUps: [
      "为什么内层是循环而不是单次交换？（换回来的值可能仍需归位）",
      "用取负标记代替交换怎么写？值域含负数时标记法会出什么问题？",
      "改成找缺失的最小非负整数（含 0）要怎么调整？",
    ],
    favorited: false,
  },

  {
    id: "algo-11",
    nodeId: "p1-array-string",
    question: `11. 盛最多水的容器（LeetCode 11）
题意：给定 n 条垂直线 height[i]，选两条与 x 轴构成容器，使盛水最多。面积 = min(两柱高度) × 两柱间距，返回最大面积。
示例：输入 [1,8,6,2,5,4,8,3,7] → 输出 49（选下标 1 和 8：min(8,7)×7=49）。
约束：n == height.length，2 ≤ n ≤ 10^5，0 ≤ height[i] ≤ 10^4。`,
    answer: `## 题意与约束分析
n ≤ 10^5 ⇒ O(n²) 枚举所有柱对（约 10^10 对）必然超时，必须 O(n)。面积由「矮柱 × 宽度」决定——宽度大但矮柱限制面积，这一 trade-off 正是双指针移动方向的依据。

## 思路推导：从暴力到最优
暴力：枚举所有 (i,j) 对算面积取最大，O(n²)。
观察：从最宽 pair（0, n-1）起步，之后宽度只会缩小；想补偿宽度损失，必须提高矮柱高度 ⇒ 移动矮的一侧指针有可能找到更高的矮柱，移动高的一侧则面积必然变小（宽度↓ 且矮柱 ≤ 原矮柱）。
最优：对撞双指针，l=0、r=n-1，每步计算面积并更新答案，移动矮侧指针，直到两指针相遇。

## 正确性要点
- 排除法论证：设 height[l] ≤ height[r]。任何 (l, j)（j<r）的面积 ≤ height[l]×(j-l) < height[l]×(r-l) ⇒ l 参与的所有未枚举 pair 都不如当前 ⇒ 排除 l 不会丢失最优解。
- 每步排除一侧的全部可能性，搜索空间从 O(n²) 收敛到 O(n) 步，且最优解必在搜索路径上。
- 两柱相等时移动任一侧均可，排除论证对称成立。

## 复杂度分析
- 时间：O(n)，每步排除一根柱子
- 空间：O(1)，仅指针与最值变量

## 代码实现
\`\`\`go
func maxArea(height []int) int {
    l, r := 0, len(height)-1
    best := 0
    for l < r {
        h := height[l]
        if height[r] < h {
            h = height[r] // h 为两侧矮柱
        }
        if area := h * (r - l); area > best {
            best = area
        }
        if height[l] < height[r] { // 矮的一侧不可能再出更优解，排除
            l++
        } else {
            r--
        }
    }
    return best
}
\`\`\`

## 边界与易错点
- 移动条件写反（移动高侧）是经典错误：会跳过最优解。
- n=2 时一轮循环直接得答案，无需特判。
- 存在零高度：面积为 0，矮侧指针正常推进。
- 面积最大 10^4 × 10^5 = 10^9，Go int 无溢出之虞。

## 举一反三
- LC 42 接雨水：同为「矮板决定水量」，双指针按矮侧结算。
- LC 167 两数之和 II（有序数组）：对撞双指针。
- LC 15 / 18 三数/四数之和：排序 + 内层对撞双指针。
- 识别信号：「双变量围区间 + 目标随双端变化 + 要把 O(n²) 降到 O(n)」→ 对撞双指针 + 排除法论证。

## 实际工程应用
排除法双指针是查询优化的直觉模型：数据库做 range join 时用排序+双端收敛裁剪扫描空间、广告竞价系统在「预算 × 流量」二维约束下快速收敛最优配对，本质都是「证明一侧不可能更优就整体裁剪」，与本题移动矮侧的论证同构。`,
    keyPoints: [
      "面积 = 矮柱 × 宽度；从最宽起步宽度只减，必须博矮柱变高",
      "排除法：移动矮侧，该侧所有未枚举 pair 均不优于当前 ⇒ 无漏",
      "移动高侧面积必变小（宽↓、矮柱不增）⇒ 恒不移动",
      "两柱相等时任移一侧，排除论证对称成立",
    ],
    followUps: [
      "为什么移动高侧永远不可能得到更优解？写出一行不等式证明。",
      "若要求返回取得最大面积的两根柱子下标，怎么改？",
      "接雨水（LC 42）与本题的「矮板逻辑」有什么异同？",
    ],
    favorited: false,
  },

  {
    id: "algo-15",
    nodeId: "p1-array-string",
    question: `15. 三数之和（LeetCode 15）
题意：给定整数数组 nums，找出所有和为 0 且不重复的三元组 [a,b,c]，按值去重（不是按下标去重）。
示例：输入 [-1,0,1,2,-1,-4] → 输出 [[-1,-1,2],[-1,0,1]]（值相同的三元组只算一组）。
约束：3 ≤ nums.length ≤ 3000，-10^5 ≤ nums[i] ≤ 10^5。`,
    answer: `## 题意与约束分析
要求返回所有解且按值去重 ⇒ 两数之和的哈希法在这里面临去重噩梦，排序+双指针成为标准答案。n ≤ 3000，O(n²) 约 9×10^6 可行，O(n³) 的 2.7×10^10 超时。

## 思路推导：从暴力到最优
暴力：三重循环枚举 + Set 去重，O(n³)，超时。
观察：排序后固定第一个数 nums[i]，问题化为「在 nums[i+1..n-1] 中找两数和 = -nums[i]」——有序数组两数之和用对撞双指针 O(n) 解决。
去重分三层：① i 层：nums[i]==nums[i-1] 跳过；② 命中后 l 跳过与右侧相同值；③ 同理 r。排序使同值相邻，去重只需看邻居。

## 正确性要点
- 排序后按 i<l<r 枚举有序三元组，所有解都会被枚举到，且每组值只被枚举一次（同值元素相对次序固定）。
- 双指针不漏：sum<0 时移 r 只会更小故必移 l，sum>0 反之，排除法与 LC 11 同理。
- 提前剪枝：nums[i]>0 时三数皆正，和不可能为 0，直接 break（排序保证后面更大）。

## 复杂度分析
- 时间：O(n²)，排序 O(n log n) + 外层 n × 内层 O(n)
- 空间：O(log n)~O(n) 排序栈空间（不计输出）

## 代码实现
\`\`\`go
func threeSum(nums []int) [][]int {
    sort.Ints(nums)
    res := [][]int{}
    n := len(nums)
    for i := 0; i < n-2; i++ {
        if i > 0 && nums[i] == nums[i-1] {
            continue // 第一层去重：固定数跳过重复值
        }
        if nums[i] > 0 {
            break // 排序后最小数已为正，不可能凑 0
        }
        l, r := i+1, n-1
        for l < r {
            sum := nums[i] + nums[l] + nums[r]
            if sum < 0 {
                l++
            } else if sum > 0 {
                r--
            } else {
                res = append(res, []int{nums[i], nums[l], nums[r]})
                for l < r && nums[l] == nums[l+1] {
                    l++ // 第二层去重
                }
                for l < r && nums[r] == nums[r-1] {
                    r-- // 第三层去重
                }
                l++
                r--
            }
        }
    }
    return res
}
\`\`\`

## 边界与易错点
- 三层去重漏任何一层都会输出重复组，这是最高频挂点。
- 命中去重后 l、r 必须再各走一步（l++、r--），否则死循环或重复输出。
- 去重循环必须带 l<r 防越界。
- [0,0,0,0] 应只输出一组 [0,0,0]，是检验去重完备性的最小用例。

## 举一反三
- LC 16 最接近的三数之和：不判等，维护最小差值。
- LC 18 四数之和：再套一层循环，大数溢出要换 int64。
- LC 259 较小的三数之和：只要计数，双指针区间累加。
- 识别信号：「k 数之和 + 返回所有解 + 按值去重」→ 排序 + (k-2) 层循环 + 对撞双指针。

## 实际工程应用
「排序降维 + 双指针收敛」是多表关联查询的经典策略：OLAP 引擎做三表等值 join 时先 sort-merge 再扫描收敛、风控规则引擎匹配「三特征组合命中黑名单」时也是先排序固定一维再双指针扫剩余两维，思想同构。`,
    keyPoints: [
      "排序使「固定一数 + 对撞双指针」成立，且同值相邻便于去重",
      "三层去重：i 层、命中后 l 层、r 层，漏一层即重复输出",
      "剪枝：nums[i]>0 直接 break，三正数不可能和为 0",
      "命中后 l、r 去重完必须再各走一步，否则死循环",
    ],
    followUps: [
      "四数之和怎么扩展？为什么累加和要换 int64？（LC 18）",
      "只统计三元组个数不输出明细时，能否做得更快？",
      "最接近 target 的三数之和怎么改？（LC 16）",
    ],
    favorited: false,
  },

  {
    id: "algo-42",
    nodeId: "p1-array-string",
    question: `42. 接雨水（LeetCode 42）
题意：给定 n 根柱子的高度图 height，计算下雨后能接多少水。单柱水量 = min(左侧最高柱, 右侧最高柱) - 自身高度（负则为 0）。
示例：输入 [0,1,0,2,1,0,1,3,2,1,2,1] → 输出 6（共接 6 个单位水）。
约束：n == height.length，1 ≤ n ≤ 2×10^4，0 ≤ height[i] ≤ 10^5。`,
    answer: `## 题意与约束分析
核心公式：water[i] = max(0, min(leftMax[i], rightMax[i]) - height[i])。n ≤ 2×10^4，O(n) 空间的 DP 也能过，但 O(1) 空间的双指针是面试满分答案，且论证过程是考察重点。

## 思路推导：从暴力到最优
暴力：对每根柱向左右扫最大值，O(n²)。
DP：预处理 leftMax/rightMax 两个数组，O(n) 时间 O(n) 空间——可用但不最优。
关键观察：对位置 l，若已知 leftMax < 右端某根真实存在的柱子高度，则无论右侧中间还有什么，l 的水位由 leftMax 单独决定（min 取小者）⇒ 矮边可立即结算，无需知道另一侧的精确最大值。
最优：双指针 l、r 从两端收缩，维护 leftMax、rightMax；height[l] < height[r] 时结算 l，否则对称结算 r。

## 正确性要点
- 矮边结算合法：height[l] < height[r] ⇒ 右端存在高度 ≥ height[r] 的柱 ⇒ l 的真实 rightMax ≥ height[r] > height[l]；而 leftMax 已知 ⇒ min(leftMax, rightMax) = leftMax，结算正确。
- leftMax/rightMax 随指针推进单调更新，始终等于已扫区间的真实最大值。
- 每根柱恰被结算一次（l 或 r 经过它），不重不漏。
- 对比单调栈：栈存递减下标，遇高柱弹栈按「凹槽」横向结算，同样 O(n) 时间但要 O(n) 空间。

## 复杂度分析
- 时间：O(n)，对比暴力 O(n²)
- 空间：O(1)，对比 DP / 单调栈的 O(n)

## 代码实现
\`\`\`go
func trap(height []int) int {
    l, r := 0, len(height)-1
    leftMax, rightMax := 0, 0
    water := 0
    for l < r {
        if height[l] < height[r] { // 左侧矮：l 的水位只由 leftMax 决定
            if height[l] >= leftMax {
                leftMax = height[l]
            } else {
                water += leftMax - height[l]
            }
            l++
        } else { // 右侧矮或相等：对称结算 r
            if height[r] >= rightMax {
                rightMax = height[r]
            } else {
                water += rightMax - height[r]
            }
            r--
        }
    }
    return water
}
\`\`\`

## 边界与易错点
- 结算与更新互斥：height[l] >= leftMax 时只更新最大值，不加水。
- 两端相等时走哪边都行，代码统一走 else 分支结算 r。
- n ≤ 2 时必为 0，循环天然不产生水量。
- 常见误解：按「当前柱与相邻柱的局部凹槽」加水——水量由全局两侧最大值决定，与邻居无关。

## 举一反三
- LC 11 盛最多水的容器：矮板逻辑的近亲，对照记忆。
- LC 84 柱状图中最大的矩形：单调栈经典，与接雨水的栈解法对照。
- LC 407 接雨水 II（二维）：堆 + BFS，完全不同的量级。
- 识别信号：「两侧约束取 min 决定单点的值」→ 双指针矮边结算或单调栈。

## 实际工程应用
「矮边先结算」是信息不完全时流式决策的模型：网络拥塞控制按 min(拥塞窗口, 接收窗口) 决定发送量、供应链按 min(上游产能, 下游需求) 逐段结算瓶颈——只需确认瓶颈侧即可先行结算非瓶颈侧，与本题双指针的论证完全一致。`,
    keyPoints: [
      "单柱水量 = min(左最大, 右最大) - 柱高；由全局两侧最大值决定而非邻居",
      "矮边先结算：height[l]<height[r] 时 l 的 min 必为 leftMax，可立即结算",
      "leftMax/rightMax 单调维护，每柱恰结算一次 ⇒ O(n) 时间 O(1) 空间",
      "单调栈按凹槽横向结算是等价视角，O(n) 空间",
    ],
    followUps: [
      "为什么 height[l]<height[r] 时无需知道右侧的精确最大值？",
      "用单调栈重写一遍，两种视角各自结算的是什么「区域」？",
      "二维地形接雨水怎么做？（LC 407，堆+BFS）",
    ],
    favorited: false,
  },

  {
    id: "algo-3",
    nodeId: "p1-array-string",
    question: `3. 无重复字符的最长子串（LeetCode 3）
题意：给定字符串 s，求不含重复字符的最长子串的长度（子串必须连续，不是子序列）。
示例：输入 "abcabcbb" → 输出 3（答案是 "abc"）。
约束：0 ≤ s.length ≤ 5×10^4，s 由英文字母、数字、符号和空格组成。`,
    answer: `## 题意与约束分析
子串连续 ⇒ 滑动窗口的天然场景。n ≤ 5×10^4 ⇒ O(n²) 枚举起点（约 2.5×10^9）超时，必须 O(n)。字符集含空格与符号 ⇒ 用 map 而非 26 长数组（或 [128]int 覆盖 ASCII）。

## 思路推导：从暴力到最优
暴力：枚举每个起点向右扩展到重复，O(n²)。
观察：窗口 [left, right] 无重复时，固定 left 的最大 right 已知；left 右移后 right 无需回退——新窗口只是旧窗口去掉左端。
进一步优化：当 s[right] 与窗口内位置 p 的字符重复，left 必须一步跳到 p+1（left 落在 [left, p] 内的窗口都含重复，无检查价值）⇒ 记录每个字符最近出现位置，left 直接跳转，right 永不回退 ⇒ O(n)。

## 正确性要点
- 循环不变式：每轮迭代时窗口 [left, right] 内无重复字符。
- right 遇重复字符且其上次位置 p ≥ left：left 更新为 p+1；若 p < left（重复在窗口外）不影响，left 不动。
- left 单调不减、right 单调增 ⇒ 每字符进出窗口各一次 ⇒ O(n)。
- 每步「以 right 结尾的最长合法子串」长度 = right-left+1，取 max 即全局最优（最优子串必有右端点，必被覆盖）。

## 复杂度分析
- 时间：O(n)，双指针各走 n 步，对比暴力 O(n²)
- 空间：O(min(n, |Σ|))，以字符集大小为界

## 代码实现
\`\`\`go
func lengthOfLongestSubstring(s string) int {
    last := make(map[byte]int) // 字符 -> 最近出现下标+1（0 表示未见过）
    left, best := 0, 0
    for right := 0; right < len(s); right++ {
        if p := last[s[right]]; p > left { // 重复字符在窗口内
            left = p // 左边界跳到重复位置之后
        }
        last[s[right]] = right + 1
        if right-left+1 > best {
            best = right - left + 1
        }
    }
    return best
}
\`\`\`

## 边界与易错点
- 重复字符上次位置在窗口外（p < left）时不能收缩 left，否则窗口错缩——"abba" 中第二个 a 即此陷阱。
- 存「下标+1」可用零值表示「未见过」，省一个布尔表。
- 空串返回 0，循环不进自然正确。
- 逐字节处理只对 ASCII 正确；若含中文需先转 []rune。

## 举一反三
- LC 438 找到字符串中所有字母异位词：定长窗口 + 计数比较。
- LC 76 最小覆盖子串：变长窗口 + 满足性计数，难度跃升。
- LC 904 水果成篮 / LC 159 至多两个不同字符：窗口约束换成「种类数 ≤ 2」。
- 识别信号：「连续子串/子数组 + 满足某约束的最长/最短」→ 滑动窗口。

## 实际工程应用
滑动窗口+最近位置表是流式去重与协议处理的标配：TCP 接收窗口按序列号滑动去重、API 网关的滑动窗口限流（每 key 记录最近时间戳）、日志实时分析里「最近 N 分钟唯一访客」统计，都是同一副「右扩左缩 + 状态表」骨架。`,
    keyPoints: [
      "不变式：窗口 [left,right] 始终无重复；遇重 left 跳到上次位置+1",
      "left 只增不减 + right 不回退 ⇒ 严格 O(n)",
      "重复字符上次位置在窗口外（p<left）时不能收缩 left（abba 陷阱）",
      "存「下标+1」可用 0 表示未出现，省一个布尔表",
    ],
    followUps: [
      "至多含两个不同字符的最长子串怎么改？（LC 159 / 904）",
      "要求返回子串本身而不仅是长度，怎么改？",
      "字符串含中文时逐字节扫描会出什么问题？",
    ],
    favorited: false,
  },

  {
    id: "algo-438",
    nodeId: "p1-array-string",
    question: `438. 找到字符串中所有字母异位词（LeetCode 438）
题意：给定字符串 s 和 p，找出 s 中所有 p 的异位词子串的起始下标（字符计数与 p 完全相同的连续子串）。
示例：输入 s = "cbaebabacd", p = "abc" → 输出 [0,6]（下标 0 的 "cba" 与下标 6 的 "bac"）。
约束：1 ≤ s.length, p.length ≤ 3×10^4，仅含小写英文字母。`,
    answer: `## 题意与约束分析
异位词 ⇔ 26 字母计数向量相等。窗口长度固定为 |p| ⇒ 定长滑动窗口。n ≤ 3×10^4 ⇒ 每窗 O(26) 比较、总 O(26n) 可过；差异计数器可做到每窗 O(1) 但非必需。

## 思路推导：从暴力到最优
暴力：枚举每个起点，取 len(p) 长子串排序比较，O(n·k log k)，超时。
观察：窗口右移一格，只是出一个字符、进一个字符，计数数组可 O(1) 增量维护；比较两个 26 长数组 O(26)。
最优：need 数组存 p 的计数，win 数组存当前窗口计数；首窗初始化后，每步右端 ++、左端 --，win == need（Go 数组可直接 == 比较）则记录起点。

## 正确性要点
- 异位 ⇔ 计数向量相等（多重集相等判定），充要条件，无漏无错。
- 窗口长度恒为 len(p)：先加右端再减左端，步进后窗口恰好代表 [i-len(p)+1, i]。
- 增量维护正确性：每步只改两个槽位，win 始终等于当前窗口的真实计数。
- 起点下标 = 右端 i - len(p) + 1，off-by-one 是主要挂点。

## 复杂度分析
- 时间：O(n + 26n) ≈ O(n)，对比暴力 O(n·k log k)
- 空间：O(1)，两个 26 长数组（不计输出）

## 代码实现
\`\`\`go
func findAnagrams(s string, p string) []int {
    res := []int{}
    if len(p) > len(s) {
        return res
    }
    var need, win [26]int
    for i := 0; i < len(p); i++ { // 初始化目标计数与首个窗口
        need[p[i]-'a']++
        win[s[i]-'a']++
    }
    if need == win { // Go 数组可直接比较
        res = append(res, 0)
    }
    for i := len(p); i < len(s); i++ { // 右进左出，窗口定长
        win[s[i]-'a']++
        win[s[i-len(p)]-'a']--
        if win == need {
            res = append(res, i-len(p)+1)
        }
    }
    return res
}
\`\`\`

## 边界与易错点
- len(p) > len(s) 直接返回空，否则首窗初始化越界。
- 先加后减顺序写反，会在同一轮里减错下标。
- 首窗（下标 0）必须单独检查，主循环从 i=len(p) 起。
- Go 数组可 == 比较是语言福利；换语言要逐槽比或改用差异计数器。

## 举一反三
- LC 567 字符串的排列：同模型，只要找到一个即返回 true。
- LC 242 有效的字母异位词：计数数组基础版。
- LC 76 最小覆盖子串：变长窗口 + 计数，难度跃升。
- 识别信号：「定长子串 + 字符多重集匹配」→ 定长滑窗 + 计数数组。

## 实际工程应用
定长滑窗+指纹比对是序列匹配的工程原型：生物信息学在基因组上搜固定 k-mer 模式、入侵检测系统（IDS）用定长窗口匹配攻击特征签名、备份系统内容定义分块（CDC）前的滚动校验和，骨架与本题一致。`,
    keyPoints: [
      "异位 ⇔ 计数向量相等：定长窗口 + 双计数数组逐窗比较",
      "窗口增量维护：右进一出，每步 O(1)，比较 O(26)",
      "Go 数组可直接 == 比较是语言福利，其他语言用差异计数器",
      "起点 = 右端下标 - len(p) + 1，off-by-one 高发",
    ],
    followUps: [
      "只判定是否存在一个异位词子串怎么简化？（LC 567）",
      "用单个差异计数器（matched 种数）把比较降到 O(1) 怎么写？",
      "p 比 s 长时不做前置返回会发生什么？",
    ],
    favorited: false,
  },
];