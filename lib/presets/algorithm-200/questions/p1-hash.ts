// lib/presets/algorithm-200/questions/p1-hash.ts
// 「算法 200 题」Phase 1 · 哈希表专题（10 题深度题解）
//
// 设计说明：
//   - 每题答案为八段式深度结构：题意约束分析 → 思路推导（从暴力到最优）→
//     正确性要点 → 复杂度对比 → Go 实现 → 边界易错 → 举一反三 → 实际工程应用
//   - 本专题主线：哈希表四大用法——存在性查询、计数统计、自定义 key（规范形）、
//     双向映射；以及「下标即哈希」「桶排」「Floyd 判环」等哈希思想的空间压缩变体
//   - 旧版答案只贴代码被用户投诉无深度，本版按面试口述标准重写

import type { Question } from "../../../types";

export const P1_HASH_QUESTIONS: Question[] = [
  {
    id: "algo-347",
    nodeId: "p1-hash",
    question: `347. 前 K 个高频元素（LeetCode 347）
题意：给定整数数组 nums 和整数 k，返回出现频率前 k 高的元素。答案唯一，返回顺序不限。
示例：输入 nums = [1,1,1,2,2,3], k = 2 → 输出 [1,2]（1 出现 3 次、2 出现 2 次）。
约束：1 ≤ nums.length ≤ 10^5，k ≤ 不同元素的个数；进阶：时间复杂度必须优于 O(n log n)。`,
    answer: `## 题意与约束分析
「前 k 高频」= 统计 + Top-K。进阶要求优于 O(n log n) ⇒ 全排序出局；堆 O(n log k) 达标，桶排 O(n) 最优。频率上界是 n ⇒ 可以用「下标即频次」的桶。

## 思路推导：从暴力到最优
暴力：哈希计数后对所有不同元素按频次排序，O(m log m)，m 为不同元素数，最坏 O(n log n)。
堆：维护大小 k 的小顶堆，O(n log k)，k 小时很优，但实现较重。
桶排：频次值域 ⊆ [1, n] ⇒ 开 n+1 个桶，bucket[f] 装频次为 f 的所有元素；从高频桶向低频扫描，凑满 k 个即止。计数 O(n) + 建桶 O(m) + 扫描 O(n) = O(n)，而计数本身就要 Ω(n)，已是理论下界。

## 正确性要点
- 频次值域 ⊆ [1,n] ⇒ 桶数组长度 n+1 足够，任何元素都有桶可入，无遗漏。
- 从高到低扫桶：先扫到的元素频次 ≥ 后扫到的 ⇒ 凑满 k 个时，截断点之后元素频次均不更高，答案合法。
- 题目保证答案唯一 ⇒ 同一频次层不会「半进半出」产生歧义，桶内全取即可。

## 复杂度分析
- 时间：O(n)，对比堆 O(n log k)、全排序 O(n log n)
- 空间：O(n)，桶数组 + 计数表

## 代码实现
\`\`\`go
func topKFrequent(nums []int, k int) []int {
    cnt := make(map[int]int)
    for _, x := range nums {
        cnt[x]++
    }
    buckets := make([][]int, len(nums)+1) // 下标即频次
    for x, c := range cnt {
        buckets[c] = append(buckets[c], x)
    }
    res := make([]int, 0, k)
    for f := len(nums); f >= 1 && len(res) < k; f-- { // 高频到低频扫描
        res = append(res, buckets[f]...)
    }
    return res
}
\`\`\`

## 边界与易错点
- 桶长必须 n+1（频次最大可取 n），开成 n 会越界。
- append(buckets[f]...) 可能让 res 超过 k 个；答案唯一时同频全取不影响，若要求恰好 k 个需截断。
- k 等于不同元素个数时返回全部，扫描到底自然正确。
- 堆法要用小顶堆（弹最小留 Top-K），用大顶堆是常见写反。

## 举一反三
- LC 692 前 K 个高频单词：频次 + 字典序双键比较，用堆或排序。
- LC 215 数组中的第 K 个最大元素：快速选择 O(n)，与桶排同属「不排序找 Top-K」。
- LC 451 根据字符出现频率排序：桶排全量输出。
- 识别信号：「Top-K + 频率/值域有界」→ 桶排；「Top-K + 流式/值域无界」→ 堆。

## 实际工程应用
桶排计次是实时排行榜的底层骨架：热榜统计先计数再按分桶出榜、监控系统 Top-N 慢查询聚合、CDN 热点 URL 统计——值域有界时桶排 O(n) 无对手，这正是 Redis 用计数器+分桶而非全排序做热榜的原因。`,
    keyPoints: [
      "频次值域 [1,n] ⇒ 桶数组下标即频次，建桶 O(m)、扫描 O(n)",
      "计数本身 Ω(n) ⇒ 桶排 O(n) 已是理论下界，优于堆 O(n log k)",
      "高频桶向低频扫，凑满 k 即止；答案唯一 ⇒ 同频截断无歧义",
      "堆版用小顶堆保留 Top-K，适合流式 / 值域无界场景",
    ],
    followUps: [
      "改用大小为 k 的小顶堆怎么写？什么场景堆比桶排更合适？",
      "要求结果按频次降序输出时，桶排要加什么处理？",
      "求第 K 大元素（非频次）的最优解是什么？（LC 215 快速选择）",
    ],
    favorited: false,
  },

  {
    id: "algo-448",
    nodeId: "p1-hash",
    question: `448. 找到所有数组中消失的数字（LeetCode 448）
题意：给定含 n 个整数的数组 nums，其中 nums[i] ∈ [1, n]，有些元素出现两次、有些零次。找出 [1,n] 范围内所有未出现的整数，要求 O(n) 时间 + O(1) 额外空间。
示例：输入 [4,3,2,7,8,2,3,1] → 输出 [5,6]（5 和 6 从未出现）。
约束：n == nums.length，1 ≤ n ≤ 10^5，1 ≤ nums[i] ≤ n。`,
    answer: `## 题意与约束分析
值域恰好 [1,n] 且槽位恰好 n 个 ⇒「下标即哈希」的完美前提。O(1) 空间排除哈希集合；还要不丢失原数据 ⇒ 用「取负」在原地打可逆标记（值域为正，负数是天然的空闲标记位）。

## 思路推导：从暴力到最优
暴力：哈希集合记录出现值再扫 [1,n]，O(n) 空间违规。排序后扫缺失：O(n log n) 且破坏原数组。
观察：值 x 出现 ⇔ 下标 x-1「被点亮」。数组值全为正 ⇒ 把 nums[x-1] 取负即点亮，原值不丢失（取绝对值还原）。
最优：一趟遍历，对每个 x 令 i=|x|-1，若 nums[i]>0 则取负；第二趟扫，nums[i] 仍为正 ⇒ i+1 从未出现。

## 正确性要点
- 标记与存在性一一对应：值 x 被处理过 ⇔ nums[x-1] 变负；x 未出现 ⇔ nums[x-1] 保持为正。
- 取负幂等性靠「仅在 >0 时取负」保证：重复值不会把负数翻回正数。
- 读 x 时必须用绝对值：该槽可能已被前面的值标记为负，但 |nums[i]| 仍是原始值信息。
- 除符号位外不修改任何信息 ⇒ 第二趟判定精确，O(1) 空间成立。

## 复杂度分析
- 时间：O(n)，两趟线性扫描
- 空间：O(1)（不计输出数组），对比哈希集合 O(n)

## 代码实现
\`\`\`go
func findDisappearedNumbers(nums []int) []int {
    for _, x := range nums {
        i := x
        if i < 0 {
            i = -i // 还原被标记过的值
        }
        i-- // 值 v 映射到下标 v-1
        if nums[i] > 0 {
            nums[i] = -nums[i] // 取负标记「值 i+1 出现过」
        }
    }
    res := []int{}
    for i, v := range nums {
        if v > 0 { // 未被标记 ⇒ 值 i+1 缺失
            res = append(res, i+1)
        }
    }
    return res
}
\`\`\`

## 边界与易错点
- 遍历中必须用 |x| 再 -1，直接用负的 x 会算出负下标 panic。
- 取负前必须判 >0：重复值二次标记会把负数翻回正，把「出现过」误判成「缺失」。
- 小用例验证：[1,1] → [2]、n=1 → []，检验下标映射。
- 输出数组不计入空间是题目惯例，面试要主动说明；同时要说明「算法修改了原数组」这一代价。

## 举一反三
- LC 41 缺失的第一个正数：交换归位版下标哈希。
- LC 442 数组中重复的数据：同取负标记，输出重复值而非缺失值。
- LC 645 错误的集合：找一个重复 + 一个缺失，同模板。
- 识别信号：「值域 [1,n] + O(1) 空间 + 存在性/重复统计」→ 下标原地标记。

## 实际工程应用
原地标记是嵌入式与内核开发的生存技巧：位图分配器用单比特表示内存页占用、布隆过滤器用位数组判存在——思想都是「数据自己承载元信息」，在内存寸土寸金的场景（如网卡 DMA 描述符环）省掉独立状态表。`,
    keyPoints: [
      "值域 [1,n] + n 个槽 ⇒ 下标即哈希：值 x 点亮下标 x-1",
      "取负是可逆标记：读数用 |x|，仅在 >0 时取负防重复值翻回",
      "第二趟仍为正的下标 i ⇒ i+1 从未出现",
      "O(1) 空间的代价是修改原数组，面试要主动说明",
    ],
    followUps: [
      "若数组只读不可改，O(1) 空间还能做吗？（只能退到排序或位图）",
      "改成找所有出现两次的数怎么写？（LC 442）",
      "与 LC 41 的交换归位法相比，取负标记的优劣各是什么？",
    ],
    favorited: false,
  },

  {
    id: "algo-242",
    nodeId: "p1-hash",
    question: `242. 有效的字母异位词（LeetCode 242）
题意：给定字符串 s 和 t，判断 t 是否为 s 的字母异位词（字符种类与计数完全相同，顺序不同）。
示例：输入 s = "anagram", t = "nagaram" → 输出 true；s = "rat", t = "car" → 输出 false。
约束：1 ≤ s.length, t.length ≤ 5×10^4，仅含小写字母；进阶：若含 Unicode 字符如何处理。`,
    answer: `## 题意与约束分析
判定两个字符多重集相等。仅 26 个小写字母 ⇒ 定长计数数组优于哈希 map（常数小、无冲突）。n ≤ 5×10^4，O(n) 一趟即可。

## 思路推导：从暴力到最优
暴力：两串各自排序后比较，O(n log n)，能过但非最优。
观察：异位 ⇔ 每个字母出现次数相等 ⇔ 计数向量相等。
最优：26 长计数数组，一趟遍历中 s 加 t 减（同步增减，一趟完成），最后检查数组是否全零。前置剪枝：长度不等直接 false。

## 正确性要点
- 充要性：同步增减后 cnt[c]=0 对所有 c 成立 ⇔ s、t 中 c 出现次数全等 ⇔ 异位。
- 长度前置检查：不等长必不异位，且保证同趟增减的下标对齐。
- 计数数组无哈希冲突问题，判定是精确的而非概率的。

## 复杂度分析
- 时间：O(n)，对比排序法 O(n log n)
- 空间：O(1)，26 长数组与输入规模无关

## 代码实现
\`\`\`go
func isAnagram(s string, t string) bool {
    if len(s) != len(t) {
        return false
    }
    var cnt [26]int
    for i := 0; i < len(s); i++ {
        cnt[s[i]-'a']++
        cnt[t[i]-'a']-- // 同步增减，一趟完成
    }
    for _, c := range cnt {
        if c != 0 {
            return false
        }
    }
    return true
}
\`\`\`

## 边界与易错点
- 长度不等必须最先返回 false，既是免费剪枝也保证下标对齐。
- 减成负数没关系，最终非零即 false，无需中途判负。
- Unicode 进阶：换 map[rune]int，计数数组不再适用，复杂度不变。
- 逐字节处理只对 ASCII 正确，含中文要先转 []rune。

## 举一反三
- LC 49 字母异位词分组：计数向量作 map key。
- LC 438 / 567：滑动窗口 + 计数比较。
- LC 383 赎金信：单向供给版计数（库存扣减）。
- 识别信号：「字符多重集相等/包含判定」→ 计数数组。

## 实际工程应用
计数向量比较是文本指纹的基础：搜索引擎对 query 做 term 频率向量比对判重、抄袭检测系统的词袋指纹（bag-of-words signature）、数据库校验两个 GROUP BY 结果集是否一致，都是「计数向量 ⇔ 多重集相等」的直接应用。`,
    keyPoints: [
      "异位 ⇔ 计数向量相等；一趟同步增减，终点全零即 true",
      "26 长数组 O(1) 空间，无哈希冲突，判定精确",
      "长度前置检查是免费剪枝，也保证同趟增减下标对齐",
      "Unicode 进阶换 map[rune]int，思路与复杂度不变",
    ],
    followUps: [
      "只用一个数组同时处理两串，为什么不会相互干扰？",
      "含 Unicode 字符时算法怎么改，复杂度会变吗？",
      "分组所有异位词时这个计数器如何复用为 key？（LC 49）",
    ],
    favorited: false,
  },

  {
    id: "algo-383",
    nodeId: "p1-hash",
    question: `383. 赎金信（LeetCode 383）
题意：给定 ransomNote 和 magazine 两个字符串，判断能否用 magazine 中的字符拼出 ransomNote（magazine 中每个字符只能使用一次）。
示例：输入 ransomNote = "aa", magazine = "aab" → 输出 true；magazine = "ab" → 输出 false（a 不够用）。
约束：1 ≤ ransomNote.length, magazine.length ≤ 10^5，仅含小写字母。`,
    answer: `## 题意与约束分析
单向供给关系：magazine 是库存，ransomNote 是需求，需求按多重集 ⊆ 库存。与 LC 242 的「相等」不同，这里只要「够用」⇒ 单向计数扣减。10^5 规模 O(m+n) 两趟即可。

## 思路推导：从暴力到最优
暴力：对 ransomNote 每个字符在 magazine 中查找并删除，O(n·m) 或借助排序 O(n log m)。
观察：字符种类仅 26 ⇒ 先盘点库存（计数数组），再逐个扣需求，任一字符库存透支（变负）即 false。
最优：一趟建库存 O(m)，一趟扣需求 O(n)，透支时提前返回。

## 正确性要点
- 充要性：ransomNote 可拼出 ⇔ 每个字符需求数 ≤ 库存数 ⇔ 扣减过程中不出现负库存。
- 「每字符最多用一次」由「每扣一次减一」精确建模。
- 提前返回不误判：某字符已透支时，后续任何操作都无法补回该字符的缺口。

## 复杂度分析
- 时间：O(m+n)，对比暴力 O(n·m)
- 空间：O(1)，26 长计数数组

## 代码实现
\`\`\`go
func canConstruct(ransomNote string, magazine string) bool {
    var cnt [26]int
    for i := 0; i < len(magazine); i++ {
        cnt[magazine[i]-'a']++ // 盘点库存
    }
    for i := 0; i < len(ransomNote); i++ {
        c := ransomNote[i] - 'a'
        cnt[c]--
        if cnt[c] < 0 { // 库存透支
            return false
        }
    }
    return true
}
\`\`\`

## 边界与易错点
- ransomNote 比 magazine 长必 false：可前置剪枝，不剪也能由负库存兜住。
- 扣到 0 是合法的（刚好用尽），< 0 才失败，边界别写反。
- 与 LC 242 混用「双向增减」会误判——这里是单向库存模型。
- Unicode 场景同样要换 map[rune]int。

## 举一反三
- LC 242 有效的字母异位词：双向相等版计数。
- LC 1160 拼写单词：多个单词对同一库存的批量扩展。
- LC 691 贴纸拼词：库存是多张贴纸的背包问题，难度跃升。
- 识别信号：「供给能否覆盖需求（多重集包含）」→ 库存计数 + 扣减。

## 实际工程应用
库存扣减模型是资源管理的最小原型：电商下单校验库存（每 SKU 计数、透支即超卖——工程上用 Redis DECR 判负实现）、配额系统对 API 调用计数扣减、编译器寄存器分配，本质都是本题的「计数盘点 + 透支检测」。`,
    keyPoints: [
      "单向供给模型：magazine 库存计数，ransomNote 逐个扣减",
      "库存变负即 false；扣到 0 合法（刚好用尽）",
      "O(m+n) 两趟，透支提前返回不误判",
      "与 LC 242 的「相等」不同，这里只要「够用」",
    ],
    followUps: [
      "magazine 是只读一遍的数据流、ransomNote 已知，流程怎么排？",
      "多个 ransomNote 对同一 magazine 批量判定怎么优化？",
      "升级成「贴纸可无限复用」会变成什么问题？（对比 LC 691）",
    ],
    favorited: false,
  },
  {
    id: "algo-205",
    nodeId: "p1-hash",
    question: `205. 同构字符串（LeetCode 205）
题意：给定字符串 s 和 t，判断 s 能否通过「字符一对一替换」变成 t：相同字符必须映射到同一字符，不同字符不得映射到同一字符（映射是双射）。
示例：s = "egg", t = "add" → true（e→a, g→d）；s = "foo", t = "bar" → false（o 要同时映到 a 和 r）；s = "paper", t = "title" → true。
约束：1 ≤ s.length ≤ 5·10^4，t.length == s.length，由任意 ASCII 字符组成。`,
    answer: `## 题意与约束分析
「同构」= 双射（一一对应）。很多人只建 s→t 单向表就交卷，栽在 s="ab"、t="aa"：a→a、b→a 两个不同源字符「各自都有映射」，但目标字符被抢占 ⇒ 必须再查 t→s 方向。长度 5·10^4、任意 ASCII ⇒ 用定长数组替 map 常数更小。

## 思路推导：从暴力到最优
暴力：对每对 (s[i], t[i])，回扫历史位置 j：s[j]==s[i] ⇔ t[j]==t[i] 必须恒成立，O(n²)。
正解一（双向映射）：s2t[c] 记 c 映到谁、t2s[c] 记谁映到 c，每步两次 O(1) 查询完成双向校验。
正解二（最近出现模式）：双射 ⟺ 对应字符的「上一次出现下标」模式完全一致。lastS/lastT 两数组，比较 lastS[s[i]] 与 lastT[t[i]]，不等即 false，否则同步更新。两表合一趟、常数最小，且天然双向——首推。

## 正确性要点
- 双向缺一再防不住：单向 s→t 只防「一对多」（一个源映两个目标），防不住「多对一」（两个源抢一个目标）；最近出现模式比较同时覆盖两者。
- 先查后写：查完再落表，保证表中永远只存已验证的一致映射。
- 长度相等是题目保证；不保证时先判长度出局。

## 复杂度分析
- 时间：O(n) 单趟
- 空间：O(σ)，σ 为字符集大小（ASCII 定长 256 ⇒ 常数空间）

## 代码实现
\`\`\`go
func isIsomorphic(s string, t string) bool {
    var lastS, lastT [256]int // 存「下标+1」，0 表示尚未出现
    for i := 0; i < len(s); i++ {
        a, b := s[i], t[i]
        if lastS[a] != lastT[b] { // 最近出现模式断裂 ⇒ 双射不成立
            return false
        }
        lastS[a] = i + 1
        lastT[b] = i + 1
    }
    return true
}
\`\`\`

## 边界与易错点
- 只建单向 map：s="ab"、t="aa" 误判 true——面试经典翻车点，务必主动举此例。
- 哨兵偏移 +1：数组零值兼作「未出现」，若字符值 0 合法（含 NUL 的输入），不加偏移会撞车；题面 ASCII 可打印字符时不加也对，但讲了加分。
- 比较必须发生在「更新前」，否则两表永远相等、恒 true。
- 用两张 map[byte]byte 也能过，但哈希常数大，且要分别处理「键不存在」与「值不匹配」两种情况，代码更绕。

## 举一反三
- LC 290 单词规律：字符级双射升级为单词级双射，同一模板。
- LC 890 查找和替换关键模式：多单词对同一 pattern 的批量同构匹配。
- 识别信号：「两个序列结构是否相同」→ 双射校验（双向映射 / 最近出现模式）。

## 实际工程应用
双射校验是「结构等价」的通用判定：协议迁移时校验新旧字段映射是否一一对应、分库分表后校验分片键映射无碰撞、i18n 模板校验两侧占位符集合一致（{name} 不能一边一个一边两个）。「最近出现模式」还用于流式检测「当前事件序列是否重演历史结构」，是行为风控的特征之一。`,
    keyPoints: [
      "同构 = 双射：单向映射只防一对多，不防多对一",
      "s=\"ab\",t=\"aa\" 是单向方案的经典反例，面试主动举",
      "「最近出现下标」两数组一比较，等价双向校验且常数最小",
      "哨兵存「下标+1」：规避字符值 0 与「未出现」撞车",
    ],
    followUps: [
      "字符集换成 Unicode 后数组方案怎么改？（map[rune]int 或先规范化）",
      "要求输出具体映射关系而非只判布尔时怎么改？",
      "判断 n 个字符串两两同构如何降复杂度？（每串算规范形 key 再分组）",
    ],
    favorited: false,
  },
  {
    id: "algo-290",
    nodeId: "p1-hash",
    question: `290. 单词规律（LeetCode 290）
题意：给定规律字符串 pattern（小写字母）和按空格分隔的字符串 s，判断 s 是否遵循该规律：pattern 中每个字母双射到 s 中一个唯一非空单词。
示例：pattern = "abba", s = "dog cat cat dog" → true；pattern = "abba", s = "dog cat cat fish" → false；pattern = "aaaa", s = "dog cat cat dog" → false。
约束：1 ≤ pattern.length ≤ 300，s 只含小写字母和空格，无前导/尾随空格，相邻单词恰一个空格。`,
    answer: `## 题意与约束分析
LC 205 的「单词版」：映射单元从字符换成单词。两处新坑：① s 要先分词，且 len(words) 必须等于 len(pattern)——"ab" 对 "dog dog dog" 这类词数不符是独立失败路径；② 单词是字符串，数组下标法用不了，只能上 map。双射校验逻辑与 205 完全同构。

## 思路推导：从暴力到最优
暴力：对每个 i 回扫历史 j：pattern[j]==pattern[i] ⇔ words[j]==words[i]，O(n²·L)，L 为平均词长。
正解：两张哈希表——p2w 存「字母→单词」、w2p 存「单词→字母」。逐位双向查：任一边「已映射且不一致」即 false。strings.Split 一次分词，总复杂度 O(|s|)。
执行顺序：先判长度相等再进循环，既挡掉独立失败路径，又让循环内无需越界保护。

## 正确性要点
- 双射两方向各拦一类错误：p2w 冲突 = 一个字母对两个单词（一对多）；w2p 冲突 = 两个字母抢一个单词（多对一，"ab"→"dog dog"：i=1 时 p2w[b] 未注册能通过，但 w2p["dog"]='a'≠'b' 拦下——正是 205 中 "ab"/"aa" 的翻版）。
- 「查完双向再双写」保证映射表始终一致，不会把未验证的映射落库。
- 长度相等是词级对齐前提，先验。

## 复杂度分析
- 时间：O(|s|)，分词 + 一趟双查
- 空间：O(n·L)，words 数组 + 两张映射表

## 代码实现
\`\`\`go
func wordPattern(pattern string, s string) bool {
    words := strings.Split(s, " ")
    if len(words) != len(pattern) { // 词数 ≠ 模式长度，直接出局
        return false
    }
    p2w := make(map[byte]string, len(pattern))
    w2p := make(map[string]byte, len(pattern))
    for i := 0; i < len(pattern); i++ {
        c, w := pattern[i], words[i]
        if v, ok := p2w[c]; ok && v != w { // 一对多
            return false
        }
        if v, ok := w2p[w]; ok && v != c { // 多对一
            return false
        }
        p2w[c], w2p[w] = w, c
    }
    return true
}
\`\`\`

## 边界与易错点
- 忘记判 len(words) != len(pattern)：循环内直接 panic 或漏判。
- 空串陷阱：strings.Split("", " ") 返回 [""]（长度 1），不是空切片——本题保证 s 非空，面试可主动指出此坑展示语言功底。
- 连续空格/前后空格：题面保证干净输入；真实数据用 strings.Fields 容错（Fields 把 "a  b" 也切成两词，Split 会产出空词）。
- 只建单向 map：与 205 同坑，"ab"→"dog dog" 误判 true。

## 举一反三
- LC 205 同构字符串：字符级原型，两题对照记忆。
- LC 291 单词规律 II（Hard）：s 无分隔符，需回溯枚举切分点——「判定」升级为「搜索」。
- 识别信号：「模式串 vs 实例序列的一致性」→ 双射两张表；「要找出怎么对应」→ 回溯。

## 实际工程应用
「模式-实例双射校验」遍地都是：日志 grok 解析校验 %IP %METHOD %METHOD 重复槽位必须同值、URL 路由反校验 /user/:id/order/:id 两个 :id 必须解析出同值、数据脱敏保证同一敏感词全文映射为同一掩码。Split 与 Fields 的语义差异也是 Go 工程面试高频追问点。`,
    keyPoints: [
      "205 单词版：p2w 与 w2p 两张表缺一不可，各拦一类冲突",
      "先判词数与模式长度相等，再进逐词校验",
      "「查完双向再双写」保证映射表一致性",
      "Split 与 Fields 语义差异：空串 / 连续空格场景不同",
    ],
    followUps: [
      "若 s 没有空格分隔（LC 291），为什么必须回溯？状态怎么设计？",
      "要求返回映射表而非布尔值时代码怎么改？",
      "同一 pattern 高频重复查询，如何预处理加速？（缓存 pattern 规范形）",
    ],
    favorited: false,
  },
  {
    id: "algo-36",
    nodeId: "p1-hash",
    question: `36. 有效的数独（LeetCode 36）
题意：判断一个 9×9 数独棋盘（未填满，'.' 表示空格）当前状态是否有效：每行、每列、每个 3×3 宫内数字 1-9 均不重复。注意只需校验当前状态，不要求数独可解。
示例：略（经典题面，含一个有效盘与一个宫内重复的无效盘）。
约束：board 恒为 9×9，单元格字符为 '1'-'9' 或 '.'。`,
    answer: `## 题意与约束分析
棋盘恒 9×9 ⇒ 复杂度无所谓，考点在「一次遍历完成行/列/宫三维查重」的编码手法与宫编号推导。有效 ≠ 可解：只查重、不做回溯求解，别顺手写成 LC 37。

## 思路推导：从暴力到最优
暴力：三遍扫描——先验 9 行、再 9 列、再 9 宫，各开集合，代码冗长且 3×81 次检查。
正解：一趟遍历。每个非空格 (i,j) 同时向「第 i 行」「第 j 列」「第 k 宫」三张表注册，任一撞车即 false。宫编号 k = (i/3)*3 + j/3：i/3、j/3 把坐标折叠到宫的 3×3 网格，再行优先展开成 0-8。
位图版：数字仅 1-9 ⇒ 用 9 位 bitmask 替集合，rows[i]、cols[j]、boxes[k] 各为一个 int，按位或注册、按位与查重，27 个 int 搞定，常数碾压哈希。

## 正确性要点
- 对同一格，三个维度全部「查完再写」：先写后查会拿自己当重复。
- 宫编号公式：(i/3) 是宫行、(j/3) 是宫列，行优先展开 ⇒ 9 宫两两不串号；拿 (0,0)~(8,8) 九点手算可验证覆盖 0-8。
- '.' 跳过，不参与任何注册。

## 复杂度分析
- 时间：O(81) = O(1)；泛化 n×n 为 O(n²)
- 空间：O(27) = O(1)；泛化 O(n)

## 代码实现
\`\`\`go
func isValidSudoku(board [][]byte) bool {
    var rows, cols, boxes [9]int // 位图：第 d 位为 1 表示数字 d+1 已出现
    for i := 0; i < 9; i++ {
        for j := 0; j < 9; j++ {
            c := board[i][j]
            if c == '.' {
                continue
            }
            bit := 1 << (c - '1')      // 数字 → 位
            k := (i/3)*3 + j/3         // 宫编号
            if rows[i]&bit != 0 || cols[j]&bit != 0 || boxes[k]&bit != 0 {
                return false // 三维度任一撞车
            }
            rows[i] |= bit
            cols[j] |= bit
            boxes[k] |= bit
        }
    }
    return true
}
\`\`\`

## 边界与易错点
- 宫编号写成 (i/3) + (j/3)*3：结果是转置镜像，9 宫依然两两不串号，正确性不受影响——面试别在公式上卡壳，现场推导一遍即可。
- 位偏移用 c-'1'（得 0-8）而非 c-'0'（得 1-9）：后者多占一位，虽不错但浪费。
- 三个维度拆成「查一行写一行」再查下一维：恰好不错，但若把查/写混在各自 if 里极易引入「同格自撞」bug，统一「查完再写」最稳。
- 题目只判当前有效性，别画蛇添足做可解性回溯。

## 举一反三
- LC 37 解数独：回溯 + 本题三维位图做 O(1) 合法性检查，经典组合拳。
- LC 2133 检查是否每一行每一列都包含全部整数：行列两维的简化版。
- 识别信号：「多维度同时不重复」→ 每维度一张注册表，一趟遍历。

## 实际工程应用
「多维度注册查重」是约束校验引擎的最小内核：排课系统同一时段教师/教室/班级三维互不冲突、航空排座同航班座位/旅客/舱位多键唯一、数据库联合唯一索引的插入前预检，都是本题的注册模式。位图压缩还常见于限流器标记与布隆过滤器的手写实现。`,
    keyPoints: [
      "一趟遍历三维注册：行、列、宫各一张表，查完再写",
      "宫编号 k=(i/3)*3+j/3：坐标折叠到 3×3 宫格再行优先展开",
      "数字 1-9 ⇒ 9 位位图替代集合，27 个 int 常数最小",
      "有效 ≠ 可解：只查重，不做回溯",
    ],
    followUps: [
      "升级为求解数独（LC 37）时，位图怎么复用进回溯框架？",
      "棋盘泛化为 n×n（n = k²）时代码要动哪几处？",
      "要求输出「哪里冲突」的诊断信息而非布尔值，怎么改？",
    ],
    favorited: false,
  },
  {
    id: "algo-219",
    nodeId: "p1-hash",
    question: `219. 存在重复元素 II（LeetCode 219）
题意：给定整数数组 nums 和整数 k，判断是否存在两个不同下标 i、j，满足 nums[i] == nums[j] 且 |i - j| ≤ k。
示例：nums = [1,2,3,1], k = 3 → true（下标 0 与 3）；nums = [1,2,3,1,2,3], k = 2 → false（相同值距离均 ≥ 3）。
约束：1 ≤ nums.length ≤ 10^5，0 ≤ k ≤ 10^5，-10^9 ≤ nums[i] ≤ 10^9。`,
    answer: `## 题意与约束分析
「等值 + 下标距离窗」双约束。n、k 均达 10^5 ⇒ O(nk) 暴力出局。两种 O(n) 视角：①「每个值只记最近一次出现」的最近下标表；②「窗口内不许同值」的大小-k 滑动集合。两者一体两面，面试要求能互推。

## 思路推导：从暴力到最优
暴力：固定 i 向后扫至多 k 格找等值，O(nk)，n=10^5 时 10^10 出局。
最近下标表：遍历到 i 时查 last[x]（x 上次出现的下标），存在且 i-last[x] ≤ k 即命中；随后无条件更新 last[x]=i。
为什么只看「最近一次」就够：对同值 x 的任意更老下标 h < last[x]，必有 i-h > i-last[x]——最近的都超窗，更老的更超。只留最近下标是无损压缩。
滑动集合版：维护窗口 (i-k, i] 的元素集合，x 已在集合即命中；否则加入 x 并驱逐滑出元素 nums[i-k]。集合大小恒 ≤ k，k ≪ n 时空间更优。

## 正确性要点
- 最近下标无损性：如上推导，同值只留最近不丢解——这是本题的核心论证。
- 判定在更新之前：先用旧 last[x] 判距离，再覆盖为新下标；顺序反了会用自己跟自己比（i-i=0 ≤ k 恒 true）。
- 集合版驱逐的是滑出者 nums[i-k]，不是 x 的旧值，别写混。

## 复杂度分析
- 时间：O(n) 单趟
- 空间：最近下标版 O(min(n, 值域))；集合版 O(min(n, k))

## 代码实现
\`\`\`go
func containsNearbyDuplicate(nums []int, k int) bool {
    last := make(map[int]int, len(nums)) // 值 → 最近出现下标
    for i, x := range nums {
        if j, ok := last[x]; ok && i-j <= k { // 上次出现仍在窗内
            return true
        }
        last[x] = i // 无条件刷新：只有最近的才配参与后续判定
    }
    return false
}
\`\`\`

## 边界与易错点
- k=0：不同下标距离至少 1 > 0，必 false；代码由 i-j≥1 自然兜住，无须特判。
- 距离条件是 ≤ k 而非 < k，对齐题面 |i-j| ≤ k。
- last[x] 更新不能加「仅未命中才更新」的多余分支——无条件覆盖才是「最近」语义。
- 集合版窗口大小：含当前元素时最多 k+1 个，驱逐时机错一格就会误判距离恰 k+1 的对子。

## 举一反三
- LC 217 存在重复元素：k=∞ 的退化版，一个集合查重即可。
- LC 220 存在重复元素 III：追加 |nums[i]-nums[j]| ≤ t 值距约束，需有序集合或桶化，难度跃升。
- 识别信号：「同值 + 下标窗约束」→ 最近下标表；「窗口内聚合性质」→ 滑动集合。

## 实际工程应用
最近下标表就是「事件去抖」的算法内核：监控告警相同 fingerprint 在 k 秒内只报一次、日志系统对同错误码滑动窗口去重、反作弊检测「同一设备 k 次请求内重复提交」。「只保留最近一次」的无损压缩思想，也是流处理 state 瘦身与 TTL 去重缓存的通用手法。`,
    keyPoints: [
      "同值只保留最近下标：更老的必更远，无损压缩",
      "先判距离再覆盖更新，顺序即正确性（否则自己比自己）",
      "距离条件 ≤ k；k=0 由 i-j≥1 自然兜住",
      "滑动集合版与最近下标版一体两面，k≪n 时集合版省空间",
    ],
    followUps: [
      "升级为值距约束 |nums[i]-nums[j]| ≤ t（LC 220）为什么裸哈希不够了？",
      "数据是无限流且 k 很大时，两种方案的空间各是什么？",
      "要求返回所有命中对 (i,j) 时，最近下标法还够用吗？（不够，需存全量下标列表）",
    ],
    favorited: false,
  },
  {
    id: "algo-202",
    nodeId: "p1-hash",
    question: `202. 快乐数（LeetCode 202）
题意：定义变换：将正整数替换为它每个数位上数字的平方和。重复该过程，若最终等于 1 则是快乐数；若进入不含 1 的循环则不是。判断 n 是否为快乐数。
示例：n = 19 → 1²+9²=82 → 8²+2²=68 → 6²+8²=100 → 1²+0+0=1，返回 true。
约束：1 ≤ n ≤ 2^31 - 1。`,
    answer: `## 题意与约束分析
表面是数学题，实质是「纯函数迭代轨道是否入环、入哪个环」的判定。关键数学事实：轨道不会发散——d 位数下一步 ≤ 81d，三位以上必严格缩小 ⇒ 值必落入有界集合 ⇒ 鸽巢原理保证必入环。于是问题等价于：环是 {1}（快乐）还是别的（不快乐）。

## 思路推导：从暴力到最优
暴力直觉：无限迭代看会不会到 1——不快乐时永不终止，直接出局。
哈希集合：每步把值存入 seen，新值已在 seen 即发现环 ⇒ 判环值是否 1。空间 O(轨道长)。
Floyd 快慢指针：把迭代函数看作隐式链表的 next 指针，判环零空间。slow 一步、fast 两步，相遇即入环。
为什么本题不用找环入口：只问「环是不是 {1}」，而 1 是不动点（1→1），进入 {1} 循环后 fast 会精确停在 1 ⇒ 相遇值/终止值是否 1 即答案，省掉第二阶段。

## 正确性要点
- 有界性论证（81d 上界）是「必入环」的依据，也是 Floyd 适用前提，面试要主动讲。
- 终止条件：fast==1 ⇒ true；slow==fast ⇒ 环，答案为「环值是否 1」，即 fast==1。
- fast 必须先走一步初始化（或用 do-while 语义）：slow=fast=n 的「假相遇」会让循环体零执行直接判环——n=7 这类快乐数会被误判 false。

## 复杂度分析
- 时间：O(log n) 进入有界区 + O(环长) 相遇，总体远低于朴素模拟
- 空间：Floyd O(1)；哈希版 O(轨道长)

## 代码实现
\`\`\`go
func isHappy(n int) bool {
    step := func(x int) int {
        sum := 0
        for x > 0 {
            d := x % 10
            sum += d * d
            x /= 10
        }
        return sum
    }
    slow, fast := n, step(n) // fast 先走一步，规避初始假相遇
    for fast != 1 && slow != fast {
        slow = step(slow)       // 慢者一步
        fast = step(step(fast)) // 快者两步
    }
    return fast == 1
}
\`\`\`

## 边界与易错点
- 循环条件位序：是 fast != 1 && slow != fast（&&），写成 || 会错过终止。
- fast 不先走一步：slow==fast=n 立即退出，返回 n==1——n=7（快乐数）误判 false，务必注意。
- 误以为要先找环入口：本题环值即判据，不需要 LC 142 的第二阶段。
- 哈希版：集合先存起点 n 再迭代，语义最清晰；漏存虽不丢环（下一圈仍检到），但逻辑要讲圆。

## 举一反三
- LC 141 / LC 142 环形链表：Floyd 判环与找环入口的原型题。
- LC 287 寻找重复数：「下标跳跃」即 next 指针的隐式链表，同宗判环。
- 识别信号：「纯函数无限迭代求定点/环」→ 哈希存轨道，或 Floyd 要 O(1) 空间。

## 实际工程应用
「迭代轨道判环」是状态机健康检查的原型：工作流引擎检测流程定义是否存在死循环迁移、爬虫检测 URL 规范化后的跳转环（A→B→A）、分布式锁续期失败的死锁探测。Floyd 双速率探测「零额外内存在线检环」的特性，在嵌入式与内核链表巡检中仍是标准手法。`,
    keyPoints: [
      "81d 上界 ⇒ 轨道有界 ⇒ 必入环，问题转判环",
      "Floyd 快慢指针 O(1) 空间；终止值是否 1 即答案",
      "fast 必须先走一步：规避 slow==fast 的初始假相遇",
      "本题不用找环入口——环上的值本身就是判据",
    ],
    followUps: [
      "证明：为什么三位以上的数迭代后必然严格减小？",
      "要求输出入环前步数（环入口）时，Floyd 第二阶段怎么写？",
      "换成「立方和」等其他变换算法还成立吗？（成立，只要轨道有界）",
    ],
    favorited: false,
  },
  {
    id: "algo-349",
    nodeId: "p1-hash",
    question: `349. 两个数组的交集（LeetCode 349）
题意：给定两个整数数组 nums1、nums2，返回它们的交集。结果中每个元素必须唯一，输出顺序不限。
示例：nums1 = [1,2,2,1], nums2 = [2,2] → [2]；nums1 = [4,9,5], nums2 = [9,4,9,8,4] → [9,4]（或 [4,9]）。
约束：1 ≤ nums1.length, nums2.length ≤ 1000，0 ≤ nums1[i], nums2[i] ≤ 1000。`,
    answer: `## 题意与约束分析
「交集 + 去重」。约束极小（值域 0-1000）时位图/数组都能过，但面试要给不依赖值域的通用哈希解，并主动展开两条进阶线：① 数组已有序时双指针 O(1) 额外空间；② 结果保留重复次数（LC 350）的计数版。

## 思路推导：从暴力到最优
暴力：对 nums1 每个元素扫 nums2 查存在，O(m·n)，结果再扫重，整体 O(m·n) 级。
哈希集合法：把较短数组装入集合 seen（选短的是纯空间优化），遍历另一数组，命中 ⇒ 写入结果并从 seen 删除——「命中即删」让每个交集值至多进结果一次，天然去重，无需第二个集合。
有序版：两数组先排序，双指针对撞——相等取值、两侧各自跳过重复；不等移较小者。时间 O(m log m + n log n)，原地排序时额外空间 O(1)。
流式视角：若 nums2 是只能读一遍的流，「小表建哈希 + 流式探测」结构依然成立——这是 hash join 的考点。

## 正确性要点
- 去重两种载体：结果集查重，或「命中即删」使 seen 中每个值至多命中一次。
- 短数组建集合不影响正确性，纯优化。
- 双指针版取值后两侧必须同时跳过各自的重复段，否则结果重复或死循环。

## 复杂度分析
- 哈希版：时间 O(m+n)，空间 O(min(m,n))
- 排序双指针版：时间 O(m log m + n log n)，额外空间 O(1)

## 代码实现
\`\`\`go
func intersection(nums1 []int, nums2 []int) []int {
    if len(nums1) > len(nums2) { // 保证用短数组建集合
        nums1, nums2 = nums2, nums1
    }
    seen := make(map[int]struct{}, len(nums1))
    for _, x := range nums1 {
        seen[x] = struct{}{}
    }
    var res []int
    for _, x := range nums2 {
        if _, ok := seen[x]; ok {
            res = append(res, x)
            delete(seen, x) // 命中即删：每个交集值只进一次结果
        }
    }
    return res
}
\`\`\`

## 边界与易错点
- 结果去重：写成「命中即 append」却不删 seen ⇒ [2,2] 类重复，错。
- 交换 nums1/nums2 只换切片头引用，逻辑安全；但交换后别再按旧印象用长度。
- 值域 0-1000 是烟雾弹：可开 [1001]bool 位图秒杀，但面试先给通用哈希，再提位图作约束特化，层次更清晰。
- 双指针版「相等取值后只推一侧」：会重复取值，必须两侧推进并跳过重复。

## 举一反三
- LC 350 两个数组的交集 II：结果保留最小频次 ⇒ 存在性集合升级为计数表。
- 对撞双指针框架：与 LC 88 合并有序数组、LC 167 有序两数和共享。
- 识别信号：「两集合共同元素」→ 小集合建表 + 大集合流式探测；「保留次数」→ 计数表。

## 实际工程应用
「小表建哈希 + 大表流式探测」正是数据库 hash join 的内核（build 侧永远选小表），也是推荐系统粗排「用户兴趣集 ∩ 物品标签集」的在线实现。命中即删的去重技巧对应 SQL 的 INTERSECT（去重交），LC 350 对应 INTERSECT ALL（保次交）——面试点出这层对应能直接展示工程视野。`,
    keyPoints: [
      "短数组建集合：O(min(m,n)) 空间，天然适配流式长数组",
      "「命中即删」一行搞定结果去重，无需第二个集合",
      "有序时对撞双指针省空间，与 LC 88 / LC 167 同框架",
      "保留次数用计数表 ⇒ LC 350，即 INTERSECT vs INTERSECT ALL",
    ],
    followUps: [
      "若 nums2 是只能读一遍的无限流，算法与空间怎么调整？",
      "要求交集结果有序输出时，哪种方案总代价最低？",
      "三数组交集怎么扩展？还能 O(m+n+p) 吗？",
    ],
    favorited: false,
  },
];