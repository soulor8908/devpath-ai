// lib/presets/algorithm-200/questions/p1-stack-queue.ts
// 栈与队列模块深度题解（12 题）：括号匹配、最小栈、栈队互实现、表达式求值、
// 路径简化、字符串解码、单调栈家族（每日温度/下一个更大元素/股票跨度/最大矩形）、单调队列。
// 设计思路：每题答案固定 8 段，单调栈部分特别强调「出栈时刻 = 答案确定时刻」这一不变式，
// 以及均摊复杂度论证——这是面试官区分背题与理解的真正分水岭。

import type { Question } from "../../../types";

export const P1_STACK_QUEUE_QUESTIONS: Question[] = [
  {
    id: "algo-20",
    nodeId: "p1-stack-queue",
    question: `1. 有效的括号（LeetCode 20）
题意：给定只包含 ()[]{} 六种字符的字符串，判断括号是否有效：左括号必须被同类型右括号闭合，且闭合顺序正确。
示例：输入 "()[]{}" → 输出 true；输入 "([)]" → 输出 false（交叉嵌套非法）。
约束：字符串长度 1 ~ 10^4，只含括号字符。`,
    answer: `## 题意与约束分析
括号合法性判定。合法括号串的本质是「最近未闭合的左括号必须先闭合」——这正是后进先出。长度 10^4 意味着 O(n) 一遍扫描轻松达标，关键是把「同类型匹配」和「顺序正确」两个条件都映射到栈操作上。

## 思路推导：从暴力到最优
暴力思路是反复消去相邻的匹配对（类似消消乐），每轮扫一遍找相邻对，最坏 O(n^2)。观察合法串的结构性质：扫描过程中，任何时刻等待匹配的左括号里，最后出现的那个必须最先被右括号闭合——LIFO 结构呼之欲出。于是用栈：遇左括号入栈；遇右括号时查栈顶是否为同类型左括号，是则弹出（完成一对匹配），否则非法。全部扫完栈空才合法。查表技巧：用 map 存「右括号 → 左括号」，读到字符先查表，查到说明是右括号，查不到说明是左括号，一个表同时完成分类与匹配检查。

## 正确性要点
- 不变式：栈中自底向上保存「尚未闭合的左括号，按出现顺序排列」；每处理一个右括号，若串仍可能合法，则它必须匹配栈顶（任何其他选择都会造成交叉嵌套，违反顺序性）。
- 失败条件完备性：非法串只有三种——右括号时栈空（无左可配）、栈顶类型不符（类型错配）、扫完栈非空（有左未配）。代码对三种都拦截。
- 终止：扫描有穷，循环必止。

## 复杂度分析
- 时间：O(n) —— 每字符一次入栈或出栈，对比暴力消对的 O(n^2)
- 空间：O(n) —— 最坏全是左括号

## 代码实现
\`\`\`go
func isValid(s string) bool {
	pair := map[byte]byte{')': '(', ']': '[', '}': '{'} // 右括号 -> 对应左括号
	stack := []byte{}
	for i := 0; i < len(s); i++ {
		c := s[i]
		if left, isRight := pair[c]; isRight {
			// 右括号：栈空或栈顶不匹配都是非法
			if len(stack) == 0 || stack[len(stack)-1] != left {
				return false
			}
			stack = stack[:len(stack)-1] // 匹配成功，弹出
		} else {
			stack = append(stack, c) // 左括号入栈等待匹配
		}
	}
	return len(stack) == 0 // 有左未配则非法
}
\`\`\`

## 边界与易错点
- 奇数长度：合法串长度必为偶数，可提前剪枝，但不剪也不影响正确性。
- 以右括号开头：第一次查到右括号时栈空，立即 false。
- 扫完忘判栈空：输入 "(((" 会错返回 true。
- 用「左括号 → 右括号」的映射方向：逻辑也能写但分类判断更绕；右→左方向配合「查得到即右括号」最简洁。

## 举一反三
- LeetCode 22 括号生成：回溯生成 + 本题做校验函数，或直接用左右括号余额剪枝。
- LeetCode 32 最长有效括号：栈存下标而非字符，栈底放哨兵下标计算长度。
- LeetCode 1249 移除无效的括号：两遍扫描（或栈记下标）找出多余括号。
- LeetCode 921 使括号有效的最少添加：不用栈，两个计数器即可，是本题的「计数器化」。
- 识别信号：「嵌套结构合法性 / 最近未闭合项」→ 栈；只需计数不需位置时可退化为计数器。

## 实际工程应用
括号匹配是所有解析器的第一课：JSON/XML 解析器的标签配对校验、编译器词法分析后表达式括号匹配、编辑器的光标括号高亮配对，底层都是这个栈模型。IDE 中「跳转到匹配括号」功能用的就是同一下标栈。HTML 模板引擎对未闭合标签的报错定位，也是本题思想的直接延伸。`,
    keyPoints: [
      "合法括号串 ⇔ 最近未闭合左括号最先闭合，LIFO 是判据本身，栈不是技巧而是结构",
      "右→左映射表一举两得：查得到即为右括号并完成匹配检查，省去 if-else 分类",
      "三种非法情形（空栈遇右、类型错配、扫完栈非空）必须全部拦截，漏一个就 WA",
    ],
    followUps: [
      "只含一种括号时能不用栈吗？（能，一个余额计数器即可：遇左 +1 遇右 -1，负值或结尾非零即非法）",
      "如何找出最长的合法括号子串？（LeetCode 32：栈存下标，栈底放一个 -1 哨兵用于算长度）",
      "并发解析超大文件时分块扫描，括号匹配还能做吗？（能但麻烦：块间需传递未闭合栈，本质是增量解析问题）",
    ],
    favorited: false,
  },
  {
    id: "algo-155",
    nodeId: "p1-stack-queue",
    question: `2. 最小栈（LeetCode 155）
题意：设计一个栈，支持 Push、Pop、Top 以及在 O(1) 时间内检索栈中最小元素的 GetMin 操作。
示例：Push(-2)、Push(0)、Push(-3)、GetMin() → -3；Pop() 后 Top() → 0，GetMin() → -2（最小值随弹出动态变化）。
约束：最多 3×10^4 次操作；Pop/Top/GetMin 只在栈非空时调用。`,
    answer: `## 题意与约束分析
给栈追加一个 O(1) 的 GetMin。难点在于最小值是「动态集合」的最值：Pop 可能把当前最小值弹走，次小值要立刻补位。这排除了「记一个全局最小值变量」的懒办法——变量无法回溯历史状态。

## 思路推导：从暴力到最优
暴力：GetMin 时遍历整个栈找最小，O(n) 每次，3×10^4 次操作最坏 10^9，超时。观察：栈的状态演变是「后进先出」的，任意时刻的最小值只取决于当前栈内容；更精细地说，每压入一个元素 x，「此刻栈内最小值」就从旧最小值变为 min(旧最小值, x)。这个「此刻最小值」本身也随 Push/Pop 同步演变——它也是一个栈！于是维护辅助栈 mins，与主栈等长同步操作：Push 时压入 min(x, mins 栈顶)，Pop 时同步弹出。GetMin 就是 mins 栈顶，O(1)。另一种省空间变体：辅助栈只在「压入值 ≤ 当前最小」时同步压入，Pop 时若弹出值等于辅助栈顶才同步弹出——最小值重复压入的次数等于「刷新最小值的次数」，最坏仍 O(n) 但平均更省。

## 正确性要点
- 同步栈不变式：mins 的栈顶恒等于 data 当前全部元素的最小值。归纳证明：初始空栈成立；Push 时压入 min(x, 旧最小) 恰为新集合最小；Pop 时两栈同步弹出，栈顶恢复到「不包含被弹元素时的最小值」——因为 mins 的历史层完整保存了过去的所有最小值状态。
- 这就是关键洞察：辅助栈存的不是一个值，而是「最小值的全部历史快照」，所以回溯免费。

## 复杂度分析
- 时间：四个操作全部 O(1) —— 对比暴力 GetMin 的 O(n)
- 空间：O(n) —— 辅助栈与主栈等长

## 代码实现
\`\`\`go
type MinStack struct {
	data []int // 主栈
	mins []int // 辅助栈：与主栈同步，栈顶恒为当前最小值
}

func Constructor() MinStack {
	return MinStack{}
}

func (s *MinStack) Push(val int) {
	s.data = append(s.data, val)
	m := val
	if len(s.mins) > 0 && s.mins[len(s.mins)-1] < val {
		m = s.mins[len(s.mins)-1] // 此刻最小值 = min(新值, 旧最小)
	}
	s.mins = append(s.mins, m)
}

func (s *MinStack) Pop() {
	s.data = s.data[:len(s.data)-1]
	s.mins = s.mins[:len(s.mins)-1] // 同步弹出，历史最小值自动回溯
}

func (s *MinStack) Top() int {
	return s.data[len(s.data)-1]
}

func (s *MinStack) GetMin() int {
	return s.mins[len(s.mins)-1] // 栈顶即当前最小值
}
\`\`\`

## 边界与易错点
- 首次 Push 时辅助栈空：比较前必须判空，否则越界。
- 省空间变体忘记等号：Push 条件写成 < 而非 ≤ 时，重复压入相同最小值只记一次，Pop 其中一个就错误地弹出辅助栈顶，最小值丢失。
- Pop 只弹主栈忘弹辅助栈：最小值历史与主栈错位，后续 GetMin 全错。
- 题目保证非空调用，但工程实现中 GetMin 应返回错误或 panic 语义明确化。

## 举一反三
- LeetCode 239 滑动窗口最大值：「动态集合求最值」的进阶，窗口滑动使两端都变化，用单调队列。
- LeetCode 895 最大频率栈：同样给栈附加维度信息，用频率分组栈。
- LeetCode 901 股票价格跨度：单调栈存压缩后的历史，思想与本题「存历史快照」同源。
- 识别信号：「栈 + 附加 O(1) 聚合查询」→ 同步辅助栈存该维度的历史快照。

## 实际工程应用
「同步辅助栈存历史快照」是监控系统里滚动窗口极值计算的极简模型：行情系统维护最近 N 笔价格的最高/最低价，撤单（Pop）后极值要能立即回溯，用的就是同步极值栈结构。撤销-重做系统（undo/redo）中「随操作同步演变的派生状态」也普遍用这个模式——主栈存操作，辅助栈存对应状态下的聚合值。`,
    keyPoints: [
      "辅助栈存的是「最小值的全部历史快照」，Pop 后自动回溯到上一时刻的最小值，这是 O(1) 的根源",
      "Push 压入 min(val, 旧最小) 而非 val 本身——辅助栈与主栈等长同步是正确性前提",
      "省空间变体的等号纪律：≤ 压入、相等才弹出，漏掉等号会在重复最小值场景出错",
    ],
    followUps: [
      "要求同时支持 GetMax 呢？（再加一个同步 maxs 栈，空间翻倍仍 O(n)）",
      "单栈存差值的做法听说过吗？（栈存与当前最小值的差，最小值单独记；省一个栈但实现易错，溢出要当心）",
      "如果操作序列里有并发 Push/Pop，同步栈还安全吗？（不安全，两栈更新不是原子的，需要整体加锁）",
    ],
    favorited: false,
  },
  {
    id: "algo-232",
    nodeId: "p1-stack-queue",
    question: `3. 用栈实现队列（LeetCode 232）
题意：仅用两个栈（只能 Push/Pop/Peek/IsEmpty）实现 FIFO 队列：Push、Pop、Peek、Empty。
示例：Push(1)、Push(2)、Peek() → 1、Pop() → 1、Empty() → false（先进先出，1 先出）。
约束：最多 100 次操作，元素 1 ~ 9；进阶要求 Pop/Peek 均摊 O(1)。`,
    answer: `## 题意与约束分析
栈是 LIFO，队列是 FIFO，单个栈无法逆转顺序，但两次反转等于不变——LIFO 套 LIFO 就是 FIFO。核心考点是「均摊 O(1)」的论证，而不是单次操作的最坏时间。

## 思路推导：从暴力到最优
方案一（每次 Pop 都倒腾）：Pop 时把主栈全部倒入辅助栈、取底、再倒回，Pop 单次 O(n)，太低效——每个元素被反复搬运，n 次操作总代价 O(n^2)。方案二（双栈分工）：维护 in 栈和 out 栈，Push 永远进 in；Pop/Peek 从 out 取——out 非空直接取栈顶（它是最早进入的元素），out 为空时才把 in 的全部元素一次性倒入 out（顺序恰好反转，in 的栈底变 out 的栈顶，即队头）。关键洞察：「倒栈」这个动作只在 out 空时发生，且每个元素一生只被倒一次，搬运总次数被元素个数钉死，不会被操作次数放大。

## 正确性要点
- FIFO 正确性：in 栈中元素按到达顺序自栈底向栈顶排列；倒入 out 后顺序整体反转，最早到达的在 out 栈顶；out 未空前新元素只进 in 不干扰 out，故出队序列严格等于到达序列。out 空时倒入保证了「in 中较新的元素永远不会抢在 out 中较老元素之前出队」。
- 均摊 O(1) 论证（聚合分析）：每个元素的生命周期恰好经历 4 次原子操作——Push 进 in、Pop 出 in、Push 进 out、Pop 出 out。n 次操作总代价 O(n)，均摊到每次操作 O(1)。单次 Pop 触发倒栈时最坏 O(n)，但这笔开销早已由之前的 n 次廉价 Push「预付」。会计法（给每个 Push 预存 2 元搬运费，倒栈时取出支付）可以给出同样结论，面试时任选一种讲清楚即可。

## 复杂度分析
- 时间：Push O(1)；Pop/Peek 均摊 O(1)，最坏单次 O(n) —— 对比方案一的每次 O(n)
- 空间：O(n)

## 代码实现
\`\`\`go
type MyQueue struct {
	in  []int // 入队栈：Push 只进这里
	out []int // 出队栈：Pop/Peek 只取这里
}

func Constructor() MyQueue {
	return MyQueue{}
}

func (q *MyQueue) Push(x int) {
	q.in = append(q.in, x)
}

// shift：out 为空时把 in 全部倒过来，顺序反转后队头在 out 栈顶
func (q *MyQueue) shift() {
	if len(q.out) == 0 {
		for len(q.in) > 0 {
			top := q.in[len(q.in)-1]
			q.in = q.in[:len(q.in)-1]
			q.out = append(q.out, top)
		}
	}
}

func (q *MyQueue) Pop() int {
	q.shift()
	v := q.out[len(q.out)-1]
	q.out = q.out[:len(q.out)-1]
	return v
}

func (q *MyQueue) Peek() int {
	q.shift()
	return q.out[len(q.out)-1]
}

func (q *MyQueue) Empty() bool {
	return len(q.in) == 0 && len(q.out) == 0 // 两栈皆空才为空
}
\`\`\`

## 边界与易错点
- shift 的条件必须是 out 为空才倒：out 非空时倒入会把新元素压到旧元素头上，FIFO 被破坏——这是本题第一大挂法。
- Empty 只查一个栈：in 空但 out 非空时队列里明明还有元素。
- Pop 前忘调 shift：out 空时直接越界。
- 用切片当栈时注意 Pop 写法 s[:len(s)-1] 不释放底层数组，本题规模无所谓，工程上留意内存滞留。

## 举一反三
- LeetCode 225 用队列实现栈：镜像题，用队列的轮转模拟 LIFO。
- LeetCode 622 设计循环队列：数组 + 双指针取模，队列实现的另一形态。
- LeetCode 641 设计循环双端队列：在 622 基础上两端都可进出。
- 识别信号：「用 A 结构模拟 B 结构」→ 找两个结构的语义差，用「两次反转」或「轮转」消除差异，并用均摊分析论证效率。

## 实际工程应用
双栈队列是消息队列延迟队列的经典实现原型：入队写到 append-only 的 in 段，消费端从 out 段读，out 耗尽时把 in 段整体「转正」，避免每条消息都挪动。日志收集系统（如 Flume 的 file channel 简化模型）、Go 调度器中本地运行队列与全局队列的交接，都能看到「批量转移摊薄成本」这一思想的影子。`,
    keyPoints: [
      "LIFO 套 LIFO = FIFO：两次反转恢复顺序，这是双栈模拟队列的全部数学",
      "倒栈只在 out 空时触发——这是 FIFO 正确性和均摊 O(1) 的共同前提",
      "均摊论证：每元素一生 4 次原子操作，n 次操作总 O(n)，单次倒栈开销由廉价 Push 预付",
      "Empty 必须两栈同查，漏查一个是经典 bug",
    ],
    followUps: [
      "均摊 O(1) 和平摊到每次最坏 O(1) 有什么区别？（均摊允许单次 O(n) 只要总账平；实时系统要求每次有界，双栈做不到）",
      "硬实时场景怎么办？（增量倒栈：每次操作只搬常数个元素，做到最坏 O(1)，实现复杂得多）",
      "能用一个栈实现吗？（递归调用栈当第二个栈可以，但等于作弊——用了隐式栈）",
    ],
    favorited: false,
  },
  {
    id: "algo-225",
    nodeId: "p1-stack-queue",
    question: `4. 用队列实现栈（LeetCode 225）
题意：仅用队列（只能 Push 到队尾、从队首 Pop/Peek、IsEmpty）实现 LIFO 栈：Push、Pop、Top、Empty。
示例：Push(1)、Push(2)、Top() → 2、Pop() → 2、Empty() → false（后进先出，2 先出）。
约束：最多 100 次操作；Pop/Top 只在栈非空时调用。`,
    answer: `## 题意与约束分析
与 232 互为镜像：队列 FIFO，栈 LIFO。两个队列无法像双栈那样天然反转顺序（队列出队不反转），所以需要主动制造反转。主流解法是单队列 + 轮转，Push 时就把顺序调好，使队头永远是栈顶。

## 思路推导：从暴力到最优
方案一（双队列）：Push 进空队列，Pop 时把元素逐个转移到另一队列、最后一个即栈顶弹出，Pop O(n)。方案二（单队列轮转，更优）：Push 时先把新元素入队，此时它在队尾——为了让栈顶（最新元素）位于队头，把队头到队尾的前 n-1 个元素依次出队再入队（轮转到队尾），新元素就来到了队头。轮转的本质是用队列自身的一次性重排，把「反转成本」集中支付在写入时刻。此后 Pop/Top 直接取队头，O(1)。代价集中在 Push 的 O(n)，换来取数 O(1)，适合「读多写少」。方案选择的关键话术：两种方案分别把 O(n) 放在 Push 还是 Pop 上，按读写比例选型。

## 正确性要点
- 轮转变换的正确性：设轮转前队列为 [a1, a2, ..., ak, x]（x 为新元素，ai 按栈底到栈顶的旧顺序）。把前 k 个依次出队入队后，队列变为 [x, a1, a2, ..., ak]，队头 x 即栈顶，且 a1..ak 的相对顺序保持不变——后续再轮转时旧元素的内部顺序被归纳保持。
- 不变式：队列从队头到队尾 = 栈从栈顶到栈底。Push 后轮转恢复该不变式；Pop 取队头即取栈顶，保持其余元素不变式。

## 复杂度分析
- 时间：Push O(n)（轮转 n-1 次）；Pop/Top/Empty O(1) —— 与双队列方案（Push O(1)、Pop O(n)）形成读写权衡
- 空间：O(n)

## 代码实现
\`\`\`go
type MyStack struct {
	q []int // 单队列：队头永远是栈顶
}

func Constructor() MyStack {
	return MyStack{}
}

func (s *MyStack) Push(x int) {
	s.q = append(s.q, x)
	n := len(s.q)
	// 轮转 n-1 次：把新元素之前的元素全部移到队尾，新元素来到队头
	for i := 0; i < n-1; i++ {
		s.q = append(s.q, s.q[0])
		s.q = s.q[1:]
	}
}

func (s *MyStack) Pop() int {
	v := s.q[0] // 队头即栈顶
	s.q = s.q[1:]
	return v
}

func (s *MyStack) Top() int {
	return s.q[0]
}

func (s *MyStack) Empty() bool {
	return len(s.q) == 0
}
\`\`\`

## 边界与易错点
- 轮转次数写成 n 而非 n-1：多转一次新元素回到队尾，不变式被破坏。
- 切片 s.q = s.q[1:] 出队：正确但注意它不释放头部空间，工程实现用环形缓冲。
- 空栈首次 Push：n=1 时轮转 0 次，队头即新元素，正确。
- 误以为两个队列也必须轮转：双队列方案的 O(n) 在 Pop 转移上，机制不同，面试时两套都要能讲。

## 举一反三
- LeetCode 232 用栈实现队列：镜像题，对比两者的「反转成本」放在哪一端。
- LeetCode 622 设计循环队列：队列的数组环形实现，是工程版队列的底座。
- LeetCode 933 最近的请求次数：队列滑动窗口的直接应用。
- 识别信号：「用 A 模拟 B」→ 先列语义差异表，再决定把转换成本放在哪个操作上，并用读写比例论证选型。

## 实际工程应用
单队列轮转维护「最新元素在队头」的思想，出现在 LRU 的队列实现变体中：访问某元素后把它挪到队头（或等价的重新入队），淘汰时从队尾取。任务调度系统里「置顶任务」语义——新到高优任务插入队首而低优任务顺序后移——与本题轮转同构。它更深刻的启示是接口选型：同一个抽象栈，底层可用数组、链表、队列实现，代价分布不同，按业务读写比例选型是工程师的日常决策。`,
    keyPoints: [
      "单队列不变式：队头到队尾 = 栈顶到栈底，Push 后轮转 n-1 次恢复不变式",
      "轮转保持旧元素内部相对顺序——归纳法保证了多轮 Push 后顺序仍正确",
      "两种方案的权衡话术：单队列 O(n) 在 Push，双队列 O(n) 在 Pop，读多写少选前者",
      "与 232 对照：栈反转是天然的（倒栈），队列反转必须主动轮转，成本显式化",
    ],
    followUps: [
      "能只用队列的常数个操作让 Push 也 O(1) 吗？（不能，FIFO 到 LIFO 的反转下界就是 O(n)，可以把 O(n) 摊到任意一侧但消不掉）",
      "如果允许用双端队列 deque 呢？（那就 trivial 了，deque 本身就是栈——说明题目限制的是单端队列语义）",
      "轮转在并发多生产者下有什么问题？（轮转期间队列状态中间态暴露，消费者可能读到未轮转完的错误顺序，需加锁）",
    ],
    favorited: false,
  },
  {
    id: "algo-150",
    nodeId: "p1-stack-queue",
    question: `5. 逆波兰表达式求值（LeetCode 150）
题意：给定后缀表达式（逆波兰式）的字符串数组 tokens，求其值。运算符只有 + - * /，整数除法向零取整。
示例：输入 ["2","1","+","3","*"] → 输出 9（等价中缀 (2+1)*3=9）。
约束：1 ≤ tokens 长度 ≤ 10^4；表达式保证合法，结果与中间值都在 32 位整数内。`,
    answer: `## 题意与约束分析
后缀表达式求值是栈的教科书应用：操作数按序出现，运算符出现时它需要的两个操作数恰好是「最近压入的两个」——LIFO 天然匹配。题目保证合法且整数运算无溢出顾虑，考点集中在运算顺序细节。

## 思路推导：从暴力到最优
后缀表达式本来就是为了「无括号线性求值」设计的：运算符的优先级和结合性在转换成后缀形式时已经编码进 token 顺序里，求值端不再需要任何优先级表。机制：扫描 tokens，数字压栈；遇到运算符时弹出两个数，先弹出的是右操作数 b，后弹出的是左操作数 a，计算 a op b 后把结果压回。扫描结束栈里只剩一个数即答案。对 - 和 / 而言 a、b 顺序决定结果：10 5 - 是 10-5=5 而非 5-10——这是本题唯一的实质性陷阱。乘法和加法交换律掩盖顺序，减除不行。中缀转后缀（调度场算法）是进阶话题，本题不需要。

## 正确性要点
- 不变式：扫描任意前缀后，栈中保存「已处理子表达式的求值结果，按它们在中缀中从左到右的顺序自栈底向栈顶排列」。
- 归纳证明：数字压栈保持顺序；遇运算符 op 时，栈顶两元素恰为 op 在中缀里的左右操作数（由后缀定义：op 紧随其右操作数子表达式之后），先弹者为右、后弹者为左，计算 a op b 压回后不变式恢复。
- 合法性保证下，结束时栈长恰为 1，即整式值。

## 复杂度分析
- 时间：O(n) —— 每个 token 一次压栈或一次弹二压一
- 空间：O(n) —— 最坏全是数字

## 代码实现
\`\`\`go
import "strconv"

func evalRPN(tokens []string) int {
	stack := []int{}
	for _, t := range tokens {
		switch t {
		case "+", "-", "*", "/":
			b := stack[len(stack)-1] // 先弹出：右操作数
			a := stack[len(stack)-2] // 后弹出：左操作数
			stack = stack[:len(stack)-2]
			var res int
			switch t {
			case "+":
				res = a + b
			case "-":
				res = a - b // 顺序敏感：左 - 右
			case "*":
				res = a * b
			case "/":
				res = a / b // Go 整数除法天然向零取整
			}
			stack = append(stack, res)
		default:
			v, _ := strconv.Atoi(t) // 数字直接压栈
			stack = append(stack, v)
		}
	}
	return stack[0]
}
\`\`\`

## 边界与易错点
- 减除的操作数顺序颠倒：先弹的当左操作数是最常见错误，10 5 / 算成 0.2 再取整直接错。
- 负数 token：Atoi 处理负号没问题；不要把 "-" 误判为运算符——区分靠 token 长度或查运算符集合。
- 除法取整方向：Go/Java 的 int 除法向零取整恰好符合题意；若用其他语言（如 Python 的 // 向下取整）需要额外处理。
- 单 token 表达式：如 ["42"]，循环只做一次压栈，返回栈底，正确。

## 举一反三
- LeetCode 224 基本计算器：中缀直接求值，双栈（数栈+运算符栈）处理优先级与括号。
- LeetCode 227 基本计算器 II：无括号有乘除，可用栈延迟加减或两遍扫描。
- LeetCode 394 字符串解码：同构的「遇括号分层」栈应用。
- 识别信号：「表达式求值」→ 后缀用单栈；中缀用双栈或先转后缀；括号分层用栈帧。

## 实际工程应用
逆波兰式是栈机和寄存器权衡的经典案例：JVM 字节码就是后缀式的栈机指令集，iload、iadd 的执行模型与本题逐行对应；Forth 语言和 PostScript 页面描述语言都直接采用 RPN。计算器固件（HP 计算器传统）用 RPN 正是因为无需括号、求值只需一个栈，硬件实现成本极低。规则引擎把 DSL 编译成 RPN 后逐条执行也是同一模型。`,
    keyPoints: [
      "后缀式求值不变式：栈底到栈顶 = 各子表达式结果按中缀从左到右的顺序",
      "先弹右、后弹左——减除顺序敏感，这是本题唯一实质陷阱",
      "Go 整数除法向零取整恰好契合题意，换语言必须确认取整方向",
      "JVM 字节码就是 RPN 栈机，这题是理解栈式虚拟机指令执行的最小模型",
    ],
    followUps: [
      "如何把中缀转后缀？（调度场算法：运算符栈按优先级弹出，左括号压栈右括号弹到左括号）",
      "表达式可能不合法时要检查什么？（运算符时栈长 <2、结束时栈长 ≠1、除零）",
      "操作数是浮点或支持变量绑定呢？（栈元素改为带类型的值结构，变量名查环境表）",
    ],
    favorited: false,
  },
  {
    id: "algo-71",
    nodeId: "p1-stack-queue",
    question: `6. 简化路径（LeetCode 71）
题意：给定 Unix 风格绝对路径字符串，将其简化为规范路径：. 表示当前目录、.. 表示上级目录、多个连续斜杠等价于一个；根目录的 .. 仍停留在根。
示例：输入 "/a/./b/../../c/" → 输出 "/c"（b 之后 .. 回退两级到根，再进 c）。
约束：1 ≤ 路径长度 ≤ 3000；路径由目录名、.、..、/ 组成，目录名只含字母数字等安全字符。`,
    answer: `## 题意与约束分析
路径规范化。目录结构是天然的栈：进入一个目录即压栈，.. 回退即弹栈。要处理的边角有三类：连续斜杠产生空段、. 不产生动作、根目录处 .. 无处可退只能停留。长度 3000 使任何 O(n) 方案都轻松。

## 思路推导：从暴力到最优
直接按 / 切分字符串，逐段决策：空段（连续斜杠或首尾斜杠产生）与 . 段直接忽略；.. 段弹栈（栈空时忽略，即根目录回退停留原地）；其余段压栈。最终栈底到栈顶就是从根到目标的目录序列，用 / 拼接并在最前补一个 /。栈空的拼接结果是空串，恰好规范化为根路径 /。这个方案的优雅在于「忽略」「弹栈」「压栈」三种动作覆盖了所有段类型，无一漏网。注意 Split 的行为：以 / 开头切出的首元素是空串，会被「空段忽略」规则自动吸收，无需特判。

## 正确性要点
- 不变式：栈中自底向上保存「从根目录出发到达当前目录所经过的目录名序列」。
- 三种动作的语义完备性：空段和 . 不改变当前目录（忽略）；.. 使当前目录上溯一级（弹栈），栈为空意味着当前已在根，上溯仍是根（忽略弹栈请求）；普通段进入子目录（压栈）。路径串的所有段类型被这三类穷尽。
- 输出构造：规范路径 = "/" + Join(stack, "/")；栈空时 Join 为空串，结果恰为 "/"，覆盖「全程回退到根」的情形。

## 复杂度分析
- 时间：O(n) —— 切分 O(n)，每段至多一次压栈或弹栈
- 空间：O(n) —— 栈与结果字符串

## 代码实现
\`\`\`go
import "strings"

func simplifyPath(path string) string {
	stack := []string{}
	// 按斜杠切分：连续斜杠产生空段，统一忽略
	for _, part := range strings.Split(path, "/") {
		switch part {
		case "", ".":
			// 空段与当前目录记号：无动作
		case "..":
			if len(stack) > 0 {
				stack = stack[:len(stack)-1] // 回退一级；栈空即在根，停留
			}
		default:
			stack = append(stack, part) // 进入子目录
		}
	}
	return "/" + strings.Join(stack, "/") // 栈空时 Join 为空，结果为根 "/"
}
\`\`\`

## 边界与易错点
- 根目录连续 ..：如 "/../../../a"，栈空时弹栈请求被忽略，最终 "/a"；写成无保护弹栈会越界。
- 连续斜杠：如 "//home//a/"，Split 产生多个空串，全靠空段忽略规则吸收。
- 尾随斜杠："/a/b/" 切出末尾空串，同样被忽略，输出 "/a/b" 不带尾斜杠，符合规范。
- 目录名含点：如 "..."、"..a" 都是合法目录名，只有严格的 ".." 才是回退——用相等比较而非前缀匹配。

## 举一反三
- LeetCode 388 文件的最长绝对路径：给定带缩进的文件系统字符串求最长文件路径，同样用栈维护当前目录层级。
- LeetCode 20 有效的括号：同族「栈处理成对/抵消语义」。
- LeetCode 844 比较含退格的字符串：# 等价于 .. 的弹栈语义，几乎同一道题。
- 识别信号：「字符串中带抵消/回退语义的符号」→ 栈；只关心最终状态时可退化为计数或双指针。

## 实际工程应用
路径规范化是文件系统 API、Web 服务器路由和构建工具的安全基石：HTTP 服务器必须先把 URL 路径规范化再做静态文件映射，否则 ../etc/passwd 式的目录穿越攻击直接越权——Nginx、Node 的 path.normalize、Python 的 os.path.normpath 干的就是本题。构建系统（Bazel、Make）解析依赖路径、容器镜像层的路径合并，底层都是这个栈模型。`,
    keyPoints: [
      "三种段类型（忽略/回退/进入）穷尽所有输入，栈的不变式是「根到当前目录的目录名序列」",
      "根目录 .. 停留语义 = 栈空时忽略弹栈请求，一行判空覆盖",
      "Split 产生的空段（连续斜杠/首尾斜杠）被忽略规则统一吸收，零特判",
      "只有严格相等的 .. 才是回退——目录名 ..a、... 合法，相等比较是纪律",
    ],
    followUps: [
      "如果要支持符号链接，规范化还正确吗？（不正确：.. 在符号链接下可能回到不同父目录，需要真实文件系统解析——CVE 重灾区）",
      "相对路径转绝对路径怎么做？（先拼上当前工作目录再跑同一套规范化）",
      "Windows 路径（反斜杠 + 盘符）要改哪里？（切分符、根表示从 / 变为盘符，回退栈逻辑不变）",
    ],
    favorited: false,
  },
  {
    id: "algo-394",
    nodeId: "p1-stack-queue",
    question: `7. 字符串解码（LeetCode 394）
题意：给定编码规则 k[encoded] 表示 encoded 重复 k 次的字符串，解码它；编码可以嵌套，如 3[a2[c]]。
示例：输入 "3[a2[c]]" → 输出 "accaccacc"（内层 2[c] 解码为 cc，外层 3[acc] 重复三次）。
约束：1 ≤ 长度 ≤ 30；k 为正整数，输入保证合法，输出不含数字。`,
    answer: `## 题意与约束分析
嵌套结构解码。k[...] 的方括号形成严格嵌套（题目保证合法），嵌套意味着「先解内层，乘上外层重复次数，再拼到更外层」。这种「遇 [ 开新层、遇 ] 合并回上一层」的结构与括号匹配同族，栈是自然选择。长度仅 30，任何正确方法都行，但模式价值远超本题数据规模。

## 思路推导：从暴力到最优
递归下降是第一直觉：解析函数遇数字读 k、遇 [ 递归解码内层、遇 ] 返回本层结果，嵌套天然被递归栈处理。迭代等价物是双栈：数字栈存各层重复次数，字符串栈存「进入本层之前已构建好的外层前缀」。扫描维护一个当前层缓冲区 cur：遇数字累积成 k（注意多位数）；遇 [ 把 k 压数字栈、cur 压字符串栈、cur 清空进入新层；遇 ] 弹出 k 和外层前缀，本层结果 = 前缀 + cur 重复 k 次，写回 cur 继续；遇字母直接 append 到 cur。扫描结束 cur 即全文。两个栈与递归调用栈一一对应：数字栈是「参数」，字符串栈是「现场保存」。

## 正确性要点
- 不变式：任意时刻，最终结果 = strStack 自底向上各层前缀按「前缀 + 重复k次[内层]」嵌套，cur 是当前最内层已解析内容。
- 遇 ] 的合并正确性：] 出现时 cur 恰为本层括号内的完整解码（由嵌套合法性与归纳保证），弹出外层前缀 prev 与次数 k，新 cur = prev + cur×k，恰好完成本层并回到外层语境。
- 多位数处理：num = num*10 + digit 的累积在读入 [ 时结算清零，保证 k 完整。

## 复杂度分析
- 时间：O(输出长度) —— 每个输出字符被写入常数次（嵌套重复不可避免）
- 空间：O(嵌套深度 × 层内容) —— 双栈

## 代码实现
\`\`\`go
import "strings"

func decodeString(s string) string {
	numStack := []int{}    // 各层重复次数
	strStack := []string{} // 各层进入前的外层前缀
	cur := strings.Builder{}
	num := 0
	for _, ch := range s {
		switch {
		case ch >= '0' && ch <= '9':
			num = num*10 + int(ch-'0') // 多位数累积
		case ch == '[':
			numStack = append(numStack, num)
			strStack = append(strStack, cur.String())
			num = 0
			cur.Reset() // 开新层
		case ch == ']':
			k := numStack[len(numStack)-1]
			numStack = numStack[:len(numStack)-1]
			prev := strStack[len(strStack)-1]
			strStack = strStack[:len(strStack)-1]
			inner := cur.String()
			cur.Reset()
			cur.WriteString(prev) // 先接外层前缀
			for i := 0; i < k; i++ {
				cur.WriteString(inner) // 内层重复 k 次
			}
		default:
			cur.WriteRune(ch) // 普通字母进当前层
		}
	}
	return cur.String()
}
\`\`\`

## 边界与易错点
- 多位数 k（如 12[a]）：必须累积 num 而不是逐字符入栈，遇 [ 才结算。
- 嵌套层间串扰：遇 [ 忘记保存外层前缀，或遇 ] 忘记按「前缀 + 内层×k」组装，都会丢字符。
- 字母出现在括号外：如 "2[ab]c"，cur 在外层继续累积，不变式保证正确。
- 大 k 深嵌套的输出爆炸：3[a2[b3[c]]] 输出长度指数级增长——这是输出本身的大小，任何算法都无法避免，复杂度必须按输出长度计。

## 举一反三
- LeetCode 20 有效的括号：同族括号栈，但只校验不构造。
- LeetCode 726 原子的数量：化学式括号嵌套统计，双栈或递归下降同框架。
- LeetCode 1106 解析布尔表达式：带运算符的嵌套括号求值，栈帧模式。
- LeetCode 224 基本计算器：括号分层求值的另一形态。
- 识别信号：「嵌套括号 + 每层有参数（次数/运算符/计数）」→ 每层一个栈帧，参数栈 + 现场栈成对出现。

## 实际工程应用
这是「带参数的嵌套结构解析」的最小模型：模板引擎展开嵌套宏（每层的参数是循环次数）、协议解包（TLV 嵌套编码如 ASN.1）、压缩格式中重复块的展开（LZ77 的距离-长度对解码在概念上相近）。递归下降解析器处理任何嵌套文法——JSON、Lisp、正则——其手工实现的核心栈帧模式与本题的双栈完全同构。`,
    keyPoints: [
      "双栈语义：数字栈 = 各层参数（重复次数），字符串栈 = 各层现场（外层前缀），与递归调用栈一一对应",
      "遇 ] 的组装公式：新 cur = 外层前缀 + 内层 × k，顺序不能反",
      "多位数累积 num = num*10 + d，遇 [ 结算清零——最常见的 WA 点",
      "时间复杂度按输出长度计：深嵌套下输出指数级增长，是输出本身的下界",
    ],
    followUps: [
      "改用递归下降怎么写？（全局下标 + 递归函数，遇 [ 递归、遇 ] 返回，代码更短但深嵌套有栈深风险）",
      "如果 k 可以达到 10^9 怎么办？（不能真展开，需要惰性表示如 rope 结构，或只回答长度类查询）",
      "输入可能不合法（括号不配对）时加什么检查？（遇 ] 时栈空、结束时 numStack 非空即为非法）",
    ],
    favorited: false,
  },
  {
    id: "algo-739",
    nodeId: "p1-stack-queue",
    question: `8. 每日温度（LeetCode 739）
题意：给定每日温度数组，对每一天计算还要等多少天才出现更高温度；若不存在则为 0。
示例：输入 [73,74,75,71,69,72,76,73] → 输出 [1,1,4,2,1,1,0,0]（如第 3 天 75 要等 4 天到 76）。
约束：1 ≤ 数组长度 ≤ 10^5，温度 30 ~ 100。`,
    answer: `## 题意与约束分析
对每个元素找「右边第一个比它大的元素」的距离。n 到 10^5 排除了 O(n^2) 的双重循环（10^10 必超时），需要 O(n) 或 O(n log n)。这是单调栈的模板题，也是「next greater element」家族的入口。

## 思路推导：从暴力到最优
暴力：对每一天向右扫描找第一个更高温，O(n^2)。瓶颈在于大量重复比较——第 i 天比较过的元素，第 i+1 天又比较一遍。关键观察：如果今天温度比昨天高，那昨天的问题立刻有了答案（就是今天）；如果比昨天低，那昨天还在等，今天也要等——「还在等答案的日子」构成一个温度单调递减的序列（否则靠前的早就出结果了）。维护这个递减序列用栈（存下标）：每天到来时，把栈里所有温度比今天低的日子弹出，它们的答案就是今天的下标差；然后今天入栈。每个下标只入栈一次、出栈一次。

## 正确性要点
- 单调性不变式：栈内下标对应温度自栈底向栈顶严格（或非严格）递减。因为每当出现更高温度，所有更低的栈顶都被弹出才压入新元素。
- 「出栈时刻 = 答案确定时刻」：下标 j 在 i 时刻被弹出，说明 temperatures[i] > temperatures[j] 且 i 之前没有任何 k（j < k < i）满足 temperatures[k] > temperatures[j]——若有，j 早在 k 时刻就被弹出了。故 i 是 j 右边第一个更高温，ans[j] = i - j。
- 均摊 O(n)：每个下标入栈一次、出栈至多一次，内层 while 的总执行次数全数组累计不超过 n。

## 复杂度分析
- 时间：O(n) —— 均摊论证，对比暴力 O(n^2)
- 空间：O(n) —— 最坏温度全程递减，栈存满

## 代码实现
\`\`\`go
func dailyTemperatures(temperatures []int) []int {
	n := len(temperatures)
	ans := make([]int, n) // 零值恰好是「无更高温」的默认答案
	stack := []int{}      // 单调递减栈：存下标，温度自栈底向栈顶递减
	for i := 0; i < n; i++ {
		// 今天比栈顶那些日子热：它们的等待到此为止
		for len(stack) > 0 && temperatures[i] > temperatures[stack[len(stack)-1]] {
			top := stack[len(stack)-1]
			stack = stack[:len(stack)-1]
			ans[top] = i - top // 出栈时刻即答案确定时刻
		}
		stack = append(stack, i)
	}
	return ans
}
\`\`\`

## 边界与易错点
- 全程递减序列：如 [5,4,3,2,1]，无出栈发生，ans 全零——零值初始化自动覆盖，不要漏掉这个默认语义。
- 相等温度：条件用 > 弹出，相等的留在栈里等待更右侧的严格更高温，符合「更高」的严格定义；写成 >= 会把等温日错误结算。
- 栈存值不存下标：答案要的是天数差，必须存下标（值可通过下标查到）。
- 最后一天：永远在栈里等不到答案，靠默认零值正确。

## 举一反三
- LeetCode 496 下一个更大元素 I：同模板，多一步用哈希表映射查询数组的答案。
- LeetCode 503 下一个更大元素 II：循环数组版，下标取模 + 遍历 2n。
- LeetCode 901 股票价格跨度：求「左边第一个更大值」的距离，方向反过来 + 跨度压缩。
- LeetCode 84 柱状图中最大的矩形：左右两个方向的最近更小值，单调栈巅峰应用。
- 识别信号：「每个元素找左/右侧第一个比它大/小的元素」→ 单调栈，方向决定弹出条件。

## 实际工程应用
单调栈是流式数据「等待未来事件确认」问题的标准件：行情系统中计算每根 K 线之后多少周期才创新高；告警系统里「该指标多久后被超越」的统计。代码中的「出栈即结算」模式对应着事件驱动系统里的「未来事件到达时批量回调历史等待者」——与股票市场中的限价单撮合（更高买价到来时成交一批挂低的卖单）在结构上完全同构。`,
    keyPoints: [
      "单调递减栈的不变式：栈内温度从栈底到栈顶递减，栈里全是「还在等答案的日子」",
      "出栈时刻 = 答案确定时刻：j 被 i 弹出，证明 i 是 j 右侧第一个更高温",
      "均摊 O(n) 论证：每下标入栈一次出栈至多一次，while 总次数 ≤ n",
      "弹出条件用严格大于：等温日要继续等待，>= 会错误结算",
    ],
    followUps: [
      "如果要求「左边第一个更高温」呢？（从右往左遍历，同一模板镜像）",
      "温度范围只有 30~100，能用这个性质优化吗？（可以用桶记录下标，但 O(n) 单调栈已最优，桶只省常数）",
      "数据是流式到来的（看不到未来）怎么办？（单调栈天然支持流式：新温度到达时在线结算，无需存全量）",
    ],
    favorited: false,
  },
  {
    id: "algo-503",
    nodeId: "p1-stack-queue",
    question: `9. 下一个更大元素 II（LeetCode 503）
题意：给定循环数组 nums（最后一个元素的下一个回到第一个），对每个元素找下一个更大元素，不存在则为 -1。
示例：输入 [1,2,1] → 输出 [2,-1,2]（下标 2 的 1 循环回开头后遇到 2）。
约束：1 ≤ 数组长度 ≤ 10^4，-10^9 ≤ 元素 ≤ 10^9。`,
    answer: `## 题意与约束分析
739/496 的循环数组版：每个元素的「下一个」允许绕回数组开头。难点是循环带来的无限性——下标 0 的右侧在逻辑上包含了整个数组的其余部分，需要把「绕一圈」有限化。

## 思路推导：从暴力到最优
暴力：对每个元素从 i+1 开始绕圈扫描最多 n-1 步，O(n^2)。沿用单调栈思路，问题只剩「循环如何表示」。关键洞察：循环数组上「i 的右侧」等价于把数组复制一遍拼接后 [i+1, i+n-1] 这段窗口；而拼接后的数组 i+n 位置的值就是 nums[i % n]。于是遍历 2n 个下标、用 i % n 取值，第二圈天然给第一圈没等到答案的元素第二次机会。优化：第二圈的元素不需要再入栈（它们的答案若存在，在第一圈内已被同值元素结算；入栈只会重复劳动）——只对 i < n 的下标入栈，栈里存的都是第一圈下标，结算直接写 ans。

## 正确性要点
- 拼接等价性：循环意义下 i 右侧第一个更大元素 = 拼接数组 nums+nums 中 i 右侧第一个更大元素（若存在于 i+n-1 之内）；超过一圈还没找到说明不存在（更大值若存在必在一圈内出现）。
- 第二圈不入栈的正确性：第二圈下标 i（≥ n）与第一圈下标 i-n 同值；栈中若还有未结算元素，比它小的同值元素再入栈不会带来新结算，而比它大时结算的仍是第一圈下标。故只入栈第一圈即可保证 ans 全部正确，且避免重复计算。
- 单调性与「出栈即答案」的论证与 739 完全一致。

## 复杂度分析
- 时间：O(n) —— 遍历 2n 步，每下标入栈出栈至多一次
- 空间：O(n)

## 代码实现
\`\`\`go
func nextGreaterElements(nums []int) []int {
	n := len(nums)
	ans := make([]int, n)
	for i := range ans {
		ans[i] = -1 // 默认不存在
	}
	stack := []int{} // 单调递减栈，存第一圈下标
	for i := 0; i < 2*n; i++ {
		idx := i % n // 取模实现循环遍历
		for len(stack) > 0 && nums[idx] > nums[stack[len(stack)-1]] {
			top := stack[len(stack)-1]
			stack = stack[:len(stack)-1]
			ans[top] = nums[idx] // 出栈结算：右侧第一个更大者
		}
		if i < n {
			stack = append(stack, idx) // 只入栈第一圈下标
		}
	}
	return ans
}
\`\`\`

## 边界与易错点
- 全相等数组：无元素被弹出，ans 全 -1，正确。
- 最大值的答案：最大元素右侧不存在更大者，绕一圈也找不到，-1 正确。
- 第二圈也入栈：功能上仍正确（同值入栈不改变结算结果）但浪费且下标可能越出 ans 界（i % n 入栈则无事，直接入栈 i 会写越界）——只入第一圈是最干净的写法。
- 遍历时用 i 直接索引 nums：i ≥ n 时越界，必须 i % n。

## 举一反三
- LeetCode 739 每日温度：非循环版 + 求距离而非值。
- LeetCode 496 下一个更大元素 I：非循环 + 子集查询。
- LeetCode 42 接雨水：单调栈的另一形态（找两侧更高者围成凹槽）。
- LeetCode 556 下一个更大元素 III：数字位重排的「下一个更大」，不同机制但同名家族。
- 识别信号：「循环数组 + 下一个更大/小」→ 2n 遍历 + 下标取模，第二圈只结算不入栈。

## 实际工程应用
环形结构上的「下一个满足条件者」查询在工程里随处可见：环形缓冲区中查找下一个可写槽位、一致性哈希环上为 key 找顺时针第一个节点（这正是分布式缓存 Sharding 的核心查询，工程上用跳表/红黑树加速，但小数据时就是环形单调扫描）、轮询调度器找下一个健康后端。2n 取模的「虚拟拼接」技巧是把环形问题线性化的标准手法。`,
    keyPoints: [
      "循环线性化：遍历 2n + 下标取模，等价于把数组复制拼接，环形问题变直线问题",
      "第二圈只结算不入栈：同值元素重复入栈不产生新结算，且可避免 ans 越界",
      "答案必在一圈内出现或不存在——这是 2n 上界正确的理由",
      "与一致性哈希环同构：环形上「顺时针第一个更大者」是分布式系统的日常查询",
    ],
    followUps: [
      "能只遍历 n 次解决吗？（可以：第一遍先压入全部下标再正走第二遍，但代码并不比 2n 简洁，2n 是标准写法）",
      "如果要求「上一个更大元素」（逆时针）呢？（从右往左做同样的 2n 遍历，或 i 取模后方向反转）",
      "数据范围到 10^5 且有多次查询呢？（预处理 + 查询分离；多次查询考虑离线处理或线段树，不再用单次单调栈）",
    ],
    favorited: false,
  },
  {
    id: "algo-901",
    nodeId: "p1-stack-queue",
    question: `10. 股票价格跨度（LeetCode 901）
题意：设计 StockSpanner 类，每次调用 Next(price) 返回今日价格的「跨度」：从今天往前数，连续多少个交易日的价格小于等于今日（含今天）。
示例：依次 Next(100)、Next(80)、Next(60)、Next(70)、Next(60)、Next(75)、Next(85) → 返回 1,1,1,2,1,4,6（75 那天往前 75,60,70,60 共 4 天连续 ≤75）。
约束：最多 10^5 次 Next 调用，价格 1 ~ 10^5。`,
    answer: `## 题意与约束分析
在线算法题：价格流式到达，每次 Next 要立刻回答「向前连续不超过今日价格的天数」。10^5 次调用要求每次均摊接近 O(1)。跨度定义里的「连续 ≤ 今日」一旦遇到更大价格即中断——本质是找「左边第一个比今天大的价格」，跨度 = 今日下标 - 那个位置。

## 思路推导：从暴力到最优
暴力：每次 Next 从昨天往前扫直到遇到更大价格，最坏 O(n) 每次，总 O(n^2)。沿用单调栈思路：维护价格单调递减的栈（栈底到栈顶价格递减），今日价格到来时弹出所有 ≤ 今日的价格——但跨度怎么算？直接数弹出个数不对：弹出的天可能不连续（它们之间隔着已被更早弹出结算过的天）。精妙之处：给栈中每个元素附一个「跨度」字段，记录该元素入栈时自己的答案。弹出时把被弹者的跨度累加到今日跨度上——因为被弹者的跨度已经压缩了它左边那一整段连续更小者的天数。这相当于把历史的连续段「折叠」进了栈元素的权重里。最后 (今日价, 累计跨度) 入栈。

## 正确性要点
- 不变式：栈内 (价格, 跨度) 按价格自栈底向栈顶严格递减；每个元素的跨度 = 从它入栈那天向前、价格连续 ≤ 它的天数（已被它「折叠」）。
- 累加正确性：今日价 p 弹出栈顶序列 (p1,s1),(p2,s2),...（均 ≤ p）。p1 是昨天，s1 覆盖了昨天向前连续 ≤ p1 的天，这些天价格也都 ≤ p1 ≤ p，且紧接昨天——连续；p1 及其覆盖段弹完后新的栈顶 p2 紧接这段历史之前，同样满足 ≤ p……归纳得累加所有被弹者的跨度加 1（今天自己）恰好是「向前连续 ≤ 今日」的总天数。
- 均摊 O(1)：每个元素入栈一次、出栈至多一次，总弹出次数 ≤ 总调用次数。

## 复杂度分析
- 时间：Next 均摊 O(1)，最坏单次 O(n) —— 对比暴力每次 O(n)
- 空间：O(n) —— 最坏价格全程递增，栈存满

## 代码实现
\`\`\`go
type StockSpanner struct {
	prices []int // 单调递减栈：价格
	spans  []int // 与价格一一对应的折叠跨度
}

func Constructor() StockSpanner {
	return StockSpanner{}
}

func (s *StockSpanner) Next(price int) int {
	span := 1 // 至少含今天
	// 弹出所有 ≤ 今日的价格，把它们折叠的跨度累加进来
	for len(s.prices) > 0 && s.prices[len(s.prices)-1] <= price {
		span += s.spans[len(s.spans)-1]
		s.prices = s.prices[:len(s.prices)-1]
		s.spans = s.spans[:len(s.spans)-1]
	}
	s.prices = append(s.prices, price)
	s.spans = append(s.spans, span) // 折叠后的跨度随元素入栈
	return span
}
\`\`\`

## 边界与易错点
- 首日调用：栈空，span=1，正确。
- 价格严格递增序列：每次 Next 都弹空前栈并累加全部历史，span 恰为总天数，正确。
- 弹出条件 ≤：等于今日价格的天要计入跨度（题目定义是小于等于），用 < 会漏算等价格日。
- 只数弹出个数不加权：被弹元素之间的「已折叠历史段」被漏掉，如 [100,80,60,70,75] 中 75 的跨度会错算成 2 而非 4——加权累加是本题灵魂。

## 举一反三
- LeetCode 739 每日温度：找右边第一个更大，方向相反、无折叠权重。
- LeetCode 84 柱状图中最大的矩形：单调栈 + 宽度计算，宽度思想与本题跨度相通。
- LeetCode 907 子数组的最小值之和：单调栈 + 贡献度计算，折叠思想的另一形态。
- LeetCode 155 最小栈：「栈元素携带附加聚合信息」的同族技巧。
- 识别信号：「在线流式 + 向前连续段统计」→ 单调栈 + 元素携带折叠权重，出栈时累加权重。

## 实际工程应用
本题就是真实行情系统里「N 日新高统计」「连续上涨天数」的原型计算。更一般地，「流式数据上向前看连续满足条件的窗口长度」是监控告警的高频需求：接口错误率连续 N 个周期低于阈值才恢复告警、传感器连续超温时长统计。折叠权重的技巧——把已结算的历史段压缩成栈元素的权重——是流式聚合系统（Flink 的增量聚合、Prometheus 的记录规则）中状态压缩的通用思想。`,
    keyPoints: [
      "跨度 = 今日下标 - 左边第一个更大价格的下标；单调递减栈在线维护这个边界",
      "折叠权重是灵魂：栈元素携带自己的历史跨度，出栈时累加，避免重复遍历已结算段",
      "弹出条件 ≤ 严格对应题目「小于等于」的定义，等价格日要计入",
      "均摊 O(1)：每价格入栈一次出栈至多一次，while 总次数 ≤ 总调用数",
    ],
    followUps: [
      "如果要查「向前连续严格小于的天数」呢？（弹出条件改为 <，其余不变——定义与弹出条件的对应是考点）",
      "如果还要支持「撤回最近一次 Next」呢？（需要记录每次入栈前弹出了哪些元素，用操作日志栈回滚）",
      "内存受限不能存全部历史呢？（栈本身就是压缩表示，最坏仍 O(n)；进一步可只保留近 K 天的近似，牺牲精确性）",
    ],
    favorited: false,
  },
  {
    id: "algo-84",
    nodeId: "p1-stack-queue",
    question: `11. 柱状图中最大的矩形（LeetCode 84）
题意：给定柱状图各柱子高度（宽度均为 1），求能在这张柱状图中画出的最大矩形面积。
示例：输入 [2,1,5,6,2,3] → 输出 10（高度 5 和 6 的两根柱子组成宽 2 高 5 的矩形）。
约束：1 ≤ 柱子数 ≤ 10^5，高度 0 ~ 10^4。`,
    answer: `## 题意与约束分析
求柱状图内接最大矩形。每个候选矩形由「高度 = 某根柱子、宽度向两侧延伸到第一个更矮的柱子为止」唯一刻画——最大矩形必然以某根柱子的高度为高（否则可以向上放大）。n 到 10^5 要求 O(n)，穷举每个高再扫两侧是 O(n^2)，需要单调栈把「两侧第一个更矮者」一次算出。

## 思路推导：从暴力到最优
暴力：对每根柱子向左右扫描找第一个更矮的柱子，面积 = h × (r - l - 1)，O(n^2)。观察：「右边第一个更矮者」正是单调栈的结算事件——当遍历到柱子 i 且 h[i] 比栈顶柱子矮时，栈顶柱子的高度「再也无法向右扩展」，以其为高的最大矩形在此刻确定：右边界 = i，左边界 = 出栈后的新栈顶（即左边第一个更矮者），宽 = r - l - 1。维护高度单调递增的栈（栈底到栈顶递增），每根柱子入栈一次、出栈一次，出栈时结算。哨兵技巧：高度数组头部补 0（保证左边界总有栈底可指）、尾部补 0（强制所有柱子在遍历结束时出栈结算），消掉所有边界特判。

## 正确性要点
- 出栈时宽度公式的正确性：栈顶 mid 被 i 弹出时，h[i] < h[mid]，且 i 是 mid 右侧第一个更矮者——若其间存在更矮者，mid 早被弹出。新栈顶 left 满足 h[left] < h[mid]，且是左侧第一个更矮者——单调递增栈保证 (left, mid] 区间内所有柱子 ≥ h[mid]。故以 h[mid] 为高的矩形恰好覆盖 (left, i)，宽 i - left - 1，该矩形是此高度的最大矩形。
- 完备性：每根柱子都会出栈结算一次（尾部哨兵 0 强制清空），而最大矩形必以某根柱子为高，故所有候选都被枚举，最大值正确。
- 均摊 O(n)：每根柱子入栈一次、出栈至多一次。

## 复杂度分析
- 时间：O(n) —— 对比暴力 O(n^2)
- 空间：O(n) —— 最坏高度全程递增

## 代码实现
\`\`\`go
func largestRectangleArea(heights []int) int {
	// 首尾补哨兵 0：头部保证左边界存在，尾部强制清空栈
	h := append([]int{0}, heights...)
	h = append(h, 0)
	stack := []int{0} // 单调递增栈：存下标，哨兵 0 先入栈
	ans := 0
	for i := 1; i < len(h); i++ {
		for len(stack) > 0 && h[i] < h[stack[len(stack)-1]] {
			mid := stack[len(stack)-1] // 以 h[mid] 为高的矩形此刻结算
			stack = stack[:len(stack)-1]
			left := stack[len(stack)-1] // 左侧第一个更矮者
			right := i                  // 右侧第一个更矮者
			area := h[mid] * (right - left - 1)
			if area > ans {
				ans = area
			}
		}
		stack = append(stack, i)
	}
	return ans
}
\`\`\`

## 边界与易错点
- 高度含 0：0 高度柱子自然成为边界，算法不受影响；哨兵也是 0，注意原数组的 0 与哨兵无冲突。
- 宽度算成 right - left：左右边界本身不可达（更矮），宽必须减 1。
- 忘记尾部哨兵：递增序列如 [1,2,3] 全程无出栈，最大矩形（高 3 或高 2 宽 2 等）丢失。
- 弹出条件用 <= 还是 <：用 < 时等高柱子保留在栈中，等高段的结算由最后一根等高柱子完成，宽度仍正确；两种写法都对，但必须与自己理解的「边界语义」一致，混用会算错宽。

## 举一反三
- LeetCode 85 最大矩形：二维 01 矩阵版，逐行压缩成柱状图后调用本题，经典降维。
- LeetCode 42 接雨水：同样依赖两侧边界，可用单调栈或双指针。
- LeetCode 739/503：单调栈基础动作的同族题。
- LeetCode 1504 统计全 1 子矩形：85 的计数版，同一降维套路。
- 识别信号：「以某元素为瓶颈（最矮/最小）向两侧扩展求最优」→ 单调栈结算「两侧第一个更劣者」。

## 实际工程应用
直方图分析是资源规划的常见模型：广告位系统中把一排可选时段的价格/流量看成柱状图，评估「连续打包售卖的最大收益矩形」；云计算的容量规划里按时间轴的负载直方图评估「恒定水位能覆盖的最大连续区间」；图像处理中直方图均衡前的区域面积计算。85 题的逐行降维技巧更是把二维可行性问题压成一维单调栈的标准工程手法。`,
    keyPoints: [
      "核心刻画：最大矩形必以某根柱子为高，高度确定后宽度由「两侧第一个更矮者」唯一决定",
      "出栈结算公式：宽 = right - left - 1，right 是当前矮柱下标，left 是出栈后的新栈顶",
      "首尾双哨兵 0：头哨兵保证 left 总存在，尾哨兵强制所有柱子出栈结算，消灭全部特判",
      "均摊 O(n)：每柱入栈一次出栈至多一次，while 总次数 ≤ n",
    ],
    followUps: [
      "二维矩阵中找最大全 1 矩形怎么做？（LeetCode 85：每行把上方连续 1 的个数当柱高，逐行跑本题，O(行×列)）",
      "如果柱子宽度不相同呢？（高度 × (前缀和宽度差)，单调栈不变，宽度改用下标对应的前缀宽度）",
      "能用分治做吗？复杂度？（以区间最矮柱子分治，平均 O(n log n) 最坏 O(n^2)——递增序列退化，不如单调栈稳）",
    ],
    favorited: false,
  },
  {
    id: "algo-239",
    nodeId: "p1-stack-queue",
    question: `12. 滑动窗口最大值（LeetCode 239）
题意：给定数组 nums 和窗口大小 k，窗口从左向右每次滑动一位，返回每个窗口中的最大值组成的数组。
示例：输入 nums=[1,3,-1,-3,5,3,6,7], k=3 → 输出 [3,3,5,5,6,7]（共 n-k+1 个窗口）。
约束：1 ≤ n ≤ 10^5，1 ≤ k ≤ n。`,
    answer: `## 题意与约束分析
滑动窗口的在线最值查询。n 到 10^5，每个窗口暴力找最大是 O(nk) 最坏 10^10，必须均摊 O(1) 每窗口。堆可以做到 O(n log k)，但最优解是单调双端队列 O(n)——这是「滑动窗口 + 最值」的标准答案。

## 思路推导：从暴力到最优
暴力每窗扫描 O(k)。堆（大顶堆 + 懒删除）把每窗降到 O(log k)，但实现繁琐。最优解的结构观察：窗口滑动时，如果新元素 x 比窗口内某些旧元素大，那些旧元素「在 x 存活于窗口的整个期间都不可能是最大值」（它们比 x 小且比 x 先出窗）——它们可以永久淘汰。淘汰后剩下的候选者必然单调递减：维护一个双端队列，存下标，对应值从队首到队尾严格递减。新元素入队前，从队尾弹出所有 ≤ 它的元素（它们已被永久淘汰），再入队；窗口左界滑过时，若队首下标已出窗则弹出。此时队首恒为窗口最大值。均摊论证：每个下标入队一次、出队至多一次。

## 正确性要点
- 单调性不变式：队列内下标递增且对应值严格递减。入队前弹出所有 ≤ 新元素的队尾，保证了递减性；只从队首弹出出窗元素，保证下标在窗内。
- 队首即最大值：窗口内任何元素要么在队列里、要么已被某个比它大且比它晚进窗的元素淘汰（淘汰它的元素当时与它同窗或更晚进窗，故淘汰合法）；队列里下标最小（最先进窗）且值最大的就是队首。
- 出窗时机：新元素 i 入队前先弹出 dq[0] <= i-k 的队首，保证结算 ans 时队首在窗口 [i-k+1, i] 内。
- 均摊 O(n)：n 个下标各入队一次出队至多一次，总操作 ≤ 2n。

## 复杂度分析
- 时间：O(n) —— 对比暴力 O(nk)、堆 O(n log k)
- 空间：O(k) —— 队列最多存一个窗口的下标

## 代码实现
\`\`\`go
func maxSlidingWindow(nums []int, k int) []int {
	dq := []int{} // 单调递减双端队列：存下标，值从队首到队尾递减
	ans := make([]int, 0, len(nums)-k+1)
	for i := 0; i < len(nums); i++ {
		// 1. 队首出窗：下标落在窗口左界之外则弹出
		if len(dq) > 0 && dq[0] <= i-k {
			dq = dq[1:]
		}
		// 2. 维护单调性：≤ 新元素的队尾被永久淘汰
		for len(dq) > 0 && nums[dq[len(dq)-1]] <= nums[i] {
			dq = dq[:len(dq)-1]
		}
		dq = append(dq, i)
		// 3. 窗口形成后记录答案，队首即窗口最大
		if i >= k-1 {
			ans = append(ans, nums[dq[0]])
		}
	}
	return ans
}
\`\`\`

## 边界与易错点
- k=1：每个窗口只有自己，队尾淘汰总是成立，每步 ans 追加 nums[i]，正确。
- k=n：只产生一个窗口，i=n-1 时记录全队最大值，正确。
- 出窗判断写在结算之后或写成 dq[0] < i-k+1 的等价错误形式：窗口边界差一，ans 含窗外元素。
- 队尾淘汰条件用 < 而非 ≤：等值元素保留两份，功能仍正确但队首弹出出窗元素时可能误留一个等值旧下标……实际上 < 保留等值旧下标，旧下标先出窗被弹，语义仍对；≤ 更干净。两者都 AC，但要能讲清差别。
- 队首出窗在每步只弹一次：队列里最多一个下标出窗（窗口每步只滑一位），if 而非 for 即可。

## 举一反三
- LeetCode 1438 绝对差不超过限制的最长连续子数组：滑动窗口 + 单调队列维护最大最小值双队列。
- LeetCode 862 和至少为 K 的最短子数组：前缀和 + 单调队列（更难的变体）。
- LeetCode 1696 跳跃游戏 VI：DP + 滑动窗口最大值优化，单调队列在 DP 里的应用。
- LeetCode 84 柱状图中最大的矩形：单调结构家族的另一巅峰。
- 识别信号：「定长/不定长滑动窗口 + 窗口内最值」→ 单调双端队列；「淘汰逻辑：比新元素劣且先进窗的永不可能再当答案」。

## 实际工程应用
滑动窗口最值是流式监控的标准件：APM 系统实时计算「最近 5 分钟接口 P99 延迟」「最近 1 小时错误率峰值」，数据流无限到达，单调队列用 O(1) 均摊维护滚动极值。金融行情里滚动 N 周期最高/最低价（唐奇安通道）、网络设备里滑动窗口丢包率统计，底层都是它。时序数据库（InfluxDB/Prometheus）的连续查询引擎对滚动 min/max 的增量维护，思想与本题一脉相承。`,
    keyPoints: [
      "淘汰逻辑：比新元素小且先进窗的元素永不可能再当最大值——从队尾永久淘汰，这是 O(n) 的根源",
      "不变式：队列下标递增、对应值递减，队首恒为当前窗口最大值",
      "均摊 O(n)：每下标入队一次出队至多一次，总操作数 ≤ 2n，严格优于堆的 O(n log k)",
      "出窗检查每步只需 if 一次：窗口每步只滑一位，至多一个队首出窗",
    ],
    followUps: [
      "同时维护窗口最大值和最小值怎么做？（两个单调队列分别维护，1438 题就是双队列）",
      "数据流无限长且要求随时查询呢？（单调队列天然在线，每来一个元素 O(1) 均摊更新并给出当前窗口最值）",
      "分布式场景下数据分片到达，怎么合并各分片的窗口最大值？（单调队列不可直接合并；工程上用两段时间戳对齐后重新滑窗，或改用可合并的摘要结构如 t-digest 做近似）",
    ],
    favorited: false,
  },
];
