// lib/presets/algorithm-200/questions/p1-sort-binary.ts
// 「算法 200 题」知识库 · 节点 p1-sort-binary（排序与二分）深度题解，共 11 题
// 覆盖：二分答案入门 -> 左右边界 -> 旋转数组 -> 峰值/二分划分 -> 二维矩阵搜索 -> 区间合并 -> 自定义排序
// 设计思路：八段式结构（题意/推导/证明/复杂度/代码/易错/变体/工程），强调单调性识别信号与循环不变式，
// 让候选人能现场口述推导链条，而不是背答案。

import type { Question } from "../../../types";

export const P1_SORT_BINARY_QUESTIONS: Question[] = [
  {
    id: "algo-69",
    nodeId: "p1-sort-binary",
    question: `69. x 的平方根（LeetCode 69）\n题意：给定非负整数 x，返回 x 的算术平方根的整数部分（向下取整），不允许使用内置开方函数。\n示例：输入 x = 8 → 输出 2（8 的平方根约 2.828，舍去小数部分为 2）\n约束：0 <= x <= 2^31 - 1，答案不超过 46341`,
    answer: `## 题意与约束分析
求 floor(sqrt(x))，不能调库。x 上限 21 亿意味着：答案最大 46340，而 mid*mid 在 32 位 int 下会溢出，这是本题唯一的坑。答案空间很小，二分约 17 次迭代即可收敛。

## 思路推导：从暴力到最优
暴力从 0 逐个试 i 直到 i*i > x，返回 i-1，时间 O(√x)。卡点是枚举了大量无效候选。关键结构性质：f(i)=i² 关于 i 单调递增，谓词「i² ≤ x」在整数域呈 [真真真…假假假] 的单调形态——这正是二分答案的识别信号。对答案空间二分，找满足条件的最大值。防溢出两招：用 64 位乘法，或把 mid*mid <= x 改写成 mid <= x/mid，整数除法天然安全。

## 正确性要点
- 循环不变式：答案始终在 [lo, hi] 内，且 hi+1 起的值平方都大于 x；lo > hi 时 hi 就是最后一个满足 i² ≤ x 的整数。
- mid <= x/mid 时记录 ans=mid 并向右收缩，保证返回的是满足条件的最大值而非任意可行值。
- 整数域等价性：mid <= x/mid（向下取整除法）⟺ mid² <= x，两个方向都可由取整性质推出。

## 复杂度分析
- 时间 O(log x)：x=2^31 时约 31 次迭代，对比暴力 O(√x) 的 46341 次。
- 空间 O(1)：只用常数个变量。

## 代码实现
\`\`\`go
func mySqrt(x int) int {
    if x < 2 {
        return x // 0 和 1 的平方根是自身，也避免 mid=0 除零
    }
    lo, hi, ans := 1, x, 0
    for lo <= hi {
        mid := lo + (hi-lo)/2
        // 用除法代替乘法，规避 mid*mid 溢出
        if mid <= x/mid {
            ans = mid   // mid 是候选，继续向右找更大
            lo = mid + 1
        } else {
            hi = mid - 1
        }
    }
    return ans
}
\`\`\`

## 边界与易错点
- 溢出：mid*mid 在 x 接近 2^31 时溢出 32 位 int；Go 的 int 虽是 64 位，面试默认按 32 位防守，用 x/mid 或显式 int64。
- x=0/1 必须特判（或保证 mid >= 1），否则 x/mid 出现除零。
- 「记录 ans + lo=mid+1」与「收缩 hi 后返回 hi」两种写法等价，前者与二分答案题族模板统一，更值得背。

## 举一反三
- LeetCode 367 有效的完全平方数：同一单调谓词，判等即可；信号「谓词单调 → 二分」。
- LeetCode 875 爱吃香蕉的珂珂：从数轴二分升级为答案空间二分；信号「求最值且判定容易 → 二分答案」。
- LeetCode 278 第一个错误的版本：单调形态反转为 [假假…真真]，收缩方向对调。

## 实际工程应用
二分答案是探测型系统的通用骨架：限流器探测系统可承受的最大 QPS、视频 ABR 找不卡顿的最高码率，本质都是「单调谓词 + 低代价 check」的二分。`,
    keyPoints: [
      "谓词「i²≤x」呈真真假假单调形态，是二分答案的识别信号",
      "防溢出用 mid <= x/mid 除法改写，与乘法在整数域等价但免疫溢出",
      "记录候选 ans 并 lo=mid+1，求的是满足条件的最大值",
      "x<2 特判同时规避了 mid=0 时的除零",
    ],
    followUps: [
      "如果要求结果保留两位小数怎么做？（整数答案基础上继续二分 [ans, ans+1) 或用牛顿迭代）",
      "牛顿迭代法怎么写？与二分相比收敛速度和坑点各是什么？",
      "为什么 mid <= x/mid 与 mid² <= x 在整数域严格等价？给出两个方向的证明。",
    ],
    favorited: false,
  },
  {
    id: "algo-34",
    nodeId: "p1-sort-binary",
    question: `34. 在排序数组中查找元素的第一个和最后一个位置（LeetCode 34）\n题意：给定非递减数组 nums 和目标值 target，返回 target 的起始与结束下标，不存在返回 [-1,-1]，要求 O(log n)。\n示例：输入 nums = [5,7,7,8,8,10], target = 8 → 输出 [3,4]（8 首次出现在下标 3，最后出现在下标 4）\n约束：0 <= nums.length <= 10^5，元素与 target 在 [-10^9, 10^9]`,
    answer: `## 题意与约束分析
有序数组找 target 连续段端点，要求 O(log n)。非递减保证等值元素必连续，转化为求该段左右端点。

## 思路推导：从暴力到最优
暴力扫 O(n) 浪费「有序」；命中任一 target 再两侧扩，全 target 时退化 O(n)。最优解是两次边界二分：lower_bound(target) 得左边界，lower_bound(target+1)-1 得右边界，统一成 lower_bound 模板是本题最大价值。

## 正确性要点
- 不变式：[0, lo) 全 < target 且 [hi, n) 全 >= target，lo==hi 时 lo 即首个 >= target 的下标。
- 收缩配对：nums[mid] >= target 则 hi=mid，否则 lo=mid+1；lo<hi 配向下取整不死循环。
- 右边界 = lower_bound(target+1) - 1：首个大于 target 的前一位即最后出现处。

## 复杂度分析
- 时间 O(log n)：两次独立二分，对比暴力 O(n)。
- 空间 O(1)。

## 代码实现
\`\`\`go
func searchRange(nums []int, target int) []int {
    left := lowerBound(nums, target)
    if left == len(nums) || nums[left] != target {
        return []int{-1, -1} // 越界或值不等则不存在
    }
    return []int{left, lowerBound(nums, target+1) - 1}
}

// lowerBound 返回第一个 >= target 的下标
func lowerBound(nums []int, target int) int {
    lo, hi := 0, len(nums)
    for lo < hi {
        mid := lo + (hi-lo)/2
        if nums[mid] >= target {
            hi = mid // mid 可能是答案
        } else {
            lo = mid + 1
        }
    }
    return lo
}
\`\`\`

## 边界与易错点
- 死循环写法：lo<=hi 配错收缩方向会在两元素区间死循环；lo<hi + hi=mid 模板天然安全。
- 左边界越界：全小于 target 时 lo==n，先判 left==len(nums) 再访问 nums[left]。
- 空数组 lower_bound 返回 0，被第一个 if 拦截，无需特判。

## 举一反三
- LeetCode 35 搜索插入位置：lower_bound 本体；信号「找首个满足条件的位置」。
- LeetCode 278 第一个错误的版本：同模板，谓词换成 isBadVersion(mid)。
- LeetCode 611 有效三角形的个数：排序后固定两边、二分第三边，「排序+边界二分」组合拳。

## 实际工程应用
lower_bound 是有序存储引擎的基本操作：InnoDB 范围扫描定位、Prometheus 按时间戳定位压缩块、std::map range query，本质都是边界二分。`,
    keyPoints: [
      "左右边界统一成 lower_bound：第一个 >= target 的位置，一次模板复用两次",
      "右边界 = lower_bound(target+1) - 1，把「最后出现」转成「首个大于」",
      "不变式：[0,lo) 全 < target、[hi,n) 全 >= target，lo==hi 时收敛",
      "lo<hi 与 hi=mid 配对的模板天然免死循环，值得作为默认写法",
    ],
    followUps: [
      "为什么用 lo<hi 而不是 lo<=hi？两种模板的收缩方向和返回值分别怎么配对？",
      "全数组都是 target 时，「找到任一位置再向两侧扩」为什么退化到 O(n)？",
      "如何顺便统计 target 出现次数？（right - left + 1，注意不存在时的情况）",
    ],
    favorited: false,
  },
  {
    id: "algo-33",
    nodeId: "p1-sort-binary",
    question: `33. 搜索旋转排序数组（LeetCode 33）\n题意：升序且互异的数组在某个未知下标旋转（如 [0,1,2,4,5,6,7] 变成 [4,5,6,7,0,1,2]），在其中搜索 target，要求 O(log n)。\n示例：输入 nums = [4,5,6,7,0,1,2], target = 0 → 输出 4（0 位于下标 4）\n约束：1 <= nums.length <= 5000，元素互不相同，1 <= k(旋转次数) < nums.length`,
    answer: `## 题意与约束分析
旋转破坏全局有序，但保留关键结构：任取中点切一刀，至少一半仍严格有序，可在不完整的有序性上直接二分。

## 思路推导：从暴力到最优
暴力扫 O(n) 没用上「原数组有序」；先找旋转点再普通二分绕路。更优一次二分直取：比较 nums[lo] 与 nums[mid] 判哪半边有序，target 在有序半边值域内就收缩过去，否则去另半边。

## 正确性要点
- 半序引理：nums[lo] <= nums[mid] 时 [lo, mid] 严格升序（旋转点只在一侧，互异保证另半边有序）。
- 收缩不变式：每轮后 target 若存在必在新 [lo, hi] 内——端点值判定不在有序半边时才舍弃它，成员判定精确。
- 等号归属：nums[lo] <= nums[mid] 带等号覆盖 lo==mid 的两元素情形，否则 [1,3] 走错分支。

## 复杂度分析
- 时间 O(log n)：每轮舍弃一半，对比暴力 O(n)。
- 空间 O(1)。

## 代码实现
\`\`\`go
func search(nums []int, target int) int {
    lo, hi := 0, len(nums)-1
    for lo <= hi {
        mid := lo + (hi-lo)/2
        if nums[mid] == target {
            return mid
        }
        if nums[lo] <= nums[mid] { // 左半有序
            if nums[lo] <= target && target < nums[mid] {
                hi = mid - 1
            } else {
                lo = mid + 1
            }
        } else { // 右半有序
            if nums[mid] < target && target <= nums[hi] {
                lo = mid + 1
            } else {
                hi = mid - 1
            }
        }
    }
    return -1
}
\`\`\`

## 边界与易错点
- 半序判定漏等号：nums[lo] < nums[mid] 时，长度 2 的数组 lo==mid 会把有序左半误判为无序。
- target 的区间判断必须对应有序半边的 [nums[lo], nums[mid]) 与 (nums[mid], nums[hi]]，写反会漏解。
- 与含重复版本（LC 81）混用：端点相等无法判序需 lo++，本题互异不需要。

## 举一反三
- LeetCode 81 搜索旋转排序数组 II：允许重复，加 lo++ 处理歧义，最坏 O(n)。
- LeetCode 153 寻找旋转排序数组中的最小值：同一结构，「找 target」换成「找分界点」。
- LeetCode 1095 山脉数组中查找目标值：先找峰再两侧有序段二分，半序思想组合版。

## 实际工程应用
半有序思想覆盖环形缓冲：Kafka segment 环形切分、CPU 环形运行队列、循环 trace buffer，定位都是先判落在哪个有序段、再段内二分。`,
    keyPoints: [
      "旋转数组任切一刀至少一半有序，这是二分可行性的根基",
      "nums[lo] <= nums[mid] 判左半有序，等号覆盖 lo==mid 的两元素情形",
      "target 是否在有序半边用两个端点值精确判定，在则收缩、不在则去另半",
      "含重复元素时端点相等失去判别力，需 lo++ 退化，最坏 O(n)",
    ],
    followUps: [
      "数组含重复元素时代码要改哪里？复杂度会退化到多少？",
      "先二分找旋转点还原数组、再普通二分，与一次二分相比各有什么优劣？",
      "证明：每轮收缩后 target 若存在必在新区间内。",
    ],
    favorited: false,
  },
  {
    id: "algo-153",
    nodeId: "p1-sort-binary",
    question: `153. 寻找旋转排序数组中的最小值（LeetCode 153）\n题意：升序且互异的数组旋转 1 到 n 次后，找出其中的最小值，要求 O(log n)。\n示例：输入 nums = [3,4,5,1,2] → 输出 1（原数组 [1,2,3,4,5] 旋转后最小值为 1）\n约束：1 <= nums.length <= 5000，元素互不相同`,
    answer: `## 题意与约束分析
最小值即旋转分界点：它左侧（若存在）是原数组后半段的大值，右侧升序直到末尾，且它严格小于前一个元素。n 最大 5000，O(log n) 要求封杀线性扫描。

## 思路推导：从暴力到最优
暴力遍历找 nums[i] < nums[i-1] 的位置 O(n)，卡点是浪费旋转结构。观察：数组被切成两个升序段，且左段所有值大于右段所有值。取 mid 与右端点 nums[hi] 比较：nums[mid] > nums[hi] 说明 mid 在左段，最小值严格在右半，lo=mid+1；否则 mid 在右段，最小值是 mid 或在其左，hi=mid。区间每次减半，收敛到唯一候选。

## 正确性要点
- 为什么和 nums[hi] 比而不是 nums[lo]：nums[mid] > nums[lo] 无法区分「mid 在左段」与「整个区间已升序」；而 nums[mid] > nums[hi] 当且仅当 mid 在左段（左段尾部必大于右段任意值），判定完备无歧义。
- 循环不变式：最小值始终位于 [lo, hi] 闭区间；hi=mid 不丢解，因为此时 nums[mid] 本身就是候选最小值。
- 终止 lo==hi 时区间只剩一个元素，即为最小值；数组未旋转时谓词恒为假，hi 一路左移收敛到 nums[0]，自动正确。

## 复杂度分析
- 时间 O(log n)，对比暴力 O(n)。
- 空间 O(1)。

## 代码实现
\`\`\`go
func findMin(nums []int) int {
    lo, hi := 0, len(nums)-1
    for lo < hi {
        mid := lo + (hi-lo)/2
        // 与右端点比较：mid 大于右端点说明最小值在右半
        if nums[mid] > nums[hi] {
            lo = mid + 1
        } else {
            hi = mid // nums[mid] 本身就是候选，不能排除
        }
    }
    return nums[lo]
}
\`\`\`

## 边界与易错点
- 收缩配对错误：lo<hi 模板里 mid 向下取整，写成 lo=mid 会在两元素区间死循环；本题必须 lo=mid+1 配 hi=mid。
- 与左端点比较是经典错法：nums[mid] > nums[lo] 时无法判断 mid 在左段还是整体升序。
- 含重复元素（LeetCode 154）时 nums[mid]==nums[hi] 失去判别力，需 hi-- 去重，最坏 O(n)。

## 举一反三
- LeetCode 154 寻找旋转排序数组中的最小值 II：加重复元素，hi-- 处理歧义。
- LeetCode 33 搜索旋转排序数组：同一结构逆向使用，先判半序再判 target。
- 旋转点下标即旋转次数，识别信号「两段单调拼接 + 找拼接缝」。

## 实际工程应用
环形结构里定位「最老一条记录」就是找旋转点：Kafka segment 的起始 offset、环形 trace buffer 的逻辑头、时间轮定时器的当前槽位，本质都是同一个二分。`,
    keyPoints: [
      "最小值即旋转分界点，左段所有值大于右段所有值是判定基础",
      "与右端点 nums[hi] 比较才完备：nums[mid] > nums[hi] 当且仅当 mid 在左段",
      "与左端点比较无法区分左段与整体升序，是高频错误写法",
      "hi=mid 不丢解：nums[mid] <= nums[hi] 时 mid 本身是候选最小值",
    ],
    followUps: [
      "为什么不能和左端点比较？给一个具体反例。",
      "含重复元素（LC 154）为什么要 hi--？最坏复杂度是多少？",
      "旋转了多少次怎么求？（最小值下标就是旋转次数）",
    ],
    favorited: false,
  },
  {
    id: "algo-162",
    nodeId: "p1-sort-binary",
    question: `162. 寻找峰值（LeetCode 162）\n题意：数组 nums 满足相邻元素互不相等，峰值是严格大于左右邻居的元素，数组边界外视为负无穷。返回任意一个峰值的下标，要求 O(log n)。\n示例：输入 nums = [1,2,3,1] → 输出 2（3 大于两侧邻居 2 和 1，是峰值，下标为 2）\n约束：1 <= nums.length <= 1000，nums[i] != nums[i+1]，可能存在多个峰值`,
    answer: `## 题意与约束分析
数组无序但相邻互异，边界视为 -∞。「返回任一峰值」和「边界 -∞」是题目的两张许可证：存在性天然成立，且只需定位一个。要求 O(log n)，n 仅 1000，但数据小不代表能混——面试官要的是对「无序也能二分」的理解。

## 思路推导：从暴力到最优
暴力逐位置验证 O(n)，卡点是浪费了比较带来的方向信息。关键观察：若 nums[mid] < nums[mid+1]，正处于上坡段，沿该方向要么持续上升到数组末尾（末尾配 -∞ 成峰）、要么中途拐下（首个下降点即峰），右侧必含峰值；对称地，nums[mid] > nums[mid+1] 时左侧（含 mid）必含峰值。于是二分「往高处爬」，每步舍弃不可能无峰的一侧。

## 正确性要点
- 存在性证明：上坡方向的序列只有两种结局——单调到边界（边界配 -∞ 成峰）或出现下降（首次下降点即峰），故上坡侧必含峰值。
- 循环不变式：[lo, hi] 内始终存在峰值；hi=mid 分支保留 mid，因为 nums[mid] > nums[mid+1] 时 mid 本身可能就是峰。
- 相邻互异保证比较永不平局，二分方向永远明确；lo==hi 时即得一个峰值下标。

## 复杂度分析
- 时间 O(log n)，对比暴力 O(n)。
- 空间 O(1)。

## 代码实现
\`\`\`go
func findPeakElement(nums []int) int {
    lo, hi := 0, len(nums)-1
    for lo < hi {
        mid := lo + (hi-lo)/2
        if nums[mid] < nums[mid+1] {
            lo = mid + 1 // 上坡方向必有峰值
        } else {
            hi = mid // 下坡方向（含 mid）必有峰值
        }
    }
    return lo
}
\`\`\`

## 边界与易错点
- mid+1 越界只在 lo<=hi 模板下发生；lo<hi 时 mid < hi <= n-1，mid+1 恒合法。
- 误以为要找全局最大值：题目接受任一局部峰，二分返回的峰不唯一但合法。
- 相邻元素若允许相等，比较平局时方向失效，算法前提被破坏——题面给互异不是随手写的。

## 举一反三
- LeetCode 852 山脉数组的峰顶索引：峰值唯一且先升后降，同一模板的特例。
- LeetCode 1095 山脉数组中查找目标值：先找峰再两侧有序段二分，组合用法。
- 识别信号：「数组无序但局部比较有方向信息 + 要求 log n → 往有利方向收缩的二分」。

## 实际工程应用
「往高处爬」是梯度思想的离散版：超参搜索的爬山法、游戏 AI 地形寻峰、无线功率控制的信标爬坡算法，都利用局部比较快速逼近极值点，无需全局有序。`,
    keyPoints: [
      "边界 -∞ 加相邻互异保证峰值必然存在，存在性不用证给面试官之外的人",
      "nums[mid] < nums[mid+1] 时右侧必含峰：要么上坡到边界、要么拐点即峰",
      "二分的本质是「区间内含解」而非「数组有序」，无序数组照样二分",
      "lo<hi 模板下 mid+1 访问永不越界，模板选择直接消除一类 bug",
    ],
    followUps: [
      "数组无序为什么还能二分？二分的真正前提是什么？",
      "如果要求返回数值最大的峰值，这个算法还适用吗？怎么办？",
      "相邻元素允许相等时会出什么问题？如何修复？",
    ],
    favorited: false,
  },
  {
    id: "algo-4",
    nodeId: "p1-sort-binary",
    question: `4. 寻找两个正序数组的中位数（LeetCode 4）\n题意：给定两个升序数组 nums1、nums2，长度分别为 m、n，求它们合并后的中位数，要求 O(log(m+n))。\n示例：输入 nums1 = [1,3], nums2 = [2] → 输出 2.00000（合并为 [1,2,3]，中位数是 2）\n约束：0 <= m, n <= 1000，1 <= m+n <= 2000，元素在 [-10^6, 10^6]`,
    answer: `## 题意与约束分析
log 级要求封杀 O(m+n) 归并。中位数即划分：切分使左半 <= 右半，且左半个数 (m+n+1)/2。

## 思路推导：从暴力到最优
暴力归并 O(m+n) 过不了 log 要求。视角转换：nums1 左段取 i 个、nums2 左段取 j=(m+n+1)/2-i 个，合法当且仅当 l1<=r2 且 l2<=r1。i 增大时 l1 增、l2 减，判定对 i 单调——可对 i 二分。

## 正确性要点
- i+j=(m+n+1)/2 保证左半多 0 或 1 个：奇数取 max(l1,l2)，偶数取两半极值平均。
- 交叉判定 l1<=r2 且 l2<=r1 等价「左半最大值 <= 右半最小值」即中位数定义。
- 边界段为空用无穷哨兵填充，判定式依然成立，无需特判。

## 复杂度分析
- 时间 O(log min(m,n))：对短数组二分。
- 空间 O(1)。

## 代码实现
\`\`\`go
func findMedianSortedArrays(nums1 []int, nums2 []int) float64 {
    if len(nums1) > len(nums2) { nums1, nums2 = nums2, nums1 }
    m, n := len(nums1), len(nums2)
    lo, hi := 0, m
    for lo <= hi {
        i := lo + (hi-lo)/2
        j := (m+n+1)/2 - i
        l1, r1, l2, r2 := -1e18, 1e18, -1e18, 1e18
        if i > 0 { l1 = float64(nums1[i-1]) }
        if i < m { r1 = float64(nums1[i]) }
        if j > 0 { l2 = float64(nums2[j-1]) }
        if j < n { r2 = float64(nums2[j]) }
        if l1 <= r2 && l2 <= r1 {
            if l2 > l1 { l1 = l2 }
            if r2 < r1 { r1 = r2 }
            if (m+n)%2 == 1 { return l1 }
            return (l1 + r1) / 2
        }
        if l1 > r2 { hi = i - 1 } else { lo = i + 1 }
    }
    return 0
}
\`\`\`

## 边界与易错点
- 对长数组二分会让 j 为负越界；入口先交换保证 nums1 较短。
- 奇偶分支写反或偶数忘除 2.0，结果差 0.5 或取错半边。
- 空数组由无穷哨兵自然覆盖，不要画蛇添足加特判。

## 举一反三
- 第 K 小通版：中位数是其特例，通用做法每次排除 k/2 个元素的减治。
- LeetCode 378 有序矩阵中第 K 小：对值域二分加计数，「答案单调 → 二分答案」。
- 信号：两个有序结构加 log 要求 → 二分划分点而非归并数据。

## 实际工程应用
二分划分是分布式归并核心：OLAP 分位数估算、MapReduce 边界采样、时序库跨 shard 求 P99，都是只二分划分点不合并数据。`,
    keyPoints: [
      "中位数转化为划分问题：i+j=(m+n+1)/2 且左半最大值 <= 右半最小值",
      "合法性对切分点 i 单调，可对短数组二分，O(log min(m,n))",
      "始终对短数组二分，保证 j 落在 [0,n] 不越界",
      "正负无穷哨兵统一处理边界段为空，消除四个特判分支",
    ],
    followUps: [
      "为什么是 (m+n+1)/2 而不是 (m+n)/2？",
      "改成求两个有序数组第 K 小（K 任意）怎么做？",
      "什么时候对值域二分更优？和二分划分相比适用条件是什么？",
    ],
    favorited: false,
  },
  {
    id: "algo-875",
    nodeId: "p1-sort-binary",
    question: `875. 爱吃香蕉的珂珂（LeetCode 875）\n题意：珂珂每小时吃一堆香蕉，速度为 k（一堆不足 k 根也算一小时），求在 h 小时内吃完所有堆的最小整数速度 k。\n示例：输入 piles = [3,6,7,11], h = 8 → 输出 4（速度 4 时耗时 1+2+2+3=8 小时刚好吃完）\n约束：1 <= piles.length <= 10^4，piles[i] <= 10^9，piles.length <= h <= 10^9`,
    answer: `## 题意与约束分析
求最小可行速度 k。堆数不超过 h 保证必有解；答案上界即最大堆，再快无收益。每堆最大 1e9，暴力枚举 k 不可行。

## 思路推导：从暴力到最优
暴力从 k=1 逐个试、每次 O(n) 算总耗时，最坏约 1e13 次操作，超时。核心性质：总耗时 T(k)=Σ⌈p/k⌉ 关于 k 单调不增，谓词「k 可行」呈 [假假…真真]——判定单调且 check 易得，二分答案标准信号。对 [1, max(piles)] 二分找第一个可行值。

## 正确性要点
- 单调性：⌈p/k⌉ 每项关于 k 单调不增，和亦然，故可行解集是后缀 [k*, +∞)，二分求后缀左端点。
- 循环不变式：[lo, hi] 内含最小可行速度；check(mid) 真则 hi=mid（mid 可行不能丢），假则 lo=mid+1。
- 向上取整恒等式：(p+k-1)/k 对正整数精确等于 ⌈p/k⌉，无浮点精度风险。

## 复杂度分析
- 时间 O(n log maxPile)：check O(n)，二分约 30 轮，对比暴力 O(n × maxPile)。
- 空间 O(1)。

## 代码实现
\`\`\`go
func minEatingSpeed(piles []int, h int) int {
    lo, hi := 1, 0
    for _, p := range piles {
        if p > hi {
            hi = p // 上界取最大堆
        }
    }
    for lo < hi {
        mid := lo + (hi-lo)/2
        if canFinish(piles, mid, h) {
            hi = mid // mid 可行，尝试更慢
        } else {
            lo = mid + 1
        }
    }
    return lo
}

func canFinish(piles []int, k, h int) bool {
    hours := 0
    for _, p := range piles {
        hours += (p + k - 1) / k // 向上取整
    }
    return hours <= h
}
\`\`\`

## 边界与易错点
- 取整写错：p/k 是向下取整，必须 (p+k-1)/k；浮点 Ceil 在 1e9 附近有精度风险。
- 下界取 0 会在 check 里除零，k 最小为 1。
- hours 累加最坏约 1e13，Go 64 位 int 安全，Java/C++ 必须 long，溢出静默变负直接挂。

## 举一反三
- LeetCode 1011 在 D 天内送达包裹的能力：同模板，check 改顺序装箱。
- LeetCode 410 分割数组的最大值：二分「最大子段和的最小值」，check 用贪心计数段数。
- LeetCode 1283 使结果不超过阈值的最小除数：check 函数与本题同构。
- 通用信号：答案单调 + 判定容易 + 求最值 → 二分答案。

## 实际工程应用
二分答案是容量规划的标准武器：限流器探测最大稳定 QPS、视频 ABR 选不卡顿的最高码率、CI 并发度取最小够用值，套路一致。`,
    keyPoints: [
      "总耗时关于速度单调不增，可行解集是后缀，二分求后缀左端点",
      "check 的核心是向上取整累加：(p+k-1)/k，避开浮点精度坑",
      "上界取 max(piles) 已足够紧，取 sum 会浪费迭代",
      "二分答案三要素：答案单调、check 易得、求最值",
    ],
    followUps: [
      "怎么严格证明答案空间具有单调性？",
      "hours 累加在 Java 里为什么要用 long？溢出后会发生什么现象？",
      "如果每堆可以分多次吃（不足 k 根不占满一小时），模型和算法怎么变？",
    ],
    favorited: false,
  },
  {
    id: "algo-74",
    nodeId: "p1-sort-binary",
    question: `74. 搜索二维矩阵（LeetCode 74）\n题意：m x n 矩阵每行从左到右升序，且每行首元素大于上一行末元素，判断 target 是否存在，要求 O(log(mn))。\n示例：输入 matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 3 → 输出 true（3 在矩阵第一行）\n约束：1 <= m, n <= 100，元素与 target 在 [-10^4, 10^4]`,
    answer: `## 题意与约束分析
行内有序加行间首尾衔接有序，意味着矩阵按行拼接后是一条完整升序长数组——这是它与 LeetCode 240 的本质区别，也是一次二分可行性的全部依据。

## 思路推导：从暴力到最优
暴力逐元素扫 O(mn)，改进版每行二分 O(m log n)。但既然展开后全局有序，最优解是把二维压成一维：对 [0, m*n-1] 做标准二分，用 mid/n、mid%n 映射回行列。也可先二分定位行、再在行内二分，两次二分同阶，但一次二分的代码最短、边界最少，是面试首选写法。

## 正确性要点
- 全局有序性：每行升序，且 row[i][0] > row[i-1][n-1]，归纳可得展开序列严格升序，标准二分的前提成立。
- 坐标映射：mid/n 是行号、mid%n 是列号，除法取余是 [0, mn) 到矩阵下标的一一映射，无遗漏无重复。
- 标准二分不变式：target 若存在必在 [lo, hi] 内，每轮严格减半，lo>hi 时不存在。

## 复杂度分析
- 时间 O(log(mn)) = O(log m + log n)，与两次二分同阶；对比暴力 O(mn)。
- 空间 O(1)。

## 代码实现
\`\`\`go
func searchMatrix(matrix [][]int, target int) bool {
    m, n := len(matrix), len(matrix[0])
    lo, hi := 0, m*n-1 // 把矩阵按行展开成一维
    for lo <= hi {
        mid := lo + (hi-lo)/2
        v := matrix[mid/n][mid%n] // 一维下标映射回二维
        if v == target {
            return true
        } else if v < target {
            lo = mid + 1
        } else {
            hi = mid - 1
        }
    }
    return false
}
\`\`\`

## 边界与易错点
- 映射写反：mid/n 是行不是列，非方阵里写反直接查错位置还未必报错。
- 乘积溢出：m、n 均不超 100 时安全，但泛化到大矩阵 m*n 可能溢出 32 位 int，需 64 位下标。
- 把本题模板误搬到 LeetCode 240：那里行首不保证大于上一行行尾，展开后非全局有序，一次二分整个失效。

## 举一反三
- LeetCode 240 搜索二维矩阵 II：只有行列各自有序，必须右上角排除法；两题对比是经典考点。
- LeetCode 378 有序矩阵中第 K 小的元素：值域二分加行内计数，矩阵有序性的另一类用法。
- 识别信号：「矩阵首尾衔接全局有序 → 压平一维二分」。

## 实际工程应用
分块有序存储即本结构：LSM-Tree 的 SSTable 块内有序且块间 key 区间不重叠、Parquet 的 row group，查询时都是「块号乘块大小加块内偏移」的一维化二分定位。`,
    keyPoints: [
      "行内有序加行间首尾衔接，展开后是一条完整升序数组，一次二分可行",
      "一维下标映射二维：行=mid/n、列=mid%n，标准除法取余",
      "O(log(mn)) 与先二分行再二分列同阶，但单二分代码最短边界最少",
      "与 LC 240 的本质区别在行间是否有全局序，一字之差算法完全不同",
    ],
    followUps: [
      "这题和 240 的题面差异到底在哪一句话？为什么算法因此不同？",
      "m 和 n 很大时 m*n 的下标计算有什么坑？怎么规避？",
      "如果只能两次二分（先定位行再定位列），代码怎么写？和一次二分比优劣？",
    ],
    favorited: false,
  },
  {
    id: "algo-240",
    nodeId: "p1-sort-binary",
    question: `240. 搜索二维矩阵 II（LeetCode 240）\n题意：m x n 矩阵每行从左到右升序、每列从上到下升序，但行首不保证大于上一行行尾，搜索 target 是否存在。\n示例：输入 matrix = [[1,4,7,11],[2,5,8,12],[3,6,9,16],[10,13,14,17]], target = 5 → 输出 true（5 在第二行）\n约束：1 <= m, n <= 300，元素与 target 在 [-10^9, 10^9]`,
    answer: `## 题意与约束分析
行列各自有序，但行间无全局序——LeetCode 74 的「压平一维二分」在此整个失效，必须用新的结构性质。m、n 最大 300，O(mn) 暴力 9e4 能过数据，但面试官要的是 O(m+n) 的结构化解法。

## 思路推导：从暴力到最优
暴力 O(mn)，改进每行二分 O(m log n)。观察四个角：左上角行最小列也最小、右下角都最大，从这两角出发比较一次得不到排除结论。换到右上角：它是当前行的最大值、当前列的最小值——比 target 大则整列（向下递增）都大，排除该列；比 target 小则整行（向右递增）都小，排除该行。每步排除一整行或一整列，最多 m+n 步收敛。

## 正确性要点
- 排除法不变式：排除第 col 列时已知 matrix[row][col] > target 且该列向下更大；排除第 row 行时已知 matrix[row][col] < target 且该行向左更小——被排除的行列绝不含 target。
- 每轮迭代 row+1 或 col-1，总步数不超过 m+n，必然终止；剩余子矩阵始终保持「target 若存在必在其中」。
- 起点的本质是鞍点：右上角同时是行序与列序的极值交点，一次比较能推出整行或整列的结论；左上、右下两个角两个方向的结论互相矛盾，不可用。

## 复杂度分析
- 时间 O(m+n)：每步排除一整行或一整列；对比暴力 O(mn)、逐行二分 O(m log n)。
- 空间 O(1)。

## 代码实现
\`\`\`go
func searchMatrix(matrix [][]int, target int) bool {
    m, n := len(matrix), len(matrix[0])
    row, col := 0, n-1 // 从右上角出发
    for row < m && col >= 0 {
        v := matrix[row][col]
        if v == target {
            return true
        } else if v > target {
            col-- // 当前列向下更大，排除整列
        } else {
            row++ // 当前行向左更小，排除整行
        }
    }
    return false
}
\`\`\`

## 边界与易错点
- 起点选错：从左上或右下出发，两个方向的排除结论互相矛盾，算法直接失效。
- 循环条件 row < m && col >= 0 缺一不可，否则越界。
- 误套 LeetCode 74 的一维二分：本矩阵行间无全局序，压平后非单调，是面试高频陷阱。

## 举一反三
- LeetCode 74 搜索二维矩阵：全局有序版本，压平二分；两题对比是必考对。
- LeetCode 378 有序矩阵中第 K 小的元素：利用「左上半 <= x、右下半 >= x」的同类结构做值域二分计数。
- 识别信号：「行列各自有序加搜索或计数 → 右上角排除法或对角线分治」。

## 实际工程应用
排除法即多维索引裁剪：时序数据库按 (时间, 标签) 双有序存块，查询沿时间维淘汰整块、标签维淘汰整列；电商 SKU 的 (价格, 上架时间) 双序视图同理。`,
    keyPoints: [
      "只有行列各自有序、行间无全局序，一维压平二分不适用",
      "右上角是鞍点：行内最大、列内最小，一次比较可排除整行或整列",
      "每步排除一行或一列，最多 m+n 步，O(m+n) 优于逐行二分",
      "不变式：被排除的行列绝不含 target，剩余子矩阵保持解的存在性",
    ],
    followUps: [
      "为什么从左上角或右下角出发不行？画个 2x2 反例。",
      "如果每行升序、每列降序，起点应该选哪个角？",
      "对角线分治（沿对角线二分再递归两个子矩阵）的思路和复杂度是什么？",
    ],
    favorited: false,
  },
  {
    id: "algo-56",
    nodeId: "p1-sort-binary",
    question: `56. 合并区间（LeetCode 56）\n题意：给定区间集合 intervals，合并所有重叠的区间（端点相接也算重叠），返回合并后互不重叠的区间数组。\n示例：输入 intervals = [[1,3],[2,6],[8,10],[15,18]] → 输出 [[1,6],[8,10],[15,18]]（[1,3] 与 [2,6] 重叠合并为 [1,6]）\n约束：1 <= intervals.length <= 10^4，intervals[i].length == 2，端点在 [0, 10^4]`,
    answer: `## 题意与约束分析
合并所有重叠区间，端点相接也算重叠；重叠有传递性，必须一次合并到位。n 最大 1e4，O(n^2) 两两合并边缘且易错。

## 思路推导：从暴力到最优
暴力两两比较反复合并，O(n^2) 且状态混乱。破局点是排序：按左端点升序后，新区间只需与结果末尾区间比较——更早区间的右端点够不到它。一趟扫描：重叠就扩展末尾右端点，否则追加新段，瓶颈全在排序。

## 正确性要点
- 排序不变式：处理到 cur 时，结果末尾区间 last 是唯一可能与 cur 重叠的未完结区间——其余区间右端点都小于 cur[0]。
- 合并规则 cur[0] <= last[1] 当且仅当重叠或相接；右端点取 max，否则 [1,4] 吞 [2,3] 会把右端点改小。
- 每个区间恰好处理一次，输出天然按左端点有序且两两不交。

## 复杂度分析
- 时间 O(n log n)：瓶颈在排序，扫描仅 O(n)；对比暴力 O(n^2)。
- 空间 O(n)：结果数组；不计输出则只有排序栈 O(log n) 到 O(n)。

## 代码实现
\`\`\`go
import "sort"

func merge(intervals [][]int) [][]int {
    sort.Slice(intervals, func(i, j int) bool {
        return intervals[i][0] < intervals[j][0] // 按左端点排序
    })
    res := [][]int{intervals[0]}
    for _, cur := range intervals[1:] {
        last := res[len(res)-1]
        if cur[0] <= last[1] { // 与末尾区间重叠或相接
            if cur[1] > last[1] {
                last[1] = cur[1] // 只扩展右端点，注意取 max
            }
        } else {
            res = append(res, cur)
        }
    }
    return res
}
\`\`\`

## 边界与易错点
- 右端点更新漏取 max：直接 last[1] = cur[1] 在嵌套区间 [1,4]+[2,3] 下把右端点改小。
- 重叠判定漏等号：端点相接 [1,2][2,3] 按题意必须合并，写成 < 就漏。
- 在原数组上边遍历边合并会下标错位，用独立结果数组最干净。

## 举一反三
- LeetCode 57 插入区间：输入已有序，三段式（左无交、中合并、右无交）O(n)。
- LeetCode 253 会议室 II：同一排序骨架配最小堆统计并发峰值。
- LeetCode 435 无重叠区间：排序后贪心保留右端点最小者，区间调度对偶问题。
- 识别信号：「区间重叠、覆盖、调度 → 先按端点排序再扫描」。

## 实际工程应用
区间合并是系统编程日常：合并日历忙段求空闲窗口、内存分配器合并相邻空闲块、防火墙 IP 段去重、基因 reads 覆盖合并，同一骨架。`,
    keyPoints: [
      "按左端点排序后只需与结果末尾区间比较，重叠关系被局部化",
      "重叠判定含等号：端点相接按题意也算重叠",
      "右端点更新必须取 max，否则嵌套区间把右端点改小",
      "时间瓶颈全在排序 O(n log n)，扫描合并本身 O(n)",
    ],
    followUps: [
      "如果区间已按左端点有序，插入一个新区间怎么 O(n) 完成？（LC 57）",
      "如何求合并后的总覆盖长度？",
      "求任意时刻最大重叠层数（同时进行会议数）怎么做？为什么用堆或差分？",
    ],
    favorited: false,
  },
  {
    id: "algo-179",
    nodeId: "p1-sort-binary",
    question: `179. 最大数（LeetCode 179）\n题意：给定非负整数数组，重新排列拼接使拼出的十进制数最大，以字符串返回（结果可能非常大）。\n示例：输入 nums = [3,30,34,5,9] → 输出 "9534330"（按 9,5,34,3,30 顺序拼接得到最大数）\n约束：1 <= nums.length <= 100，0 <= nums[i] <= 10^9`,
    answer: `## 题意与约束分析
重排拼接使结果最大，字符串返回——既规避了数值溢出，也暗示结果可能极长。n 仅 100，但全排列 n! 不可行，必须找到元素间的全序关系。

## 思路推导：从暴力到最优
暴力枚举全排列取最大，n=100 是天文数字。卡点是局部选择与全局最优的关系不明。关键洞察：相邻两个数 a、b 的先后只需比较拼接串 a+b 与 b+a 的字典序——若 a+b > b+a 则 a 必须排在 b 之前，否则交换二者结果更大（冒泡排序的交换论证）。于是定义比较器 cmp(a,b) = (a+b) > (b+a)，对字符串数组排序后顺次拼接。单次比较只拼两个短串，代价常数级。

## 正确性要点
- 交换论证：任一最优排列中若存在相邻逆序对（cmp 意义下），交换后拼接结果严格更大，与最优矛盾；故最优排列必为 cmp 有序序列。
- 全序性：cmp 满足传递性（比较 a+b 与 b+a 等价于比较两串无限循环重复后的字典序，可证为全序），因此排序结果唯一确定全局最优，不是局部贪心幻觉。
- 前导零特判：排序后首元素为 "0" 说明全数组皆零，答案为 "0" 而非一串零。

## 复杂度分析
- 时间 O(n log n 乘 L)：L 为数字串长（不超 10），即 O(n log n) 级别；对比暴力 O(n!)。
- 空间 O(nL)：存放字符串数组。

## 代码实现
\`\`\`go
import (
    "sort"
    "strconv"
    "strings"
)

func largestNumber(nums []int) string {
    strs := make([]string, len(nums))
    for i, v := range nums {
        strs[i] = strconv.Itoa(v)
    }
    sort.Slice(strs, func(i, j int) bool {
        // 比较拼接结果：a+b 与 b+a 谁大谁排前
        return strs[i]+strs[j] > strs[j]+strs[i]
    })
    if strs[0] == "0" { // 全是零的特判
        return "0"
    }
    return strings.Join(strs, "")
}
\`\`\`

## 边界与易错点
- 比较器写成数值比较：3 与 30 数值上 3 < 30，但 "330" > "303"，3 应排前——纯数值或纯字典序排序都必挂。
- 忘记全零特判：[0,0] 会输出 "00" 而非 "0"。
- a+b 与 b+a 是字符串拼接，必须先转 string 再比较，别在整数域里比。

## 举一反三
- 变体「拼最小数」：比较器反向，且要额外处理结果前导零的剥离。
- LeetCode 937 重新排列日志文件：多键自定义排序，同一「自定义全序」骨架。
- 识别信号：「元素两两组合影响全局目标 → 定义相邻比较器、证明交换更优、排序」。

## 实际工程应用
自定义全序比较器无处不在：版本号排序（1.9 小于 1.10 须按段比）、文件管理器自然排序（img2 小于 img10）、多币种金额排序，都是「业务语义重定义序关系」的同构问题。`,
    keyPoints: [
      "相邻元素的先后只需比较拼接串 a+b 与 b+a 的字典序",
      "交换论证：最优排列中相邻违反比较器则交换更优，矛盾",
      "比较器是全序关系（传递性可证），排序结果即全局最优而非贪心幻觉",
      "全零特判：首元素为 \"0\" 直接返回 \"0\"，否则输出一串零",
    ],
    followUps: [
      "怎么证明这个比较器满足传递性？（提示：比较无限循环重复串的字典序）",
      "改成拼最小数，比较器怎么改？前导零怎么处理？",
      "为什么按数值大小或按普通字符串字典序排序都是错的？各给反例。",
    ],
    favorited: false,
  },
];