// lib/presets/algorithm-200/questions/p1-bit-math.ts
// 「算法 200 题」知识库 · 节点 p1-bit-math（位运算与数学）深度题解，共 8 题
// 覆盖：异或消重 -> 位计数 -> lowbit 技巧 -> 2 的幂判定 -> 快速幂 -> 整数反转/回文/竖式加一
// 设计思路：八段式结构（题意/推导/证明/复杂度/代码/易错/变体/工程），强调位模式的代数证明，
// 让候选人理解「为什么这一位运算成立」，而不是背 trick。

import type { Question } from "../../../types";

export const P1_BIT_MATH_QUESTIONS: Question[] = [
  {
    id: "algo-136",
    nodeId: "p1-bit-math",
    question: `136. 只出现一次的数字（LeetCode 136）\n题意：数组中除一个元素只出现一次外，其余每个元素都恰好出现两次，线性时间、常数空间找出那个只出现一次的元素。\n示例：输入 nums = [4,1,2,1,2] → 输出 4（1 和 2 各出现两次相互抵消，4 只出现一次）\n约束：1 <= nums.length <= 3 * 10^4，元素在 [-3 * 10^4, 3 * 10^4]，恰有一个元素出现一次`,
    answer: `## 题意与约束分析
找单身元素，硬性约束 O(n) 时间 + O(1) 空间。「O(1) 空间」封杀哈希计数，「其余恰好两次」是异或消重的精确前提——出现三次这套就失效。

## 思路推导：从暴力到最优
暴力排序后成对检查 O(n log n)，哈希计数 O(n) 时间但 O(n) 空间违规。卡点是空间。结构性质在代数层：异或满足交换律、结合律，且 a^a=0、a^0=a。把全部元素异或起来，成对元素无论位置如何，都能经交换结合两两相邻抵消为 0，0 再与单身元素异或仍是它本身。一趟扫描一个变量，双约束同时满足。

## 正确性要点
- 代数证明：由交换律与结合律，全体元素的异或和等于「成对元素各自异或」再异或「单身元素」；每对 a^a=0，0 被幂等吸收，最终结果为 0 ^ single = single。
- 顺序无关性：交换律保证乱序输入不影响结果，这是异或方案对未排序数组天然免疫的原因。
- 前提精确性：出现偶数次的元素都消为 0，但出现三次会残留 a——题目前提松动，结论立即失效。

## 复杂度分析
- 时间 O(n)：单趟扫描；对比排序 O(n log n)、哈希 O(n) 但带 O(n) 空间。
- 空间 O(1)：只用一个累加变量，这是本方案的核心卖点。

## 代码实现
\`\`\`go
func singleNumber(nums []int) int {
    ans := 0
    for _, v := range nums {
        ans ^= v // 成对元素异或抵消，剩下单身元素
    }
    return ans
}
\`\`\`

## 边界与易错点
- 初始值必须是 0（异或单位元）；写成 ans = nums[0] 再从头遍历会把首个元素多消一次。
- 前提破防：题目改成「其余出现三次」异或立即失效，要换 LeetCode 137 的位计数法。
- 负数在 Go 中是补码表示，异或按位进行，无需任何特殊处理。

## 举一反三
- LeetCode 137 只出现一次的数字 II：其余出现三次，异或失效，改位计数 mod 3。
- LeetCode 260 只出现一次的数字 III：两个单身元素，全体异或得 x^y，再按最低差异位分组各自异或分离。
- LeetCode 268 丢失的数字：下标与值一起异或消重。
- 识别信号：「成对出现找落单 + O(1) 空间 → 异或消重」。

## 实际工程应用
异或消重是系统层经典：RAID5 用 XOR 校验条带重建坏盘数据、XOR 链接表用 prev^next 省一半指针内存、diff 两个互为备份的日志流找出差异行，都是同一代数性质。`,
    keyPoints: [
      "异或满足交换律与结合律，成对元素 a^a=0 全部抵消",
      "0 是异或单位元：a^0=a，单身元素最终幸存",
      "结果与元素顺序无关，对未排序数组天然免疫",
      "前提精确：其余必须出现偶数次，三次即失效（转 LC 137）",
    ],
    followUps: [
      "如果有两个元素只出现一次，怎么把它们都找出来？（LC 260 分组异或的思路）",
      "异或运算和模 2 加法是什么关系？为什么说异或就是无进位加法？",
      "除了找单身数，异或消重在工程里还有哪些应用？",
    ],
    favorited: false,
  },
  {
    id: "algo-137",
    nodeId: "p1-bit-math",
    question: `137. 只出现一次的数字 II（LeetCode 137）\n题意：数组中除一个元素只出现一次外，其余每个元素都恰好出现三次，线性时间、常数空间找出那个只出现一次的元素。\n示例：输入 nums = [2,2,3,2] → 输出 3（2 出现三次，3 只出现一次）\n约束：1 <= nums.length <= 3 * 10^4，元素为 32 位有符号整数，恰有一个元素出现一次`,
    answer: `## 题意与约束分析
与 136 的唯一差异是「三次」，但 a^a^a=a 会残留，异或消重直接失效——这是本题最本质的考点：你是否理解异或的本质是 mod 2 计数，从而知道它为什么不够。

## 思路推导：从暴力到最优
哈希计数 O(n) 空间违规。结构性质换到「位」这一层：把每个数按 32 位二进制拆开，独立统计第 i 位上 1 的总数。出现三次的元素在第 i 位贡献 0 或 3，都是 3 的倍数；唯一单身元素贡献 0 或 1。于是每一位总数 mod 3 恰好还原出答案在该位的值，32 位独立求解再拼装。进阶写法是数字电路法：用 ones、twos 两个变量模拟 mod 3 计数器，逐元素状态转移，本质与位统计相同。

## 正确性要点
- 位独立性：二进制各位之间互不进位，32 位可拆成 32 个互不影响的子问题分别求解。
- 计数恒等式：第 i 位的 1 总数 = 3 乘「三次元素在该位为 1 的个数」加「答案的第 i 位」，故 mod 3 恰为答案的第 i 位。
- 与 136 的差异本质：异或是 mod 2 意义下的逐位计数（只看奇偶），本题要 mod 3，单一异或只有两态无法表达三态，必须显式计数或双变量状态机。

## 复杂度分析
- 时间 O(32n) = O(n)：常数 32 轮位扫描；对比哈希 O(n) 但带 O(n) 空间。
- 空间 O(1)：只用常数个计数变量。

## 代码实现
\`\`\`go
func singleNumber(nums []int) int {
    ans := int32(0)
    for i := 0; i < 32; i++ {
        sum := 0
        for _, v := range nums {
            sum += int((int32(v) >> i) & 1) // 统计第 i 位 1 的总数
        }
        if sum%3 != 0 {
            ans |= 1 << i // 余数即答案在该位的值
        }
    }
    return int(ans)
}
\`\`\`

## 边界与易错点
- 符号位：必须用 int32 右移统计，负数的最高位（第 31 位）才能正确处理；用无符号类型移位会丢符号语义。
- 前提精确：「其余恰好三次」若混入出现五次（mod 3 余 2）的元素会污染答案。
- 位拼装时注意 ans 的类型与 int32 的转换，避免溢出位错乱。

## 举一反三
- LeetCode 136：mod 2 特例，异或即最优；两题对比记住「mod 2 用异或、mod k 用位计数」。
- LeetCode 260 只出现一次的数字 III：两个单身元素，异或加分组的经典技巧。
- 识别信号：「其余出现 k 次找落单 → 位计数 mod k」。

## 实际工程应用
按位统计是大数据原语：HyperLogLog 基数估计的寄存器思想、网络流量按位特征指纹、三重冗余系统的按位多数派表决纠错，都把「位」当独立维度并行处理。`,
    keyPoints: [
      "异或是 mod 2 计数，本题要 mod 3，单一异或无法表达三态",
      "32 位独立：每位统计 1 的总数 mod 3，恰为答案在该位的值",
      "出现三次的元素对每位贡献 0 或 3，被 mod 3 完整吸收",
      "数字电路法 ones/twos 双变量与位统计等价，是同一思想的状态机写法",
    ],
    followUps: [
      "ones 和 twos 两个变量的状态转移怎么推？（提示：按 (twos,ones) 三态循环列真值表）",
      "如果其余元素出现 k 次，通用解法是什么？复杂度多少？",
      "136 题能不能也用位计数法做？为什么大家仍用异或？",
    ],
    favorited: false,
  },
  {
    id: "algo-191",
    nodeId: "p1-bit-math",
    question: `191. 位 1 的个数（LeetCode 191）\n题意：给定一个 32 位无符号整数，返回其二进制表示中 1 的个数（汉明重量）。\n示例：输入 n = 11（二进制 1011）→ 输出 3（共有三个 1）\n约束：输入是 32 位无符号整数，进阶要求 O(1) 或更优`,
    answer: `## 题意与约束分析
求 32 位整数的 popcount。位宽固定 32，逐位检查已足够快；但「次数等于 1 的个数」的跳跃式写法更优雅，也是一切 lowbit 技巧的源头。

## 思路推导：从暴力到最优
暴力逐位检查：循环 32 次累加 (n>>i)&1，恒 32 次。观察位模式：n-1 会把 n 的最低位 1 变成 0、其右侧的 0 全变成 1，更高位不动；于是 n&(n-1) 精确地「消去最低位的 1」，其余 1 原样保留。每消一次计数加一，循环次数等于 1 的个数而非固定位宽——稀疏整数（如 2^31）一次就结束。

## 正确性要点
- 位运算引理：设 n 的最低位 1 在第 k 位，则 n-1 的第 0 到 k-1 位全为 1、第 k 位为 0、高位不变；按位与后恰消去第 k 位的 1，其余 1 不受影响。
- 循环不变式：每轮迭代 count 增加 1 且 n 中 1 的总数恰减少 1；n 归零时 count 即原数的汉明重量。
- 与逐位法等价：两者枚举的都是全部 1，只是一个按位索引逐个看、一个按值跳着消。

## 复杂度分析
- 时间 O(popcount)：循环次数等于 1 的个数，最坏 32 次；对比逐位法恒 32 次。
- 空间 O(1)。

## 代码实现
\`\`\`go
func hammingWeight(n uint32) int {
    count := 0
    for n != 0 {
        n &= n - 1 // 每次消去最低位的 1
        count++
    }
    return count
}
\`\`\`

## 边界与易错点
- 死循环写法：循环条件用 n > 0 在有符号负数类型下行为不同，参数用 uint32 最稳。
- 与 n&(-n) 混淆：n&(-n) 是「提取」最低位的 1（lowbit，树状数组核心），n&(n-1) 是「消除」它，方向相反。
- 用数学取模逐位拆（n%2）在负数上出错，位运算题就待在无符号类型里。

## 举一反三
- LeetCode 231 2 的幂：n&(n-1)==0 且 n>0，消一次就归零的判定。
- LeetCode 338 比特位计数：DP 递推 count[i] = count[i&(i-1)] + 1，正是本技巧的批量版。
- LeetCode 461 汉明距离：x^y 定位差异位后求 popcount。
- 识别信号：「逐位枚举且只关心 1 → n&(n-1) 跳跃消位」。

## 实际工程应用
popcount 是高性能计算原语（CPU 有 POPCNT 指令）：位图索引的基数统计、布隆过滤器容量估算、基因组学 k-mer 汉明距离筛选、权限位掩码的授权清点，全建立在同一操作上。`,
    keyPoints: [
      "n&(n-1) 精确消去最低位的 1，其余位不动",
      "循环次数等于 1 的个数，稀疏整数远快于固定 32 次",
      "n-1 把最低位 1 变 0、其后 0 全变 1，按位与后恰消一位",
      "与 n&(-n) 区分：一个消除 lowbit、一个提取 lowbit，方向相反",
    ],
    followUps: [
      "n&(n-1) 和 n&(-n) 分别是什么？后者在树状数组里怎么用？",
      "如何用查表法或分治法做到接近 O(1) 的 popcount？",
      "现代 CPU 有 POPCNT 指令，这些位技巧还有什么意义？",
    ],
    favorited: false,
  },
  {
    id: "algo-231",
    nodeId: "p1-bit-math",
    question: `231. 2 的幂（LeetCode 231）\n题意：给定整数 n，判断它是否为 2 的幂（1, 2, 4, 8, ...），进阶要求不用循环或递归。\n示例：输入 n = 16 → 输出 true（16 = 2^4）；输入 n = 3 → 输出 false\n约束：-2^31 <= n <= 2^31 - 1，n 可为 0 和负数`,
    answer: `## 题意与约束分析
判 2 的幂，进阶要求 O(1)。约束里 n 可为 0 和负数——这是大部分错误解法的葬身之地，尤其是 n=0 会骗过经典的位运算判据。

## 思路推导：从暴力到最优
暴力循环除 2 看能否除尽到 1，O(log n)，但进阶要求 O(1)，必须找位模式判据。2 的幂的二进制有且仅有一个 1（1=1, 2=10, 4=100）。而 n-1 恰好把那个唯一的 1 及其以下全部翻转，于是 n&(n-1) 把它消掉，结果为 0；非 2 的幂有多个 1，消一个还有剩余，结果非零。再联立 n>0 排除 0 与负数，判定完备。

## 正确性要点
- 充要性证明：n&(n-1)==0 当且仅当 n 的二进制至多一个 1（消掉最低位 1 后为空）；联立 n>0 当且仅当恰有一个 1，当且仅当 n 是 2 的正整数幂。
- 双条件缺一不可：n=0 时 0&(0-1)==0 同样成立，必须用 n>0 显式排除；负数的补码带符号位，被 n>0 一并拦下。
- 位模式本质：这是「唯一置位性」的 O(1) 判据，与 popcount(n)==1 等价但省去计数。

## 复杂度分析
- 时间 O(1)：两次位运算加一次比较；对比循环除法 O(log n)。
- 空间 O(1)。

## 代码实现
\`\`\`go
func isPowerOfTwo(n int) bool {
    // 2 的幂的二进制有且仅有一个 1，n&(n-1) 消掉它即归零
    return n > 0 && n&(n-1) == 0
}
\`\`\`

## 边界与易错点
- 漏掉 n>0：n=0 时 0&(0-1)==0 被误判 true，这是本题提交错误率最高的挂法。
- 负数：INT_MIN 的补码看似只有一个符号位 1，但 2 的幂按题面指正幂，n>0 统一排除。
- 循环解法注意 n=1（即 2^0）必须返回 true。

## 举一反三
- LeetCode 191 位 1 的个数：本题是其特例（popcount==1），n&(n-1) 消位计数。
- LeetCode 342 4 的幂：先判 2 的幂，再限定唯一的 1 在偶数位（与 0x55555555 按位与）。
- LeetCode 326 3 的幂：无位模式捷径，改用最大 3 的幂整除法——对比「2 的幂有位技巧、3 没有」。
- 识别信号：「2 的幂、唯一置位 → n&(n-1)==0」。

## 实际工程应用
2 的幂判定遍布系统编程：哈希表容量取 2 的幂以便 (n-1)&hash 位掩码代替取模、内存对齐检查（addr&(align-1)==0）、环形缓冲区下标回绕，全是同一条位运算。`,
    keyPoints: [
      "2 的幂二进制恰有一个 1，n&(n-1) 消掉后归零",
      "n>0 不可省：0 也满足 0&(0-1)==0，是最常见挂法",
      "位模式判据 O(1)，与 popcount(n)==1 等价但更快",
      "充要性：至多一个 1 且为正数，当且仅当 2 的正整数幂",
    ],
    followUps: [
      "如何判断一个数是 4 的幂？（先 2 的幂，再限定唯一的 1 在偶数位）",
      "3 的幂为什么没有类似位技巧？常规解法是什么？",
      "哈希表容量为什么取 2 的幂？和 (n-1)&hash 是什么关系？",
    ],
    favorited: false,
  },
  {
    id: "algo-50",
    nodeId: "p1-bit-math",
    question: `50. Pow(x, n)（LeetCode 50）\n题意：实现 pow(x, n)，计算 x 的 n 次幂，n 为 32 位有符号整数（可为负），x 为浮点数。\n示例：输入 x = 2.0, n = 10 → 输出 1024.0（2 的 10 次方等于 1024）\n约束：-100.0 < x < 100.0，-2^31 <= n <= 2^31 - 1，结果保证在 [-10^4, 10^4]`,
    answer: `## 题意与约束分析
实现幂运算。n 取到 INT_MIN 时直接取负在 32 位下溢出，这是快速幂之外的第二考点。结果范围有限，但中间过程不在——暴力连乘 n 次在 n 为 1e9 时直接超时。

## 思路推导：从暴力到最优
暴力连乘 O(n)，n 巨大时不可行。结构性质：指数可二进制分解——13 = 1101 二进制，故 x^13 = x^8 乘 x^4 乘 x^1。预处理 x 的逐次平方（x^1, x^2, x^4, x^8，每次自乘 O(1) 得下一项），n 的二进制哪一位是 1 就把对应幂乘进答案，乘法次数从 n 降到 log n。负 n 用 x^(-n) = (1/x)^n 转为正指数。

## 正确性要点
- 二进制分解恒等式：任意非负整数 n 等于各位权重 2^i 之和，由幂的乘法法则 x^n 等于所有「该位为 1 的 x^(2^i)」之积；每轮自乘恰好依次生成 x^(2^i) 序列。
- 循环不变式：处理完低 k 位后，ans 等于 x 的「低 k 位表示的指数」次幂，而底数变量恰为原 x 的 2^k 次幂。
- 负指数正确性：x^(-n)=(1/x)^n 在 x 非零时恒成立；Go 的 64 位 int 下 -INT_MIN 不溢出，Java/C++ 必须先转 long 再取负。

## 复杂度分析
- 时间 O(log n)：约 31 次乘法；对比暴力 O(n) 的 1e9 次。
- 空间 O(1)：迭代写法；递归写法 O(log n) 栈。

## 代码实现
\`\`\`go
func myPow(x float64, n int) float64 {
    if n < 0 {
        x = 1 / x
        n = -n // Go 64 位 int 下 INT_MIN 取负安全；Java/C++ 需先转 long
    }
    ans := 1.0
    for n > 0 {
        if n&1 == 1 {
            ans *= x // 当前二进制位为 1，把对应的 x^(2^k) 乘进答案
        }
        x *= x // 底数平方：x -> x^2 -> x^4 -> x^8
        n >>= 1
    }
    return ans
}
\`\`\`

## 边界与易错点
- INT_MIN 取负溢出：C/Java 必须先转 long long；Go 的 64 位 int 免疫，但跨语言面试要主动说出这个点。
- 三步顺序：先按 n&1 乘答案、再 x 自乘、最后 n 右移；顺序错乱则幂次整体错位。
- x=0 且 n<0 数学上无定义，题目保证结果有限可不特判，但要知道这个前提。
- 多次平方会放大浮点误差，这是迭代法的固有性质，不是 bug。

## 举一反三
- LeetCode 372 超级次方：快速幂加模运算，指数按位滚动分解（((a^b)^10) 乘 a^c）。
- 矩阵快速幂：斐波那契第 n 项 O(log n)、线性递推加速，把底数从数换成矩阵。
- 识别信号：「重复自乘或线性递推求第 n 项且 n 巨大 → 快速幂（底数可为数、矩阵、排列）」。

## 实际工程应用
快速幂是密码学的算力地基：RSA 加解密就是模幂 c = m^e mod n，Diffie-Hellman 密钥交换同理；区块链 PoW 目标值计算、大数随机采样缩放也都靠它。`,
    keyPoints: [
      "指数二进制分解：x^13 = x^8 乘 x^4 乘 x^1，乘法次数从 n 降到 log n",
      "迭代三板斧：n&1 为 1 则 ans 乘 x、x 自乘平方、n 右移一位",
      "负指数转倒数：x^(-n) = (1/x)^n",
      "INT_MIN 取负溢出：Go 64 位 int 免疫，Java/C++ 必须先转 long",
    ],
    followUps: [
      "底数换成矩阵（求斐波那契第 n 项）时快速幂怎么推广？",
      "模幂运算（RSA）在快速幂基础上多了哪一步？为什么每步都要取模？",
      "递归写法和迭代写法的空间复杂度差异？哪个更不容易写错？",
    ],
    favorited: false,
  },
  {
    id: "algo-7",
    nodeId: "p1-bit-math",
    question: `7. 整数反转（LeetCode 7）\n题意：反转 32 位有符号整数的十进制位（123 变 321，-123 变 -321，120 变 21），若反转后超出 [-2^31, 2^31-1] 返回 0。\n示例：输入 x = 120 → 输出 21（反转后前导零自然消失）\n约束：-2^31 <= x <= 2^31 - 1，假设环境不允许存储 64 位整数`,
    answer: `## 题意与约束分析
反转十进制位，溢出返回 0。「不许存 64 位」的假设意味着：溢出检测必须在溢出发生之前完成——溢出后再检查，值已经回绕，判断失效。

## 思路推导：从暴力到最优
转字符串反转依赖库函数且面试不认。正路是数学弹栈：循环 x%10 取末位、x/10 削末位，rev = rev*10 + pop 重建。卡点：rev*10 本身可能溢出，检测必须前置到乘 10 之前：rev > INT_MAX/10 必溢出；rev == INT_MAX/10 时要求 pop <= 7。负数对 INT_MIN 对称（末位 -8）。

## 正确性要点
- 前置检测充要性：rev*10+pop > INT_MAX 当且仅当 rev > INT_MAX/10，或 rev == INT_MAX/10 且 pop > 7（整数性质，INT_MAX 末位为 7）；负侧对 INT_MIN 对称成立。
- 重建不变式：每轮后 rev 是「已处理低位」的反转，x 剩未处理高位；x 归零时 rev 即完整反转。
- 末位为 0 的情形由 rev 初始 0 与整数除法自然处理，前导零自动消失，无需特判。

## 复杂度分析
- 时间 O(log10 x)：最多 10 轮；对比字符串法同阶但零库依赖。
- 空间 O(1)。

## 代码实现
\`\`\`go
import "math"

func reverse(x int) int {
    rev := 0
    for x != 0 {
        pop := x % 10 // Go 取余带被除数符号，负数直接可用
        x /= 10
        // 溢出必须在乘 10 之前检测
        if rev > math.MaxInt32/10 || (rev == math.MaxInt32/10 && pop > 7) {
            return 0
        }
        if rev < math.MinInt32/10 || (rev == math.MinInt32/10 && pop < -8) {
            return 0
        }
        rev = rev*10 + pop
    }
    return rev
}
\`\`\`

## 边界与易错点
- 先 rev*10 再检查：值已回绕，判断失效——检测必须前置到乘法之前。
- Go 的 int 是 64 位，本题溢出永不真的触发，但题目语义要求按 32 位判，必须用 math.MaxInt32 常量显式比较。
- 阈值背混：INT_MAX 末位 7、INT_MIN 末位 8，记反会在 1463847412 这类边界值上挂。
- 负数无需取绝对值：Go 的 % 带被除数符号，-123%10 = -3，直接参与重建即可。

## 举一反三
- LeetCode 8 字符串转换整数：同款「累乘前溢出预检」模板，只是逐字符读入。
- LeetCode 9 回文数：反转一半比较，用「只反转后半段」彻底规避溢出。
- 识别信号：「逐位累乘重建数值 → 乘前做商级溢出预检」。

## 实际工程应用
溢出预检是金融与协议解析的保命习惯：金额以分累加前先查上限、解析 protobuf varint 移位前验位宽、TCP 序列号回绕比较，都是「先证明安全、再执行运算」。`,
    keyPoints: [
      "溢出检测必须前置：rev*10 之前用 rev 与 MaxInt32/10 比较",
      "边界值：rev == MaxInt32/10 时要求 pop <= 7；负侧 MinInt32 对应 pop >= -8",
      "Go int 是 64 位，溢出永不真触发，但须按题意用 32 位常量显式判",
      "Go 取余带被除数符号，-123%10=-3，负数反转无需取绝对值",
    ],
    followUps: [
      "为什么溢出之后再检查就来不及？有符号溢出在 C 和 Go 里分别是什么行为？",
      "如果允许用 64 位整数，解法可以简化成什么样？",
      "输入末尾有 0 时，反转后的前导零去哪了？为什么不用特判？",
    ],
    favorited: false,
  },
  {
    id: "algo-9",
    nodeId: "p1-bit-math",
    question: `9. 回文数（LeetCode 9）\n题意：判断整数 x 是否为回文数（正读反读相同），进阶要求不将整数转为字符串。\n示例：输入 x = 121 → 输出 true（正读反读都是 121）；输入 x = -121 → 输出 false（负号破坏了镜像）\n约束：-2^31 <= x <= 2^31 - 1`,
    answer: `## 题意与约束分析
判回文，进阶禁转字符串。负数因负号直接否；全反转在 2147447412 这类大回文附近有溢出风险——本题的标准解是「只反转一半」，从根上免疫溢出。

## 思路推导：从暴力到最优
转字符串加双指针 O(位数) 易写但被进阶要求禁掉。全反转（LeetCode 7 的做法）依赖溢出预检，绕。最优观察：回文只需前后两半镜像相等，那就只把后半段反转成整数 reverted，循环到 x <= reverted 为止——此时已处理的位数不少于剩余位数，即过半。比较 x == reverted（偶数位）或 x == reverted/10（奇数位，中位数自匹配剔除）。反转一半的值不超过原数，天然免疫溢出。

## 正确性要点
- 终止条件：每轮 x 削一位、reverted 增一位，x <= reverted 时已处理位数过半，后半段完整反转。
- 奇偶分支：偶数位两半完全镜像，x == reverted；奇数位 reverted 多含一个中位数字，除 10 剔除后比较。
- 免疫溢出证明：reverted 是原数后缀的反转，位数不超过原数，其值必在原 int 范围内。
- 特判完备：负数否；末位 0 且非 0（如 10、100）否——首位非 0 而末位 0 的数不可能镜像。

## 复杂度分析
- 时间 O(log10 x)：只处理一半位数，常数再减半。
- 空间 O(1)。

## 代码实现
\`\`\`go
func isPalindrome(x int) bool {
    // 负数、以及末位是 0 的非零数都不是回文
    if x < 0 || (x%10 == 0 && x != 0) {
        return false
    }
    reverted := 0
    for x > reverted { // 只反转后半段，天然免疫溢出
        reverted = reverted*10 + x%10
        x /= 10
    }
    // 偶数位直接相等；奇数位 reverted 多一位中位数，除掉再比
    return x == reverted || x == reverted/10
}
\`\`\`

## 边界与易错点
- 漏掉末位 0 特判：x=10 时循环一轮后 x=1、reverted=1，返回 true——错，必须前置 (x%10==0 && x!=0)。
- 奇偶分支缺一不可：1221 走 x==reverted，12321 必须走 x==reverted/10。
- 循环条件写成 x != 0 就变成全反转，失去防溢出的意义。

## 举一反三
- LeetCode 234 回文链表：同一「半段反转」思想迁到链表（快慢指针找中点加反转后半）。
- LeetCode 7 整数反转：全反转加溢出预检，与本题「折半免溢出」互为对照。
- 识别信号：「回文判定且数值结构 → 折半反转比较」。

## 实际工程应用
半段比较是省资源校验范式：流式数据只缓存后半段哈希与前半段对照、DNA 反向互补序列匹配、双向链表的折叠校验，都是「只加工一半，与另一半比对」。`,
    keyPoints: [
      "负数与末位 0 的非零数直接否，特判先行",
      "只反转后半段：x > reverted 为循环条件，过半即停",
      "偶数位比 x==reverted，奇数位比 x==reverted/10（剔除中位数）",
      "reverted 是原数后缀的反转，值不超原数，天然免疫溢出",
    ],
    followUps: [
      "严格论证：为什么反转一半就一定不会溢出？",
      "输入 10 如果没有末位 0 特判会返回什么？把流程走一遍。",
      "回文链表（LC 234）怎么借用同样的折半思想？空间怎么做到 O(1)？",
    ],
    favorited: false,
  },
  {
    id: "algo-66",
    nodeId: "p1-bit-math",
    question: `66. 加一（LeetCode 66）\n题意：用数组表示一个非负整数的十进制位（最高位在前、无前导零），实现该整数加一，返回新数组。\n示例：输入 digits = [9,9] → 输出 [1,0,0]（99 + 1 = 100，需要扩容一位）\n约束：1 <= digits.length <= 100，0 <= digits[i] <= 9，最高位不为 0`,
    answer: `## 题意与约束分析
大数加一。位数最多 100，远超 int64 的 19 位——转整数再转回的路从约束上就被封死。本质是小学竖式的一步：从末位处理进位，关键洞察是进位只在「当前位是 9」时传播。

## 思路推导：从暴力到最优
转整数加一再拆回数组，100 位溢出直接否决。回到竖式本质：从最低位（数组末尾）向前扫，遇到小于 9 的位就加一收工——进位到此为止，高位全部不动；遇到 9 就置 0，进位继续向前传。若所有位都是 9（如 999），扫完后进位无处安放，在最前面补 1，数组扩容一位。一趟扫描，只有全 9 才触发扩容。

## 正确性要点
- 进位传播引理：十进制加一的进位链恰好是「末尾连续 9 的后缀」——该后缀全部变 0，后缀前一位加一；第一个非 9 位出现时传播立即终止。
- 分支完备性：任一输入只有两种结局——中途遇非 9 位（原地返回）或全 9（补 1 扩容），无第三种情形。
- 全 9 扩容正确性：k 个 9 加一等于 10 的 k 次方，十进制表示为 1 后接 k 个 0，正是新数组 [1, 0, ..., 0]。

## 复杂度分析
- 时间 O(n)：最坏全 9 扫全程；平均 O(1)——末位非 9 的概率是 90%。
- 空间 O(1)：原地修改；全 9 时 O(n) 新数组。

## 代码实现
\`\`\`go
func plusOne(digits []int) []int {
    for i := len(digits) - 1; i >= 0; i-- {
        if digits[i] < 9 {
            digits[i]++ // 无进位，直接返回
            return digits
        }
        digits[i] = 0 // 当前位变 0，进位传向高位
    }
    // 走到这里说明全是 9，需要扩容一位
    return append([]int{1}, digits...)
}
\`\`\`

## 边界与易错点
- 全 9 扩容遗漏：只原地置 0 不补位，999 变成 000，大错。
- 方向搞反：从高位开始遍历完全违背进位方向，必须从末尾向前。
- 转整数再转回：100 位远超 int64，溢出静默出错——约束里就写着禁止。

## 举一反三
- LeetCode 67 二进制求和：同款竖式，基数换 2，逢 2 进 1。
- LeetCode 2 两数相加：链表版竖式，两个加数加进位三个输入。
- LeetCode 415 字符串相加：大数加法通用模板，双指针从尾部对齐。
- 识别信号：「数组或字符串表示的大数运算 → 竖式模拟加进位传播」。

## 实际工程应用
进位传播是任意精度算术的内核：Java BigInteger 与 Python int 的底层 limb 数组加法、分布式 ID 号段溢位、版本号自动递增（1.9.9 到 2.0.0）的进位逻辑，全是这个竖式。`,
    keyPoints: [
      "进位链恰是末尾连续 9 的后缀：遇 9 置 0 续传，遇非 9 加一收工",
      "只有全 9 才需要扩容，在最前补 1（999+1=1000）",
      "禁止转整数：100 位远超 int64，溢出静默出错",
      "平均时间 O(1)：末位非 9 的概率 90%，一趟都不一定走完",
    ],
    followUps: [
      "如果改成加任意整数 k 而不是加 1，怎么扩展？",
      "二进制数组加一（类似 LC 67 的数组版）进位规则怎么变？",
      "为什么说平均时间复杂度是 O(1)？用期望分析一下。",
    ],
    favorited: false,
  },
];
