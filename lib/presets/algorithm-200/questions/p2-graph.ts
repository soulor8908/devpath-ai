// lib/presets/algorithm-200/questions/p2-graph.ts
// 「算法 200 题」Phase 2 · 图论专题（12 题深度题解）
//
// 设计说明：
//   - 每题答案为八段式深度结构：题意约束分析 → 思路推导（从暴力到最优）→
//     正确性要点 → 复杂度对比 → Go 实现 → 边界易错 → 举一反三 → 实际工程应用
//   - 本专题主线：图论四大范式——Flood Fill 与多源 BFS（200/994/130/417）、
//     拓扑排序（207/210）、并查集（684）、最短路（787/743），
//     外加图深拷贝（133）与两道「图思想」应用题（208 Trie / 399 比值图）
//   - 旧版答案只贴代码被用户投诉无深度，本版按面试口述标准重写

import type { Question } from "../../../types";

export const P2_GRAPH_QUESTIONS: Question[] = [
  {
    id: "algo-200",
    nodeId: "p2-graph",
    question: `200. 岛屿数量（LeetCode 200）
题意：给定 m×n 的 01 网格，'1' 为陆地、'0' 为水，四方向（上下左右）相连的陆地构成一座岛屿，返回岛屿数量。
示例：输入 grid = [["1","1","0"],["1","0","0"],["0","0","1"]] → 输出 2（左上连片一座、右下角一座）。
约束：1 ≤ m, n ≤ 300，grid[i][j] ∈ {'0','1'}。`,
    answer: `## 题意与约束分析
求网格中四连通块的数量，是 Flood Fill 的元问题。m,n ≤ 300 ⇒ 格子总数 9×10^4，O(mn) 一次扫描绰绰有余；关键是「每个格子只处理一次」的标记策略。

## 思路推导：从暴力到最优
没有更好的渐进复杂度可求——读入本身就要 Ω(mn)。真正的设计点在「如何标记已访问」：方案 A 开 visited 布尔数组，空间 O(mn)；方案 B「沉岛法」：DFS 到达陆地就原地改成 '0'，把网格本身当 visited，额外空间 O(1)（不计递归栈）。主循环扫到 '1' 即计数 +1 并 Flood Fill 整岛——每个连通块恰好触发一次填充，计数即块数。DFS 与 BFS 均可，DFS 代码更短，BFS 无爆栈风险。

## 正确性要点
- 填充的完备性：DFS/BFS 沿四方向扩展到所有可达陆地（可达性是传递闭包），一次填充恰好覆盖一个连通块。
- 计数的双射：主循环只在遇到未访问的 '1' 时计数并填充；已访问的陆地已被沉没，不会二次触发 ⇒ 计数与连通块一一对应。
- 沉岛法的合法性：'1'→'0' 后该格不再是陆地，等价于 visited 标记；扩展时遇 '0' 或越界即停，不会泄漏到其他岛。

## 复杂度分析
- 时间：O(mn)，每个格子最多被访问常数次（主循环一次 + 填充一次）
- 空间：沉岛法 O(1) 额外（DFS 递归栈最坏 O(mn)）；visited 法 O(mn)

## 代码实现
\`\`\`go
func numIslands(grid [][]byte) int {
    m, n := len(grid), len(grid[0])
    var dfs func(int, int)
    dfs = func(i, j int) {
        if i < 0 || i >= m || j < 0 || j >= n || grid[i][j] != '1' {
            return // 越界或非陆地，停止扩展
        }
        grid[i][j] = '0' // 沉岛：原地标记，免 visited
        dfs(i-1, j)
        dfs(i+1, j)
        dfs(i, j-1)
        dfs(i, j+1)
    }
    ans := 0
    for i := 0; i < m; i++ {
        for j := 0; j < n; j++ {
            if grid[i][j] == '1' {
                ans++     // 发现新岛
                dfs(i, j) // 整岛沉没
            }
        }
    }
    return ans
}
\`\`\`

## 边界与易错点
- 会改坏输入：沉岛法修改了原 grid，若调用方还要复用网格，必须换 visited 或先复制——面试主动说明这个权衡是加分项。
- 斜向不算连通：只扩四方向，误加对角线会把两岛并一岛。
- 深网格 DFS 爆栈：300×300 全 '1' 时递归深度 9 万，Go 的栈可增长尚安全，固定栈语言需改迭代或 BFS。
- 输入元素类型：LeetCode 给 byte（'0'/'1' 字符），不是 int，比较时别写 == 1。

## 举一反三
- LC 695 岛屿的最大面积：填充时计数面积，识别信号「连通块 + 聚合值」。
- LC 130 被围绕的区域：从边界反向染色，连通块 + 条件保留。
- LC 1254 统计封闭岛屿的数目：封闭 = 不触边界的岛，沉岛时带边界检测。
- LC 305 岛屿数量 II：在线加点，连通关系动态合并 → 并查集。
- 识别信号：「网格 + 四/八连通 + 计数/聚合」→ Flood Fill。

## 实际工程应用
Flood Fill 是图像处理的基本操作：PS 的魔棒选区、医学影像中肿瘤连通区域提取、地图服务中「同一片城区」的地块聚类都是连通块填充；游戏地图生成（Roguelike 房间连通性校验）、路径规划的可达域分析也以它为第一步；分布式系统里「集群脑裂后有几个分区」本质也是连通块计数。`,
    keyPoints: [
      "岛屿计数 = 连通块计数 = Flood Fill 触发次数",
      "沉岛法：原地改 '0' 当 visited，额外空间 O(1)，代价是改坏输入",
      "四方向可达性是传递闭包，一次填充恰好覆盖一个连通块",
      "读入即 Ω(mn)，O(mn) 算法已是最优，优化点在空间与实现",
    ],
    followUps: [
      "不许修改输入数组怎么办？（visited 或记录染色值后还原）",
      "如果陆地是逐个加入的（在线查询岛屿数），该怎么做？（LC 305 并查集）",
      "八连通怎么改？对角线相连算一座岛时代码改哪里？",
    ],
    favorited: false,
  },
  {
    id: "algo-994",
    nodeId: "p2-graph",
    question: `994. 腐烂的橘子（LeetCode 994）
题意：m×n 网格中 0=空、1=新鲜橘子、2=腐烂橘子。每分钟腐烂向四方向扩散一格。返回让所有新鲜橘子腐烂的最少分钟数；若有新鲜橘子永远无法腐烂（被空格隔离），返回 -1。
示例：输入 grid = [[2,1,1],[1,1,0],[0,1,1]] → 输出 4（第 4 分钟全部腐烂）。
约束：1 ≤ m, n ≤ 10，grid[i][j] ∈ {0,1,2}。`,
    answer: `## 题意与约束分析
多起点同时扩散、求「最远新鲜点被波及的分钟数」——这是多源 BFS 的教科书模型。网格虽小（≤100 格），但「多个初始腐烂源」和「层数即时间」两个概念必须刻进肌肉记忆。

## 思路推导：从暴力到最优
错误直觉：对每个腐烂源分别跑 BFS，取每个新鲜橘子的最早感染时间再取 max——重复遍历，实现繁琐。
结构性质：BFS 的层序展开天然是「按距离从小到大」的波前。把所有初始腐烂橘子在零时刻同时入队，等价于虚拟出一个「超级源点」；此后每一层扩展恰好对应一分钟的扩散。BFS 结束时，所有可达新鲜橘子都按最早时间腐烂，层数即答案——一次 BFS 完成所有源的合并计算。

## 正确性要点
- 多源等价性：全部源点零时刻入队，等价于增设虚拟源点 S 向每个源连边长 0 的边；从 S 开始的层序展开与「各源同时扩散」的物理过程一一对应。
- 层数即最早时间：BFS 第 t 层访问到的格子与最近源点的四方向距离恰为 t ⇒ 每个新鲜橘子按最早可能时刻腐烂，最终层数 = 全部腐烂所需最短时间。
- 判 -1：BFS 只能到达与某源四连通的格子；结束后仍有 fresh > 0 ⟺ 存在隔离区，返回 -1，判定完备。

## 复杂度分析
- 时间：O(mn)，每格入队一次，对比逐源 BFS 的 O(k·mn)（k 为源数）
- 空间：O(mn)，队列最坏装下整层波前

## 代码实现
\`\`\`go
func orangesRotting(grid [][]int) int {
    m, n := len(grid), len(grid[0])
    type pos struct{ x, y int }
    q := []pos{}
    fresh := 0
    for i := 0; i < m; i++ {
        for j := 0; j < n; j++ {
            if grid[i][j] == 2 {
                q = append(q, pos{i, j}) // 所有源点零时刻同时入队
            } else if grid[i][j] == 1 {
                fresh++
            }
        }
    }
    if fresh == 0 {
        return 0 // 没有新鲜橘子，零分钟
    }
    dirs := [][2]int{{-1, 0}, {1, 0}, {0, -1}, {0, 1}}
    minutes := 0
    for len(q) > 0 {
        size := len(q) // 当前层 = 当前这一分钟能感染的全部
        infected := false
        for s := 0; s < size; s++ {
            p := q[0]
            q = q[1:]
            for _, d := range dirs {
                x, y := p.x+d[0], p.y+d[1]
                if x >= 0 && x < m && y >= 0 && y < n && grid[x][y] == 1 {
                    grid[x][y] = 2 // 原地标记已感染
                    fresh--
                    infected = true
                    q = append(q, pos{x, y})
                }
            }
        }
        if infected {
            minutes++ // 只有真感染了才算过去一分钟
        }
    }
    if fresh > 0 {
        return -1 // 有隔离区
    }
    return minutes
}
\`\`\`

## 边界与易错点
- 初始队列只放一个源：把多源当单源 BFS 是头号错误——必须扫描全网格把所有 2 同时入队。
- 分钟数多算：最后一层扩展后队列变空，若无脑每层 minutes++ 会多计 1；用「本层是否真感染」或「初始 minutes=-1」两种写法之一修正。
- 初始 fresh==0 直接返回 0（网格全空或全腐烂），漏掉会与后面的判定混淆。
- 层内处理必须用循环前快照的 size，不能边弹边看 len(q)——否则层边界错乱，分钟数偏小。

## 举一反三
- LC 542 01 矩阵：所有 0 为源的多源 BFS，求每个 1 到最近 0 的距离——把「层数」写回格子。
- LC 1765 地图中的最高点：水域高度 0 为源，同款多源 BFS 逐层抬升。
- LC 286 墙与门：门为源，填充每个空房间到最近门的距离。
- 识别信号：「多个起点同时扩散 + 问最短时间/距离」→ 多源 BFS；无权图单源最短路 → 普通 BFS；带权 → Dijkstra。

## 实际工程应用
多源 BFS 是「扩散模拟」的标准件：流行病学中多个感染源的传播推演、舆情系统中多节点消息的触达时间估计、CDN 多边缘节点的内容预热覆盖时长、芯片设计中多电源点的信号传播延迟分析——凡「多源并发 + 最早到达时间」的物理/信息扩散，都是把源点零时刻同队入 BFS 的层序问题。`,
    keyPoints: [
      "多源 BFS = 所有源点零时刻同时入队，等价于虚拟超级源",
      "层序展开的层数即扩散分钟数，BFS 保证每个点按最早时间被感染",
      "层边界靠循环前快照 size，边弹边判断会把层数算小",
      "fresh 计数器同时承担「初始全烂返 0」与「结束判 -1」两个职责",
    ],
    followUps: [
      "为什么逐源 BFS 再取 max 是错的/慢的？多源等价于什么图变换？",
      "改成求每个新鲜橘子各自第几分钟腐烂，怎么输出？（把层数写回格子）",
      "如果腐烂源也是逐步新增的（第 t 分钟新出现源），模型要怎么改？",
    ],
    favorited: false,
  },
  {
    id: "algo-207",
    nodeId: "p2-graph",
    question: `207. 课程表（LeetCode 207）
题意：numCourses 门课（编号 0..n-1），prerequisites[i] = [a, b] 表示修 a 前必须先修 b。判断能否完成所有课程（即先修关系图中无环）。
示例：输入 numCourses = 2, prerequisites = [[1,0],[0,1]] → 输出 false（0 与 1 互为先修，成环）。
约束：1 ≤ numCourses ≤ 2000，0 ≤ prerequisites.length ≤ 5000，先修对不重复。`,
    answer: `## 题意与约束分析
先修关系构成有向图，「能修完所有课」⟺ 存在拓扑序 ⟺ 图无环（DAG）。n ≤ 2000、边 ≤ 5000，O(V+E) 的 Kahn 或 DFS 三色都轻松达标；本题考察的是对「环检测」两种经典范式的掌握。

## 思路推导：从暴力到最优
暴力思路（枚举修课排列逐一验证）O(n!) 显然不可行，要利用结构性质：DAG 必有入度 0 的点（否则沿入边无限回溯必成环）。
Kahn 法（BFS）：把所有入度 0 的点入队，逐个出队并删其出边（邻居入度 -1），新出现入度 0 的继续入队。若最终出队点数 == n 则无环；否则剩下的点构成环（环上点入度永 ≥1，永不入队）。
DFS 三色法：白（未访）/灰（在递归栈）/黑（已完成）。DFS 中遇到灰点 ⟺ 发现指向祖先的回边 ⟺ 有环。两法同为 O(V+E)，Kahn 迭代式更稳、DFS 递归式更短。

## 正确性要点
- Kahn 出队即拓扑：入度 0 意味着先修全部满足，此时出队合法；出队删边产生的每个新 0 入度点同样满足——归纳可得整个出队序列是拓扑序。
- Kahn 判环：若出队数 < n，剩余点入度均 ≥1；每个点都有入边 ⇒ 沿入边回溯在有限点集中必重复，构成有向环——判环充要，不漏不误。
- DFS 三色：灰点在递归栈上，指向灰点的边即「指向祖先」的回边；有向图有环 ⟺ DFS 必遇回边 ⟺ 必遇灰点。

## 复杂度分析
- 时间：O(V+E)，每点每边各处理一次
- 空间：O(V+E)，邻接表 + 入度数组 + 队列/递归栈

## 代码实现
\`\`\`go
func canFinish(numCourses int, prerequisites [][]int) bool {
    g := make([][]int, numCourses)
    indeg := make([]int, numCourses)
    for _, p := range prerequisites {
        g[p[1]] = append(g[p[1]], p[0]) // 先修课 b -> 后续课 a
        indeg[p[0]]++
    }
    q := []int{}
    for i := 0; i < numCourses; i++ {
        if indeg[i] == 0 {
            q = append(q, i) // 零先修依赖的课直接可修
        }
    }
    done := 0
    for len(q) > 0 {
        u := q[0]
        q = q[1:]
        done++
        for _, v := range g[u] {
            indeg[v]-- // 删除出边
            if indeg[v] == 0 {
                q = append(q, v)
            }
        }
    }
    return done == numCourses // 出队数 < 总数 ⟺ 有环残留
}
\`\`\`

## 边界与易错点
- 建边方向写反：[a,b] 是「先 b 后 a」，边为 b→a；方向反了入度全错。
- 入度减到 0 才入队：不判断就重复入队会让 done 虚增，把有环判成无环。
- 自环 [a,a]：入度永 ≥1，Kahn 自然判 false，无需特判。
- DFS 三色法只写 visited 布尔会误判：把指向黑点（已完成）的交叉边当回边——必须区分灰与黑。

## 举一反三
- LC 210 课程表 II：同模型输出拓扑序，Kahn 出队序列即是。
- LC 802 找到最终的安全状态：反向图 + 拓扑剥点，识别信号「环检测 + 状态分类」。
- LC 269 火星词典：字母间建边后拓扑排序，注意非法前缀特判。
- LC 329 矩阵中的最长递增路径：DAG 上 DFS + 记忆化，拓扑思想的递推版。
- 识别信号：「依赖/先后约束 + 可行性或顺序」→ 拓扑排序；「只问有没有环」→ Kahn 计数或三色 DFS 皆可。

## 实际工程应用
拓扑排序是构建系统的命脉：Makefile/Bazel 解析编译目标依赖图、go mod / npm 检测循环依赖、CI 流水线的 stage 调度、数据库外键迁移的顺序编排；微服务启动顺序编排、前端打包器（webpack）的模块依赖解析同样靠 Kahn 家族；调度系统里「任务有向无环图（DAG）」几乎是所有工作流引擎（Airflow、Argo）的数据模型核心。`,
    keyPoints: [
      "能修完 ⟺ 有拓扑序 ⟺ 无环：三命题等价，判环即解题",
      "Kahn 判环依据：环上点入度永 ≥1，永不入队，出队数 < n 即有环",
      "DFS 三色中「灰点」才是回边证据，单色 visited 会误判交叉边",
      "DAG 必有入度 0 点是 Kahn 可启动的前提，也是归纳的基石",
    ],
    followUps: [
      "DFS 三色标记怎么写？灰和黑两个状态分别代表什么？",
      "如果还要输出一个修课顺序呢？（LC 210，Kahn 出队序）",
      "怎么判断拓扑序是否唯一？（任意时刻队列长度恒为 1）",
    ],
    favorited: false,
  },
  {
    id: "algo-210",
    nodeId: "p2-graph",
    question: `210. 课程表 II（LeetCode 210）
题意：numCourses 门课，prerequisites[i] = [a, b] 表示修 a 前必须先修 b。返回一个合法的修课顺序（拓扑序）；若不可能完成（有环），返回空数组。
示例：输入 numCourses = 4, prerequisites = [[1,0],[2,0],[3,1],[3,2]] → 输出 [0,1,2,3] 或 [0,2,1,3]（先修 0，再修 1/2，最后 3）。
约束：1 ≤ numCourses ≤ 2000，0 ≤ prerequisites.length ≤ n×(n-1)，先修对不重复；答案不唯一时任返其一。`,
    answer: `## 题意与约束分析
LC 207 的升级版：不只判有无环，还要输出一个拓扑序。数据规模相同，O(V+E) 的 Kahn 出队序列天然就是拓扑序——把 207 的计数器换成记录数组即可。

## 思路推导：从暴力到最优
若用 DFS 三色 + 逆后序：每个点完成时压栈，结束倒序输出即拓扑序，实现稍绕。
Kahn 更直白：入度 0 的课「现在就能修」，出队顺序本身就是一种合法修课计划；每修完一门就解锁其后续（入度 -1）。不变式：任何时刻队列里的课其先修全部已出队。若最终序列长度 == n 则为合法拓扑序，否则有环返回空。答案不唯一 ⇒ 队列的弹出策略（FIFO/LIFO）不影响正确性。

## 正确性要点
- 出队即拓扑：归纳不变式——第 t 次出队的点入度为 0，其所有前驱已在序列前 t-1 项中；故序列中任意边的两端必先修在前、后续在后，满足全部约束。
- 完备性：DAG 必存在入度 0 点且删除后仍是 DAG ⇒ Kahn 在 DAG 上必然出队全部 n 点，不会漏解。
- 有环判定：出队数 < n ⟺ 剩余点入度均 ≥1 ⟺ 存在环（同 207 论证），此时返回空数组合法。

## 复杂度分析
- 时间：O(V+E)，对比「DFS 求逆后序」同阶，但常数更小、无递归
- 空间：O(V+E)，邻接表 + 入度 + 队列 + 答案数组

## 代码实现
\`\`\`go
func findOrder(numCourses int, prerequisites [][]int) []int {
    g := make([][]int, numCourses)
    indeg := make([]int, numCourses)
    for _, p := range prerequisites {
        g[p[1]] = append(g[p[1]], p[0]) // b -> a：先修指向后续
        indeg[p[0]]++
    }
    q := []int{}
    for i := 0; i < numCourses; i++ {
        if indeg[i] == 0 {
            q = append(q, i)
        }
    }
    order := make([]int, 0, numCourses)
    for len(q) > 0 {
        u := q[0]
        q = q[1:]
        order = append(order, u) // 出队顺序即修课顺序
        for _, v := range g[u] {
            indeg[v]--
            if indeg[v] == 0 {
                q = append(q, v) // 先修全部修完，解锁
            }
        }
    }
    if len(order) != numCourses {
        return []int{} // 有环，不存在拓扑序
    }
    return order
}
\`\`\`

## 边界与易错点
- 有环时必须返回空数组而非部分序列：漏写长度判断会返回一个不满足约束的前缀。
- 建边方向：序列要满足 b 在 a 前 ⇒ 边 b→a，先出队的是 b；方向反了输出的是「倒序课表」。
- 「任一合法序即可」不代表可以偷懒不建邻接表、每次 O(n) 扫全图找入度 0 点——那是 O(V^2) 退化写法。
- 零先修的孤立课：初始入度 0 全部入队，会出现在序列任意位置，合法。

## 举一反三
- LC 207 课程表：只要可行性，Kahn 计数即可。
- LC 269 火星词典：拓扑序还原字母表，含非法输入特判（前缀矛盾）。
- LC 310 最小高度树：「逐层剥叶子」是 Kahn 的入度 1 变体，剩 1~2 个中心。
- LC 444 序列重建：判断拓扑序是否唯一——队列任意时刻长度恒为 1 才唯一。
- 识别信号：「按依赖输出执行顺序」→ Kahn 出队序；「判唯一性」→ 看队列长度；「倒推关键路径」→ 逆图拓扑 + DP。

## 实际工程应用
输出拓扑序是调度器的日常：K8s 的 DAG 工作流（Argo Workflows）按依赖拓扑逐个起 Pod；数据库迁移工具（Flyway/Atlas）对 migration 按外键依赖排出执行序列；前端构建（Vite/Rollup）按模块依赖拓扑决定打包与加载顺序；大数据平台（DolphinScheduler）的 DAG 任务编排也要求给出一条可执行的拓扑链——Kahn 出队序就是这些系统里「下一个可运行任务」的生成逻辑。`,
    keyPoints: [
      "Kahn 出队序列本身就是拓扑序：计数器换成记录数组即从 207 升级",
      "不变式：队列中任意课的先修全部已出队，归纳即得合法性",
      "答案不唯一 ⇒ 队列用 FIFO 还是 LIFO 都不影响正确性",
      "有环返回空数组：长度判断是不可省略的收尾",
    ],
    followUps: [
      "用 DFS 逆后序怎么输出拓扑序？为什么要逆序？",
      "怎么判断这张图有没有唯一的拓扑序？",
      "如果每门课有耗时，怎么求修完所有课的最短时间？（拓扑 + 关键路径 DP）",
    ],
    favorited: false,
  },
  {
    id: "algo-130",
    nodeId: "p2-graph",
    question: `130. 被围绕的区域（LeetCode 130）
题意：给定 m×n 棋盘含 'X' 和 'O'，原地修改：所有被 'X' 四方向完全包围的 'O' 区域翻转为 'X'；与边界连通的 'O' 保留。
示例：输入 board = [["X","X","X"],["X","O","X"],["X","X","X"]] → 输出 [["X","X","X"],["X","X","X"],["X","X","X"]]（中心 O 被围，翻转）。
约束：1 ≤ m, n ≤ 200，board[i][j] ∈ {'X','O'}，原地修改。`,
    answer: `## 题意与约束分析
翻转「被 X 包围」的 O 区域。直接判定「是否被围」要双向搜索很麻烦；m,n ≤ 200 ⇒ O(mn) 染色即可。核心是逆向思维：与其找被围的，不如找「肯定不被围的」。

## 思路推导：从暴力到最优
正面思路：对每个 O 区域 Flood Fill 时检测是否触边界，实现要写「先收集区域、再决定翻不翻」的两段逻辑，易错。
逆向结构性质：「O 被保留 ⟺ 它与棋盘边界四连通」。边界上的 O 一定不被围；从所有边界 O 出发 Flood Fill 染色（标记成第三种颜色 '#'），染到的全部保留；染完后棋盘上剩下的 'O' 必然与边界不连通 ⟺ 被 X 完全包围，直接翻成 'X'；最后把 '#' 还原回 'O'。一次染色同时解决判定 + 保留 + 翻转。

## 正确性要点
- 保留判定的充要性：O 区域与边界连通 ⟺ 不被包围（包围的定义即「任何方向都无法通到棋盘外」），题目语义与连通性定义互为逆否。
- 染色的完备性：从全部边界 O 做 Flood Fill，可达性闭包恰好覆盖所有「与边界连通」的 O，无遗漏（四连通是等价关系）。
- 翻转的正确性：剩余未染色的 O 与边界不连通 ⇒ 四方向闭包内无边界 ⇒ 被 X 围死，翻转合法；'#' 全部还原后棋盘只含翻转结果，分类不重不漏。

## 复杂度分析
- 时间：O(mn)，每格最多被染色常数次
- 空间：O(1) 额外（原地三色，DFS 递归栈最坏 O(mn)）；BFS 版队列 O(mn)

## 代码实现
\`\`\`go
func solve(board [][]byte) {
    m, n := len(board), len(board[0])
    var dfs func(int, int)
    dfs = func(i, j int) {
        if i < 0 || i >= m || j < 0 || j >= n || board[i][j] != 'O' {
            return
        }
        board[i][j] = '#' // 与边界连通的 O 染安全色
        dfs(i-1, j)
        dfs(i+1, j)
        dfs(i, j-1)
        dfs(i, j+1)
    }
    for i := 0; i < m; i++ {
        dfs(i, 0)
        dfs(i, n-1) // 左右两列边界
    }
    for j := 0; j < n; j++ {
        dfs(0, j)
        dfs(m-1, j) // 上下两行边界
    }
    for i := 0; i < m; i++ {
        for j := 0; j < n; j++ {
            if board[i][j] == 'O' {
                board[i][j] = 'X' // 没染上的 O 才被包围
            } else if board[i][j] == '#' {
                board[i][j] = 'O' // 安全色还原
            }
        }
    }
}
\`\`\`

## 边界与易错点
- 正面硬做：先 Flood Fill 收集区域再查边界，区域大时要额外存储——逆向染色把「判定」变成「标记」，这才是出题点。
- 只从四个角染色：边界 O 不止在角上，必须扫四条边的全部格子。
- 染色与翻转的顺序：必须先染完再统一翻转；边染边翻会让逻辑纠缠，虽然本题翻成 X 恰好也是墙，但分两步才清晰可证。
- 空棋盘/全 X 棋盘：染色循环不触发，扫描不命中，天然安全。

## 举一反三
- LC 200 岛屿数量：连通块计数，Flood Fill 的元问题。
- LC 1254 统计封闭岛屿的数目：同款「不触边界」条件，先沉掉边界岛再数内部岛。
- LC 1020 飞地的数量：1 中不触边界的个数，同一逆向模板。
- LC 417 太平洋大西洋水流：双边界反向扩散取交集，逆向思维升级版。
- 识别信号：「找满足『不与边界/外部连通』的格子」→ 从边界反向染色，剩余即目标。

## 实际工程应用
「逆向可达性染色」在工程里高频出现：图片处理中抠除「封闭背景」（保留与图边连通的背景、删除封闭孔洞）；网络可达性分析中从公网反向标记可达主机，剩余内网段即为「被防火墙完全隔离」的区域；游戏 AI 中从安全区反向扩散标记，未染色区域即「被敌方包围」的死地——凡「包围/隔离」判定，先想想能不能从外部反向做。`,
    keyPoints: [
      "逆向思维：不找「被围的」，找「与边界连通的」，剩余即目标",
      "三染色法：O → #（安全）→ 还原，被围的 O 保持原色被翻 X",
      "保留 ⟺ 与边界连通：包围的语义与连通性互为逆否",
      "先完整染色再统一翻转，两阶段分离才好证明",
    ],
    followUps: [
      "为什么不直接对每个 O 区域判断是否触边界？逆向做法省了什么？",
      "如果只允许 BFS 不允许递归（栈深限制），怎么改？",
      "LC 1254 统计封闭岛屿数量与本题的思维有什么异同？",
    ],
    favorited: false,
  },
  {
    id: "algo-133",
    nodeId: "p2-graph",
    question: `133. 克隆图（LeetCode 133）
题意：给定无向连通图一个节点的引用，返回图的深拷贝。节点含 val 和 neighbors 列表，val 唯一且等于节点编号（1..100）。图可能有环、自环。
示例：输入 adjList = [[2,4],[1,3],[2,4],[1,3]]（4 节点环）→ 输出 同结构新图（值相同但全部是新建节点）。
约束：节点数 ∈ [0,100]，1 ≤ Node.val ≤ 100，无重复边；图连通（非空时）。`,
    answer: `## 题意与约束分析
深拷贝一张带环无向图：每个节点新建、邻居关系逐边复制，且同一旧节点只能克隆一次。难点不在遍历，而在「环上回访」——没有记忆化会在环上无限递归或重复建点。

## 思路推导：从暴力到最优
裸 DFS 递归克隆：沿邻居一路新建，遇到环（如 A→B→A）时 A 的副本还没建好就又要克隆 A，死循环。
结构性质：图遍历的「已访问」在克隆场景升级为「已创建」——用哈希表存 old→new 映射，DFS 进入节点 u 时先建副本并立刻登记映射，再递归克隆邻居；递归中若邻居已登记，直接返回其副本（回边指向已有副本，而非新建）。BFS 版同理：队列扩散 + 映射防重。「先登记再递归」是环安全的时序关键。

## 正确性要点
- 双射保持：映射表保证每个旧节点恰好克隆一次，新旧节点一一对应；邻居列表逐边复制 ⇒ 邻接结构同构。
- 环安全：进入 u 时「先登记 mp[u]=cp，再递归邻居」——环上的回边 dfs(u) 会命中已登记的映射而立即返回，不会重入 ⇒ 递归必终止且回边正确指向副本。
- 连通性保证：题目保证图连通（非空）⇒ 从入口单源遍历可覆盖全部节点，无遗漏；空图直接返回 nil。

## 复杂度分析
- 时间：O(V+E)，每节点建一次、每条边复制一次
- 空间：O(V)，哈希表 + 递归栈/队列

## 代码实现
\`\`\`go
func cloneGraph(node *Node) *Node {
    if node == nil {
        return nil
    }
    mp := map[*Node]*Node{} // 旧节点 -> 新节点
    var dfs func(*Node) *Node
    dfs = func(u *Node) *Node {
        if v, ok := mp[u]; ok {
            return v // 已克隆：直接复用，环上回边在此终止
        }
        cp := &Node{Val: u.Val}
        mp[u] = cp // 先登记再递归，环安全的关键时序
        for _, nb := range u.Neighbors {
            cp.Neighbors = append(cp.Neighbors, dfs(nb))
        }
        return cp
    }
    return dfs(node)
}
\`\`\`

## 边界与易错点
- 「先递归后登记」：环上无限递归爆栈——登记必须发生在遍历邻居之前，这是本题唯一核心坑。
- 用 val 当 key 还是指针当 key：本题 val 唯一所以可行，但指针 key 更通用（不依赖 val 唯一性约束）；面试说清两种选择。
- 深拷贝 vs 浅拷贝：只复制节点不复制邻居列表，或邻居里放旧节点指针，都是浅拷贝——判题器会检查新旧地址不同。
- 自环：u 的邻居含自身，dfs(u) 命中已登记映射直接返回自身副本，天然正确。
- BFS 版容易漏登记时机：入队时登记、出队时补邻居，与 DFS 同原理。

## 举一反三
- LC 138 复制带随机指针的链表：同款 old→new 映射（或交错复制法省哈希表），识别信号「非树结构深拷贝」。
- LC 1485 克隆含随机指针的二叉树：树版 + random 指针，映射表同款。
- LC 1490 克隆 N 叉树：无环无随机指针，纯递归即可，无需映射。
- 识别信号：「深拷贝带环/交叉引用的结构」→ 遍历 + old→new 哈希映射，先登记后展开。

## 实际工程应用
old→new 映射是序列化/反序列化的内核：Protobuf、Java 序列化处理对象图循环引用时就是这套「身份映射表」（reference tracking）；ORM 的 Unit of Work 模式用身份映射保证同一行记录在会话内只有一份对象；前端状态管理（如 Immer 的结构共享）和图数据库的 subgraph 抽取复制，本质也都是带映射表的图深拷贝。`,
    keyPoints: [
      "核心结构：old→new 哈希映射，保证每个旧节点恰好克隆一次",
      "环安全的时序：先登记映射再递归邻居，回边命中映射即终止",
      "克隆 = 遍历 + 防重：DFS/BFS 都行，映射表承担了 visited 职责",
      "指针做 key 比 val 做 key 更通用，不依赖题给的唯一性约束",
    ],
    followUps: [
      "如果节点的 val 不唯一，映射表 key 该怎么选？",
      "用 BFS 怎么写？登记映射的时机与 DFS 有何异同？",
      "LC 138（带随机指针链表）的「交错复制法」为什么能省掉哈希表？",
    ],
    favorited: false,
  },
  {
    id: "algo-417",
    nodeId: "p2-graph",
    question: `417. 太平洋大西洋水流（LeetCode 417）
题意：m×n 矩阵每格是海拔，雨水只能从高向低（或等）向四方向流；矩阵上/左边缘邻太平洋，下/右边缘邻大西洋。返回雨水能同时流到两大洋的所有坐标。
示例：输入 heights = [[1,2,2,3,5],[3,2,3,4,4],[2,4,5,3,1],[6,7,1,4,5],[5,1,1,2,4]] → 输出 [[0,4],[1,3],[1,4],[2,2],[3,0],[3,1],[4,0]]。
约束：1 ≤ m, n ≤ 200，0 ≤ heights[i][j] ≤ 10^5。`,
    answer: `## 题意与约束分析
找「顺流而下既能到太平洋、又能到大西洋」的格子。m,n ≤ 200 ⇒ 4 万格。若对每个格子正向 DFS 判定，最坏 O((mn)^2)；逆向从海洋边界扩散，两次 O(mn) 即可。

## 思路推导：从暴力到最优
暴力：逐格 DFS/BFS 模拟水流，记录能否抵达两大洋——重复遍历，超时。
逆向结构性质：水从格子流向海洋 ⟺ 从海洋「逆流爬坡」可达该格子（沿「高度不下降」方向走）。水流方向反转后，「能流入太平洋的格子集」=「从太平洋边界逆流可达的格子集」——于是从太平洋边界（上行+左列）做一次 DFS 标记 pac 集合，从大西洋边界（下行+右列）做一次标记 atl 集合，两集合取交集即答案。逆向一次遍历替代了逐点正向模拟。

## 正确性要点
- 逆流等价：格子 c 能顺流到边界 b ⟺ 存在路径 c→…→b 高度单调不增 ⟺ 反向路径 b→…→c 高度单调不降 ⟺ c 从边界逆流可达——充要条件，标记无重无漏。
- 交集的语义：c ∈ pac 且 c ∈ atl ⟺ 可分别流到两大洋 ⟺ 满足题意；两个集合各自完备，交也完备。
- 单调性终止：DFS 只走「更高或等」且带 visited，每格每洋最多访问一次，必终止。

## 复杂度分析
- 时间：O(mn)，两次填充，每格每洋最多访问一次，对比暴力 O((mn)^2)
- 空间：O(mn)，两个布尔矩阵 + 递归栈

## 代码实现
\`\`\`go
func pacificAtlantic(heights [][]int) [][]int {
    m, n := len(heights), len(heights[0])
    pac, atl := make([][]bool, m), make([][]bool, m)
    for i := range pac {
        pac[i], atl[i] = make([]bool, n), make([]bool, n)
    }
    dirs := [][2]int{{-1, 0}, {1, 0}, {0, -1}, {0, 1}}
    var dfs func(int, int, [][]bool)
    dfs = func(i, j int, seen [][]bool) {
        seen[i][j] = true
        for _, d := range dirs {
            x, y := i+d[0], j+d[1]
            if x >= 0 && x < m && y >= 0 && y < n && !seen[x][y] && heights[x][y] >= heights[i][j] {
                dfs(x, y, seen) // 逆流：只走向更高或等高处
            }
        }
    }
    for i := 0; i < m; i++ {
        dfs(i, 0, pac)   // 太平洋：左边界
        dfs(i, n-1, atl) // 大西洋：右边界
    }
    for j := 0; j < n; j++ {
        dfs(0, j, pac)   // 太平洋：上边界
        dfs(m-1, j, atl) // 大西洋：下边界
    }
    ans := [][]int{}
    for i := 0; i < m; i++ {
        for j := 0; j < n; j++ {
            if pac[i][j] && atl[i][j] {
                ans = append(ans, []int{i, j})
            }
        }
    }
    return ans
}
\`\`\`

## 边界与易错点
- 正向逐格 DFS：超时 + 状态重复，除非加双维度记忆化（每格记 pac/atl 两态），实现远比逆向复杂。
- 逆流方向写反：应走 heights[x][y] >= heights[i][j]（越高越能来水）；写成 <= 就成了顺流。
- 边界漏扫：太平洋 = 上行 + 左列两条边，大西洋 = 下行 + 右列，四条边缺一不可；角点被两条边各起一次，visited 去重无副作用。
- 等高地势可以互流（条件含等号），漏掉等号会漏解。

## 举一反三
- LC 130 被围绕的区域：边界反向染色，逆向思维同宗。
- LC 1020 飞地的数量：同「从边界标记、剩余即目标」。
- LC 329 矩阵中的最长递增路径：同样利用「单调路径」，但用 DFS + 记忆化 DP 求极值。
- 识别信号：「从每个点出发能否到达某类边界/汇点」→ 反转方向，从边界/汇点反向扩散标记，取集合运算。

## 实际工程应用
「反向可达性」是网络与流分析的常规武器：网络排障中从多个汇聚点反向 traceroute 标记可达域，交集即「双归可达」的高可用节点；流域地理分析（GIS）里反向坡度追踪划定分水岭；供应链里「既能从供应商 A 也能从 B 供到」的工厂集合用于双源容灾评估；数据血缘中从多个下游报表反向追溯上游表集合取交集，定位关键公共依赖。`,
    keyPoints: [
      "逆向：顺流可达海洋 ⟺ 从海洋逆流爬坡可达该格，方向反转充要成立",
      "两次边界 DFS 得两个集合，交集即答案：O(mn) 替代 O((mn)^2)",
      "逆流条件是 >=（含等号），等高地势互流，漏等号丢解",
      "太平洋占上+左两条边，大西洋占下+右，四条边缺一不可",
    ],
    followUps: [
      "正向逐格 DFS 加记忆化也能过，为什么说逆向更优？",
      "如果要求「只能流到太平洋、流不到大西洋」的格子呢？（集合差）",
      "把网格改成一般有向图，这个逆向思想还成立吗？（成立：反向图从汇点出发）",
    ],
    favorited: false,
  },
  {
    id: "algo-684",
    nodeId: "p2-graph",
    question: `684. 冗余连接（LeetCode 684）
题意：一棵树（n 节点 n-1 边）多加一条边形成含环图，edges 按输入顺序给出。返回最后一条使图出现环的边（删除它后图恢复为树）。
示例：输入 edges = [[1,2],[1,3],[2,3]] → 输出 [2,3]（1-2-3-1 成环，[2,3] 是最后出现的冗余边）。
约束：n = edges.length ∈ [3,1000]，1 ≤ edges[i][0] < edges[i][1] ≤ n，无重边。`,
    answer: `## 题意与约束分析
逐边加入，第一条「两端已连通」的边即冗余边——这是并查集的标准判环。n ≤ 1000 随便过，但并查集的「路径压缩 + 按秩合并」把每次操作压到近 O(1)，是必须讲透的范式。

## 思路推导：从暴力到最优
暴力：每加一条边，跑一次 DFS/BFS 判断两端是否已连通，O(n) 每次，总 O(n^2)。
结构性质：无向图加边判环 = 动态连通性问题。并查集维护「每个集合的代表元（根）」：find(x) 沿父指针上溯根并路径压缩；union(x,y) 先查两根，同根 ⟺ 已连通 ⟺ 当前边成环，直接返回；否则按秩把矮树挂到高树下，保持树矮。路径压缩 + 按秩合并后，单次操作摊还 O(α(n))，α 为反阿克曼函数，实际近常数。

## 正确性要点
- 判环充要：union 时两端同根 ⟺ 已存在路径连接 ⟺ 新边闭合出环；不同根则合并后仍无环（归纳：初始 n 个独立点无环，每次合并不产生环）——按序第一对同根边即题目所求。
- 路径压缩不改语义：find 只改父指针指向根，集合成员关系不变，返回根正确。
- 按秩合并保高度：矮挂高使合并后高度不增（除非等高 +1）⇒ 树高 O(log n)，叠加压缩后摊还近 O(1)。

## 复杂度分析
- 时间：O(n·α(n)) ≈ O(n)，对比暴力 O(n^2)
- 空间：O(n)，parent 与 rank 数组

## 代码实现
\`\`\`go
func findRedundantConnection(edges [][]int) []int {
    n := len(edges)
    parent := make([]int, n+1)
    rank := make([]int, n+1)
    for i := 1; i <= n; i++ {
        parent[i] = i // 初始各自成集
    }
    var find func(int) int
    find = func(x int) int {
        if parent[x] != x {
            parent[x] = find(parent[x]) // 路径压缩：沿途挂到根
        }
        return parent[x]
    }
    union := func(x, y int) bool {
        rx, ry := find(x), find(y)
        if rx == ry {
            return false // 已连通，当前边成环
        }
        if rank[rx] < rank[ry] { // 按秩合并：矮树挂高树
            rx, ry = ry, rx
        }
        parent[ry] = rx
        if rank[rx] == rank[ry] {
            rank[rx]++
        }
        return true
    }
    for _, e := range edges {
        if !union(e[0], e[1]) {
            return e // 按输入顺序，第一对冗余即答案
        }
    }
    return nil
}
\`\`\`

## 边界与易错点
- 忘路径压缩只写按秩（或反之）：仍正确但单次退到 O(log n)；两个都不写最坏退化成链 O(n)——本题 1000 规模看不出，面试必问。
- 按秩合并时挂反方向（高挂矮）：树高失控，复杂度保证失效；「矮挂高」记牢。
- 下标从 1 开始：parent 数组开 n+1，别用 0..n-1 错位。
- 题目保证「树 + 一条边」⇒ 答案唯一且必存在，按序第一对同根即解；若推广到任意图，「最后一条冗余」的定义要重新审题。

## 举一反三
- LC 685 冗余连接 II：有向图版，分「入度为 2」与「有环」三类讨论，并查集仍可收尾。
- LC 547 省份数量：并查集数连通块，识别信号「连通分量计数」。
- LC 1319 连通网络的操作次数：连通块数 -1 即最少接线数。
- LC 399 除法求值：带权并查集（维护到根的比值），并查集进阶必刷。
- 识别信号：「动态加边 + 连通性/环查询」→ 并查集；「需要撤销边」→ 可回滚并查集（只按秩不压缩）；「带边权传递量」→ 带权并查集。

## 实际工程应用
并查集是「等价类合并」的工业级结构：Kruskal 最小生成树算法用它判环（网络布线、电路设计）；社交网络中「好友关系合并」算共同圈子、推荐系统的用户簇合并；分布式系统中节点分组/故障域划分；编译器里类型推导的 union-find（Hindley-Milner）；图像分割的区域合并（超像素聚合）——凡「合并 + 查代表」的在线场景，并查集几乎无可替代。`,
    keyPoints: [
      "无向图加边判环 = 动态连通性：union 时同根即成环",
      "路径压缩 + 按秩合并，单次操作摊还 O(α(n)) 近常数",
      "路径压缩不改集合语义，按秩合并把树高压在 O(log n)",
      "按序处理 + 第一对同根返回，恰好对上「最后的冗余边」",
    ],
    followUps: [
      "路径压缩和按秩合并各自贡献什么？只做一个是多少复杂度？",
      "如果边是有向的（LC 685），并查集还能直接判环吗？",
      "需要支持「删边/回滚」时并查集怎么改？（去掉路径压缩，按秩 + 操作栈）",
    ],
    favorited: false,
  },
  {
    id: "algo-787",
    nodeId: "p2-graph",
    question: `787. K 站中转内最便宜航班（LeetCode 787）
题意：n 个城市，flights[i] = [from, to, price] 为有向航班。求从 src 到 dst、中转不超过 k 次（即最多经过 k+1 条边）的最低票价；不可达返回 -1。
示例：输入 n = 4, flights = [[0,1,100],[1,2,100],[2,3,100],[0,3,500]], src = 0, dst = 3, k = 1 → 输出 500（直飞 500；0→1→2→3 虽 300 但中转 2 次超限）。
约束：1 ≤ n ≤ 100，航班数 ≤ n×(n-1)/2，票价 ∈ [1,10^4]，无自环重边。`,
    answer: `## 题意与约束分析
带边数上限的最短路：「中转 ≤ k 次」=「路径边数 ≤ k+1」。边数限制让 Dijkstra 失效（最短路可能边数超标），必须用「按轮松弛」的 Bellman-Ford 家族。n ≤ 100、k ≤ n-1，O(k·E) 稳过。

## 思路推导：从暴力到最优
暴力 DFS 枚举所有 ≤ k+1 边的路径，指数级出局。
结构性质：Bellman-Ford 第 t 轮松弛结束后，dist 数组恰为「至多 t 条边」的最短路（归纳可证）——这个「按边数分层」的性质恰好对上中转限制。于是只跑 k+1 轮松弛：每轮用上一轮的距离快照更新所有边。关键是必须用上一轮副本（滚动数组），否则同一轮内「边链式接力」会偷偷突破边数上限。也可用分层 BFS/DP（dist[t][v]），本质同构。

## 正确性要点
- 轮次语义：归纳证明「第 t 轮后 dist[v] = 至多 t 条边到 v 的最优值」：t=0 只有 src=0；第 t 轮用 t-1 轮的 dist 对所有边松弛，得到的恰是至多 t 条边的最优（多一条边的路径 = 少一条边的最优 + 一条边）。
- 副本的必要性：用 next 数组承接本轮结果、源数据用上一轮 dist，保证「每条路径本轮至多新增一条边」；原地更新会让 u→v→w 在同一轮连跳两条边，破坏轮次语义。
- k+1 轮截断：中转 k 次 ⟺ k+1 条边，跑满即停，答案天然满足约束；不可达时 dist[dst] 仍为无穷。

## 复杂度分析
- 时间：O(k·E)，对比 Dijkstra 的 O(E log V)——后者不支持边数限制；n=100 时约 10^6
- 空间：O(n)，两个滚动数组

## 代码实现
\`\`\`go
func findCheapestPrice(n int, flights [][]int, src int, dst int, k int) int {
    const inf = int(1e9)
    dist := make([]int, n)
    for i := range dist {
        dist[i] = inf
    }
    dist[src] = 0
    for round := 0; round <= k; round++ { // 第 t 轮 = 至多 t 条边，共 k+1 轮
        next := make([]int, n)
        copy(next, dist) // 只用上一轮快照松弛，防同轮连跳
        relaxed := false
        for _, f := range flights {
            if dist[f[0]] != inf && dist[f[0]]+f[2] < next[f[1]] {
                next[f[1]] = dist[f[0]] + f[2]
                relaxed = true
            }
        }
        dist = next
        if !relaxed {
            break // 无变化提前收敛
        }
    }
    if dist[dst] == inf {
        return -1
    }
    return dist[dst]
}
\`\`\`

## 边界与易错点
- 原地松弛：同轮内 u→v→w 连跳，把「2 条边的路径」算进「第 1 轮」，k=0（直飞）场景立刻露馅——滚动副本是灵魂。
- 轮数差一：中转 k 次是 k+1 条边，循环要跑 k+1 次（round 从 0 到 k 含），少一轮漏最优解。
- inf 参与加法：dist[u]==inf 时加票价会溢出/误判，必须先判 inf 再松弛。
- 试图上 Dijkstra：堆优化贪心不感知边数，「便宜的绕远路」可能被边数限制排除而 Dijkstra 无法表达；状态加成 (点, 已用边数) 是分层图最短路，可行但代码量大于 Bellman-Ford。

## 举一反三
- LC 1928 规定时间内到达终点的最小花费：同款「限制维度 + 费用」分层 DP/Bellman 变体。
- LC 743 网络延迟时间：无边数限制的标准单源最短路，Dijkstra。
- LC 1334 阈值距离内邻居最少的城市：Floyd/Bellman 家族求全源距离再统计。
- 识别信号：「最短路 + 边数/中转/时间等额外配额」→ 按配额分层的 Bellman-Ford 或 (点,层) 二元状态 Dijkstra；纯非负权无限制 → Dijkstra。

## 实际工程应用
限轮 Bellman-Ford 即「分层最短路」：机票搜索的「最多中转 N 次最低价」就是本题原型；RIP 路由协议基于 Bellman-Ford 且跳数上限 15 防环；金融跨境汇款「至多 N 家中转行最低手续费」、物流「限 N 个中转仓的干线方案」——凡「配额约束下的最优路径」，按配额轮次松弛是最可靠的工程解。`,
    keyPoints: [
      "Bellman-Ford 第 t 轮 = 至多 t 条边的最短路，天然对上中转配额",
      "滚动副本（上一轮快照）是灵魂：原地更新会同轮连跳、突破边数上限",
      "中转 k 次 = k+1 条边，循环轮数差一是高频挂法",
      "Dijkstra 不感知边数，配额约束下要么限轮 BF 要么 (点,层) 分层图",
    ],
    followUps: [
      "为什么原地更新会错？请用 k=0 的反例说明。",
      "改成 (点, 已用边数) 状态的 Dijkstra 怎么写？复杂度多少？",
      "如果票价可以为负，还能用这套轮次松弛吗？负环怎么检测？",
    ],
    favorited: false,
  },
  {
    id: "algo-743",
    nodeId: "p2-graph",
    question: `743. 网络延迟时间（LeetCode 743）
题意：n 个节点，times[i] = [u, v, w] 表示 u 到 v 的有向信号耗时 w。从 k 发出信号，返回所有节点收到信号的最早时刻中的最大值（即全网最晚收到时间）；有节点收不到返回 -1。
示例：输入 times = [[2,1,1],[2,3,1],[3,4,1]], n = 4, k = 2 → 输出 2（节点 4 经 2→3→4 耗时 2，最晚）。
约束：1 ≤ n, k ≤ 100，1 ≤ w ≤ 100，无重边；节点编号 1..n。`,
    answer: `## 题意与约束分析
单源非负权最短路 + 取最大值，Dijkstra 的标准模板。「非负权」是 Dijkstra 成立的生命线；n ≤ 100 任何写法都过，但堆优化版 O(E log V) 是面试期望答案。

## 思路推导：从暴力到最优
Bellman-Ford 每轮全边松弛 O(V·E) 可解但浪费：非负权下有更强结构。
结构性质（贪心 + 永久化）：dist 值最小的未确定节点 u，其 dist 不可能再被任何其他节点松弛变小——因为所有边权非负，绕道只会更远。于是每次用最小堆取出当前最小的 dist[u] 即「定案」，用它松弛所有出边；失效堆项（dist 已更新的旧值）惰性跳过。每点定案一次、每边松弛一次，堆操作 O(log V)。

## 正确性要点
- 永久化论证：堆顶取出最小 dist[u] 时，假设还能更短，则最短路径上存在另一未确定点 x 使 dist[x]+w(x..u) < dist[u]；但 w ≥ 0 ⇒ dist[x] ≤ dist[u]，与 u 是堆顶最小矛盾——故出堆即定案，归纳得全部 dist 正确。
- 懒删除合法：同一节点多次入堆（每次找到更短值推入），出堆时若 d > dist[u] 说明是过期项直接丢弃；每个节点至多被「有效处理」一次。
- 最大值语义：信号并行传播，全网就绪时间 = max(dist)；任一 dist 为无穷 ⟺ 图不连通，返回 -1。

## 复杂度分析
- 时间：O(E log V)，每边至多触发一次堆推入，对比朴素数组版 O(V^2)、Bellman-Ford O(V·E)
- 空间：O(V+E)，邻接表 + dist + 堆

## 代码实现
\`\`\`go
import "container/heap"

type item struct{ node, d int }
type minHeap []item

func (h minHeap) Len() int            { return len(h) }
func (h minHeap) Less(i, j int) bool  { return h[i].d < h[j].d }
func (h minHeap) Swap(i, j int)       { h[i], h[j] = h[j], h[i] }
func (h *minHeap) Push(x interface{}) { *h = append(*h, x.(item)) }
func (h *minHeap) Pop() interface{} {
    old := *h
    n := len(old)
    it := old[n-1]
    *h = old[:n-1]
    return it
}

func networkDelayTime(times [][]int, n int, k int) int {
    g := make([][][2]int, n+1)
    for _, t := range times {
        g[t[0]] = append(g[t[0]], [2]int{t[1], t[2]}) // u -> v 权 w
    }
    const inf = int(1e9)
    dist := make([]int, n+1)
    for i := range dist {
        dist[i] = inf
    }
    dist[k] = 0
    h := &minHeap{{node: k, d: 0}}
    for h.Len() > 0 {
        cur := heap.Pop(h).(item)
        if cur.d > dist[cur.node] {
            continue // 过期堆项，懒删除
        }
        for _, e := range g[cur.node] {
            if nd := cur.d + e[1]; nd < dist[e[0]] {
                dist[e[0]] = nd
                heap.Push(h, item{node: e[0], d: nd}) // 松弛成功，推入新值
            }
        }
    }
    ans := 0
    for i := 1; i <= n; i++ {
        if dist[i] == inf {
            return -1 // 有节点不可达
        }
        if dist[i] > ans {
            ans = dist[i]
        }
    }
    return ans
}
\`\`\`

## 边界与易错点
- 负权边用 Dijkstra：贪心永久化失效（绕负边能更短），必须换 Bellman-Ford/SPFA——本题权值 ≥1 安全，但面试官必问适用前提。
- 忘懒删除判 d > dist[u]：同点旧值重复处理，逻辑仍对但常数变大，稠密图上显著。
- 有向边：times 是 u→v，建图别加成双向。
- 节点编号 1..n：数组开 n+1；k 是源点编号，dist[k]=0 初始化漏了就全图不可达。
- 答案是 max(dist) 而非 dist 之和：信号并行扩散。

## 举一反三
- LC 1514 概率最大的路径：把「最小和」换成「最大积」，同模板的堆化贪心（概率 ≤1 保证正确性）。
- LC 1631 最小体力消耗路径：路径值 = 相邻高度差最大值的最小化，「度量变形」的 Dijkstra。
- LC 787 K 站中转内最便宜航班：加边数限制后 Dijkstra 失效，见该题分析。
- LC 1368 使网格图至少有一条有效路径的最小代价：0-1 权图，双端队列 BFS（0-1 BFS）是 Dijkstra 特例。
- 识别信号：「单源 + 非负权 + 最短路/最小代价」→ Dijkstra；权 ∈ {0,1} → 0-1 BFS 更快。

## 实际工程应用
Dijkstra 是网络路由的基石：OSPF/IS-IS 链路状态协议每台路由器都跑它算最短路径树；地图导航的路由规划（叠加 A* 启发）、CDN 任播节点选择、游戏寻路（A* 是 Dijkstra + 启发函数）也都是它——凡「非负代价 + 单源最优」图问题，工程默认答案就是 Dijkstra。`,
    keyPoints: [
      "Dijkstra 生命线是非负权：堆顶出队即定案，不可能再被松弛",
      "懒删除：更短值直接推堆，出堆时判 d > dist[u] 丢弃过期项",
      "答案是 max(dist)：信号并行传播，最晚收到时间即全网延迟",
      "dist[k]=0 初始化 + 有向建图 + 1-based 下标，三个低级坑",
    ],
    followUps: [
      "证明为什么非负权下「堆顶出队即永久化」成立；有负权时哪个环节崩？",
      "朴素 O(V^2) 版和堆优化版各自适合什么密度的图？",
      "权值只会是 0 或 1 时，有什么比堆更快的做法？（0-1 BFS 双端队列）",
    ],
    favorited: false,
  },
  {
    id: "algo-208",
    nodeId: "p2-graph",
    question: `208. 实现 Trie 前缀树（LeetCode 208）
题意：实现 Trie 类：insert(word) 插入单词；search(word) 判断完整单词是否存在；startsWith(prefix) 判断是否有以 prefix 为前缀的单词。仅含小写英文字母。
示例：输入 insert("apple"), search("apple") → true, search("app") → false, startsWith("app") → true, insert("app"), search("app") → true。
约束：1 ≤ word.length, prefix.length ≤ 2000，总操作数 ≤ 3×10^4，仅小写字母。`,
    answer: `## 题意与约束分析
多模式串的「前缀级」检索结构：插入、整词匹配、前缀匹配都要 O(L)（L 为串长）。3×10^4 次操作、串长 2000 ⇒ 每次必须 O(L)。哈希表做 search 可以，但 startsWith 无能为力——前缀共享结构是本题唯一解。

## 思路推导：从暴力到最优
暴力：存数组，search 逐个比对 O(N·L)，startsWith 同理。
哈希表：search O(L)，但前缀查询要枚举全部 key 再比对，退到 O(N·L)。
结构性质：把「公共前缀合并成一条路径」——26 叉树，根到某节点的路径拼出前缀，节点带 isEnd 标记「有完整单词在此结束」。插入逐字符走路、缺节点就建；search 走到底且 isEnd 才 true；startsWith 只要求走到底。前缀查询 O(L) 且与词典规模 N 无关——共享前缀既省空间又让前缀操作天然廉价。

## 正确性要点
- 路径-前缀双射：根到节点 v 的边序列唯一拼出一个前缀 p，v 存在 ⟺ 词典中有串以 p 为前缀 ⇒ startsWith 只需沿路走通。
- isEnd 的区分语义：search = 走通 + isEnd；startsWith = 走通。app 是 apple 的前缀但不是已插入单词时，两结果不同，isEnd 精确承载这个区别。
- 插入的幂等性：重复插入同词只重复走老路径并置 isEnd=true，结构不变，结果仍正确。

## 复杂度分析
- 时间：insert/search/startsWith 均 O(L)，与词典大小无关，对比哈希表 startsWith 的 O(N·L)
- 空间：O(ΣL)，最坏无公共前缀时等于全部字符数（每节点 26 个指针槽位）

## 代码实现
\`\`\`go
type Trie struct {
    children [26]*Trie
    isEnd    bool // 有完整单词在此结束
}

func Constructor() Trie {
    return Trie{}
}

func (t *Trie) Insert(word string) {
    node := t
    for _, c := range word {
        i := c - 'a'
        if node.children[i] == nil {
            node.children[i] = &Trie{} // 缺路补路
        }
        node = node.children[i]
    }
    node.isEnd = true
}

func (t *Trie) Search(word string) bool {
    node := t
    for _, c := range word {
        i := c - 'a'
        if node.children[i] == nil {
            return false
        }
        node = node.children[i]
    }
    return node.isEnd // 整词：必须走到且是结尾
}

func (t *Trie) StartsWith(prefix string) bool {
    node := t
    for _, c := range prefix {
        i := c - 'a'
        if node.children[i] == nil {
            return false
        }
        node = node.children[i]
    }
    return true // 前缀：走通即可，不要求 isEnd
}
\`\`\`

## 边界与易错点
- search 与 startsWith 混用：search 忘查 isEnd 会把前缀当单词；抽一个 walk 函数返回末节点再分别判断，是防错好封装。
- 用 [26]*Trie 还是 map[rune]*Trie：字母表小且固定用数组快；字符集大（中文/Unicode）必须换 map，稀疏才省空间。
- Go 里 for-range 拿到 rune，c - 'a' 转 int 作下标；纯小写 ASCII 按 byte 遍历更快。
- 删除操作（进阶）：不能简单置 isEnd=false 完事——若该词是更长词的前缀，子树要保留，需引用计数或回溯清空头节点。

## 举一反三
- LC 211 添加与搜索单词：search 含 '.' 通配，Trie + DFS 分支。
- LC 212 单词搜索 II：棋盘 + Trie 剪枝，「把词典建成 Trie 再 DFS」是经典提速。
- LC 648 单词替换：Trie 找最短前缀替换，识别信号「词根匹配」。
- LC 421 数组中两个数的最大异或值：二进制位建 0/1 Trie，「数值即字符串」的妙用。
- 识别信号：「大量字符串 + 前缀查询/前缀匹配加速」→ Trie；「只做精确匹配」→ 哈希表更简单。

## 实际工程应用
Trie 是检索基础设施：输入法/搜索框自动补全（前缀取 Top-K 候选）、IP 路由表最长前缀匹配（LPM 即 0/1 Trie，路由器转发核心）、敏感词过滤（AC 自动机 = Trie + KMP 失配指针）、Lucene 倒排词典（FST 是压缩版 Trie）——「前缀共享」思想贯穿整个字符串检索栈。`,
    keyPoints: [
      "公共前缀合并成路径：insert/search/startsWith 全 O(L)，与词典规模无关",
      "isEnd 区分「是前缀」与「是完整单词」，是 search 与 startsWith 的唯一差别",
      "Trie vs 哈希表：精确匹配打平，前缀匹配哈希表完败",
      "字母表小用数组槽位，大字符集（中文）换 map 才省空间",
    ],
    followUps: [
      "怎么支持删除单词？要注意什么坑？",
      "search 支持 '.' 通配符怎么改？（LC 211，通配处 DFS 全分支）",
      "中文输入法的词库该用数组子节点还是 map？为什么？",
    ],
    favorited: false,
  },
  {
    id: "algo-399",
    nodeId: "p2-graph",
    question: `399. 除法求值（LeetCode 399）
题意：equations[i] = [a, b] 与 values[i] 表示 a/b = values[i]。对每个查询 [c, d] 返回 c/d；若 c 或 d 未出现、或无法推导，返回 -1.0。
示例：输入 equations = [["a","b"],["b","c"]], values = [2.0,3.0], queries = [["a","c"],["b","a"],["a","e"],["a","a"],["x","x"]] → 输出 [6.0,0.5,-1.0,1.0,-1.0]（a/c = a/b × b/c = 6）。
约束：1 ≤ equations.length ≤ 20，values[i] > 0，查询数 ≤ 20，变量名为小写串。`,
    answer: `## 题意与约束分析
把除法关系建成图：变量是点，a/b=v 是边 a→b 权 v、b→a 权 1/v。查询 c/d = 路径上边权连乘积。规模极小（≤20 式）⇒ DFS 足矣，但「比值沿路径可传递」的建图思想与带权并查集是考察重点。

## 思路推导：从暴力到最优
暴力：对每个查询试图代数消元，无章法。
结构性质：a/b、b/c 已知 ⇒ a/c = (a/b)×(b/c)——比值是「可沿路径累乘的边权」。建图后 c/d 合法 ⟺ c、d 在同一连通分量（同分量内任意两点的比值由生成树路径积确定且自洽——题目保证无矛盾）。于是每次查询从 c DFS 到 d，沿途累乘边权即可。进阶：Floyd 预处理全源比值 O(V^3)，或带权并查集维护「到根的比值」把查询压到近 O(1)。

## 正确性要点
- 路径积不变式：沿路径 c=x0→x1→…→xk=d，连乘 w(xi,xi+1) = (x0/x1)(x1/x2)…(xk-1/xk) = c/d（望远镜积，中间量全约掉）——任一路径给出同一正确值（题目无矛盾约束）。
- 双向边完备：建图同时加 v 与 1/v 的反向边 ⟺ 除法的可逆性；DFS 在反向边上走时自动取倒数，方向任意仍正确。
- -1 的三种情形：c 或 d 不在图中，或在不同连通分量 ⟺ 无法推导；DFS 未达 target 返回 -1，c==d 且存在时返回 1.0，分类完备。

## 复杂度分析
- 时间：建图 O(E)，每次查询 DFS O(V+E)；E ≤ 20 毫无压力，对比 Floyd 预处理 O(V^3) + 查询 O(1)
- 空间：O(V+E)，邻接表 + 访问标记

## 代码实现
\`\`\`go
func calcEquation(equations [][]string, values []float64, queries [][]string) []float64 {
    g := map[string]map[string]float64{}
    for i, eq := range equations {
        a, b := eq[0], eq[1]
        if g[a] == nil {
            g[a] = map[string]float64{}
        }
        if g[b] == nil {
            g[b] = map[string]float64{}
        }
        g[a][b] = values[i]       // a / b = values[i]
        g[b][a] = 1.0 / values[i] // 反向边取倒数
    }
    var dfs func(string, string, map[string]bool) float64
    dfs = func(u, target string, seen map[string]bool) float64 {
        if _, ok := g[u]; !ok {
            return -1.0 // 变量未出现
        }
        if u == target {
            return 1.0
        }
        seen[u] = true
        for v, w := range g[u] {
            if !seen[v] {
                if sub := dfs(v, target, seen); sub > 0 {
                    return w * sub // 望远镜连乘
                }
            }
        }
        return -1.0 // 本分支不通
    }
    ans := make([]float64, len(queries))
    for i, q := range queries {
        ans[i] = dfs(q[0], q[1], map[string]bool{})
    }
    return ans
}
\`\`\`

## 边界与易错点
- 反向边漏建或忘取倒数：查询 b/a 直接死路；1.0/values[i] 与 values[i] 必须成对。
- c == d 的特判：c 在图中时答案 1.0，c 不在图中时 -1.0——DFS 里「先查存在再比等」的顺序正好覆盖，别写反。
- 环：equations 可成环（a/b、b/c、c/a），visited 标记防死循环；成环且题目无矛盾时任意路径积一致。
- 值域：values > 0 保证连乘为正，可用「sub > 0 即找到」当哨兵；若允许负值要用 NaN 或 (found, value) 二元返回。
- 带权并查集版：find 时维护 x/root 比值并随路径压缩更新，union 时按比值方程挂根——权更新方程写错是常见挂法。

## 举一反三
- LC 684 冗余连接：裸并查集，本题的「带权」是其进阶。
- LC 990 等式方程的可满足性：等/不等约束，并查集判矛盾，识别信号「约束传播」。
- LC 737 句子相似性 II：同义词传递闭包 = 连通分量，同款建图思想。
- Floyd 变体：查询量大时（如 10^5 次）预处理比值矩阵，查询 O(1)。
- 识别信号：「变量间比例/等式关系 + 传递推导」→ 带权图遍历或带权并查集；「纯连通关系」→ 普通并查集。

## 实际工程应用
「比值图」在汇率与单位换算中是直接原型：多币种兑换网络里任意两币种汇率 = 路径连乘（检测套利环——取对数后变负环检测）；计量单位换算系统把单位当点、换算系数当边；定价引擎推导「经多手中介的到手价」——凡「关系可连乘传递」的领域，带权图 + 路径积是统一抽象，带权并查集是高频查询的落地优化。`,
    keyPoints: [
      "建图：边 a→b 权 v、b→a 权 1/v，查询 = 路径连乘（望远镜积）",
      "c/d 合法 ⟺ c、d 同连通分量，比值由路径积唯一确定",
      "反向边成对取倒数，DFS 方向任意仍正确",
      "进阶两条路：Floyd 全源预处理 / 带权并查集近 O(1) 查询",
    ],
    followUps: [
      "查询量到 10^5 时怎么优化？（Floyd 预处理或带权并查集）",
      "带权并查集中 find 路径压缩时权值怎么更新？union 的权方程怎么推？",
      "怎么检测 equations 本身的矛盾（如 a/b=2 又 a/b=3）？",
    ],
    favorited: false,
  },
];
