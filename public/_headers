# public/_headers
# Cloudflare Pages 响应头配置（2026-07-27 P2 #3 收敛 CORS）
#
# 历史根因：
#   审计发现"页面响应 CORS 全开（ACAO: *）"——HTML 页面响应里带
#   Access-Control-Allow-Origin: *，意味着任意第三方网站都能跨域 fetch 用户的页面内容。
#   虽然没有 Cookie 不会泄露用户数据，但仍暴露内部 API 结构、用户路径等信息。
#
# 闭环解法：
#   1. 静态资源（/_next/static/*、/icons/*、/manifest.json、/sw.js）—— 允许 ACAO:*
#      这些是公开资源，跨域加载是正常需求（CDN、PWA 安装、chunk 加载）
#   2. HTML 页面与 /api/* —— 不设置 ACAO（默认 same-origin，SOP 阻止跨域读）
#   3. next.config.js 的 headers() 也不设置 ACAO（守护测试防回归）
#
# 注意：CF Pages _headers 文件只支持"附加"语义，无法删除 CF dashboard 配置的头。
#       若 ACAO:* 来自 dashboard 配置，需在 Cloudflare 控制台手动移除。
#       本文件确保静态资源仍可跨域加载，且不主动为页面/API 设置 ACAO。
#
# 格式说明：
#   <path-pattern>
#     Header-Name: value
#     Header-Name-2: value2

# 静态资源：允许跨域（chunk 加载、PWA 图标、manifest、service worker）
/_next/static/*
  Access-Control-Allow-Origin: *
  Cache-Control: public, max-age=31536000, immutable

/icons/*
  Access-Control-Allow-Origin: *

/manifest.json
  Access-Control-Allow-Origin: *

/sw.js
  Access-Control-Allow-Origin: *

# 公开数据预设（preset JSON 给社区复用，允许跨域）
/data/*
  Access-Control-Allow-Origin: *

# 注意：/* 与 /api/* 不设置 Access-Control-Allow-Origin
# 浏览器同源策略（SOP）默认阻止跨域读取响应，无需显式拒绝
