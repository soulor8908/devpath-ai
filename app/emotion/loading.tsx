// app/emotion/loading.tsx
// 情绪页路由级加载态（form variant：情绪记录表单骨架）
import { RouteLoading } from "@/components/RouteLoading";

export default function Loading() {
  return <RouteLoading variant="form" />;
}
