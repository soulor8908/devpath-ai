// app/achievements/loading.tsx
// 成就页路由级加载态（list variant：成就卡片列表骨架）
import { RouteLoading } from "@/components/RouteLoading";

export default function Loading() {
  return <RouteLoading variant="list" />;
}
